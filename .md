# 补剂摄入记录系统 - 需求框架（修订版 v7）

> 本版在 v6 基础上修订以下决策：
> - **【v7新增·补录能力】** 支持历史日期补录（过去 30 天内），解决"晚上服用时不在电脑前"的场景。
> - **【v7新增·次日提醒】** 应用启动时检测过去 7 天"计划内未记录"项，主动提示补录。
> - **【v7新增·漏服标记】** 补录弹窗提供"已服用 / 漏服 / 忽略"三选项，漏服写 `status = skipped`，不扣库存。
> - **【v7新增·补录范围】** 手动录入日期可选过去 30 天（可配置），禁止未来日期。
> - **【v7新增·重复检测】** 补录时检测 `date + supplementId + timeSlot` 是否已有记录，提供"追加"或"修改"。
> - **【v7新增·补录展示】** 记录详情同时显示 `date`（服用日期）与 `createdAt`（录入时间），两者不同时标注"补录"。
> - **【v7明确】库存扣减基准**：始终基于当前库存，与记录日期无关。
> - **【v7明确】停药期/计划匹配**：按 `date` 查询，不是按录入时刻。
> 变更处用 `【v7新增】` / `【v7修订】` 标注。

---

## 1. 项目概述

### 1.1 项目目标
构建一个纯前端补剂摄入记录工具，帮助用户追踪每日补剂服用情况、管理补剂库存、分析成分摄入。系统需具备数据安全性、离线可用性。

### 1.2 技术选型
- **构建工具**：Vite
- **前端框架**：React + TypeScript
- **数据存储**：IndexedDB（本地持久化），封装库选用 Dexie.js
- **UI 方案**：shadcn/ui
- **状态管理**：Zustand（仅管理 UI 状态与派生缓存，Dexie 为数据唯一源）
- **路由方案**：React Router
- **PWA 支持**：需要（vite-plugin-pwa，配置 `registerType: 'prompt'`）
- **日期处理**：date-fns（统一使用 YYYY-MM-DD 字符串存储）
- **数据校验**：Zod
- **暗色模式**：不需要
- **国际化**：不需要
- **TanStack Query**：不加
- **目标平台**：PC 网页优先

> **PWA 更新策略**：使用 `registerType: 'prompt'`，新版本就绪时 Toast 提示"新版本已就绪，点击刷新"，由用户主动刷新，避免打卡时被静默打断。Schema 迁移仅在用户确认刷新、新版本代码接管后执行。

### 1.3 核心约束
- **纯前端应用**：无后端服务，所有逻辑在浏览器端运行。
- **本地存储**：所有数据存储在浏览器 IndexedDB 中。
- **数据安全**：数据版本管理与迁移机制；导入前强制备份；全量导出/导入；所有业务表支持软删除（deletedAt）及回收站恢复。
- **离线优先**：PWA 缓存静态资源，数据天然离线可用。
- **隐私合规**：数据仅存本地，不上传任何服务器，需在设置页明确告知用户。

---

## 2. 数据模型

> **通用约定**：
> - **所有业务表均含 `deletedAt: string | 0`**：`0` = 未删除，ISO 时间字符串 = 已删除。**新建记录必须显式写 `0`**，不允许 `undefined`、`null` 或省略。
> - **使用 `0` 而非 `null` 的原因**：IndexedDB 不索引 `null`，若用 `null` 则 `[deletedAt+date]` 等复合索引无法命中未删除记录。`0` 作为哨兵值可正常进索引，且存储层、索引层、导出层统一，Repository 层零转换。
> - Meta 表不加 `deletedAt`（key-value 配置）。
> - 所有日期字段统一 `YYYY-MM-DD` 字符串；所有时间戳统一 ISO 字符串。
> - **【整数修订】所有数量字段（dailyAmount、actualAmount、plannedAmount、stockCountInUsageUnit、amountPerServing 对应的服用维度、deltaInUsageUnit 等）一律为整数（`z.number().int()`）。系统不接受小数服用量。**
>   - 若现实中需要"半片"，请改用更小服用单位（如把 `tablet` 改为 `half_tablet` 不推荐；推荐直接以 `mg` 记成分，或用户自行按整数记录）。
>   - `ml` / `g` 服用单位同样按整数录入（如 5ml、10g），不接受 5.5ml。
>   - 成分含量 `amountPerServing` 若成分单位为 `g` 且实际为 0.5g，请换算为 500mg 并调整成分单位，或直接以 `mg` 为单位录入。

### 2.1 补剂表（Supplement）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 (UUID) |
| name | string | 补剂名称 |
| brand | string? | 品牌（可选） |
| description | string? | 描述/备注（可选） |
| unitType | enum | 服用单位（capsule/tablet/pill/granule/bag/stick/ml/g/scoop/drop） |
| stockCountInUsageUnit | number? | 当前库存数量（按服用单位计，整数，可为负数），可选，支持不记录 |
| stockUnit | string? | 库存展示单位（瓶/盒/袋等），仅用于 UI 换算显示 |
| unitsPerStock | number? | 换算率：1 个 stockUnit 等于多少服用单位（整数）。例：1瓶=60粒 → 60 |
| productionDate | string? | 生产日期 (YYYY-MM-DD) |
| expiryDate | string? | 过期日期 (YYYY-MM-DD) |
| status | enum | active/inactive/finished |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除，ISO 字符串 = 已删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **库存整数化说明**：
> - 库存以**服用单位**存储为整数，**允许为负**（表示"已超支/未补录入库"）。
> - `stockUnit` 和 `unitsPerStock` **仅用于 UI 展示换算**，不参与存储计算。
> - 示例：库存 98 粒，`stockUnit = "瓶"`，`unitsPerStock = 60` → UI 显示"约 1.63 瓶（98 粒）"。
> - 若用户不关心瓶数，`stockUnit` 可留空，UI 只显示"98 粒"。
> - 扣减逻辑：直接 `stockCountInUsageUnit - actualAmount`，无换算，永远整数。
> - **负库存**：当 `stockCountInUsageUnit < 0` 时，UI 以警告色显示，提示"库存为负，请补货或调整"。

### 2.2 成分表（Ingredient）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 成分名称 |
| unit | enum | 成分标准单位（mg/μg/g/IU/ml）。**存储层统一用 `μg`，不接受 `mcg`**；用户输入 `mcg` 时由 Zod 自动转换为 `μg`。IU 不参与 mg/μg 换算。 |
| recommendedDailyIntake | number? | 推荐每日摄入量（可选，整数） |
| upperLimit | number? | 每日上限（可选，整数） |
| description | string? | 描述/备注（可选） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |

> **成分含量说明**：
> - `recommendedDailyIntake` / `upperLimit` / `amountPerServing` 均为整数。
> - 若某成分标准单位为 `g` 但实际推荐 0.5g，请改用 `mg`（500mg）存储。
> - `μg` 为最小单位，若实际小于 1μg，请改用其他表达或合并处理（超出本系统精度范围）。

### 2.3 补剂-成分关联表（SupplementIngredient）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| ingredientId | string | 关联成分 ID |
| amountPerServing | number | 每份含该成分的量（整数） |
| effectiveFrom | string | 生效日期 (YYYY-MM-DD) |
| effectiveTo | string? | 失效日期 (YYYY-MM-DD)，为空表示当前有效 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **配方变更**：不修改原记录，将原记录 `effectiveTo` 设为昨日，新建一条 `effectiveFrom` 为今日的记录。

### 2.4 服用计划表（DosagePlan）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| dailyAmount | number | 每日计划服用量（整数） |
| timeSlots | string[] | 服用时段（morning/noon/evening/bedtime） |
| withMeal | boolean | 是否随餐服用 |
| isActive | boolean | 是否启用 |
| notes | string? | 备注（可选） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

### 2.5 每日摄入记录表（DailyIntake）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | **服用归属日期** (YYYY-MM-DD)。【v7明确】补录时为用户所选历史日期，不是录入时刻。 |
| supplementId | string | 关联补剂 ID |
| planId | string? | 关联 DosagePlan ID（为空表示临时录入） |
| plannedAmount | number? | 计划量快照（整数）。【v7明确】按 `date` 查询当时有效计划快照。 |
| actualAmount | number | 实际服用量（整数） |
| timeSlot | string | 服用时段 |
| status | enum | taken / skipped / extra / partial |
| source | enum | plan / manual |
| notes | string? | 备注（可选） |
| **stockDeducted** | **boolean** | 创建时补剂 `stockCountInUsageUnit != null` → `true`；否则 `false`。不可变，创建后不修改。用于判断该记录当初是否真实扣减过库存。 |
| **stockRollbackState** | **enum** | `'rolled_back'` / `'not_rolled_back'` / `'unknown'`。 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除，ISO 字符串 = 已删除。 |
| createdAt | string | **录入时间** (ISO)。【v7明确】补录时仍为真实写入时间，与 `date` 可不同。 |
| updatedAt | string | 更新时间 (ISO) |

> **【v7新增】`date` 与 `createdAt` 的关系**：
> - `date` = 服用归属日期，决定统计、汇总、完成度、计划/停药期匹配。
> - `createdAt` = 真实录入时间，用于审计与"补录"标注。
> - 两者不同时（`date < createdAt 的日期`），UI 标注"补录"。
>
> **`stockRollbackState` 语义**：
> - `'not_rolled_back'`：库存已扣减，未回滚。对应"记录可见"或"已恢复"。
> - `'rolled_back'`：库存已回滚，未扣减。对应"记录已软删除（撤销）"。
> - `'unknown'`：v3 及更早版本迁移遗留。库存状态未知，不自动重扣。仅用于历史数据，新建记录不产生。
>
> **`stockDeducted` 与 `stockRollbackState` 的关系**：
> - `stockDeducted = false` 时，`stockRollbackState` 仍正确维护（`'not_rolled_back'` ↔ `'rolled_back'`），但不触发库存操作。
> - `stockDeducted = true` 时，`stockRollbackState` 决定是否/如何触发库存回滚或重扣。
>
> **一致性约束**：
> - `deletedAt === 0` → `stockRollbackState === 'not_rolled_back'`
> - `deletedAt !== 0` → `stockRollbackState ∈ { 'rolled_back', 'unknown' }`
> - 由 Zod refine 强制。
>
> **软删除语义（方案A：软删除 = 撤销）**：
> - `deletedAt !== 0` 的 DailyIntake **不计入任何汇总、统计、完成度**。
> - **软删除时立即回滚库存**（仅当 `stockDeducted === true` 且 `stockRollbackState === 'not_rolled_back'`）：`stockCountInUsageUnit += actualAmount`，写入 StockLog（`deltaInUsageUnit = +actualAmount`，reason=`undo_intake`），并将 `stockRollbackState` 置 `'rolled_back'`。
> - **恢复（restore）时重新扣减库存**（仅当 `stockDeducted === true` 且 `stockRollbackState === 'rolled_back'`）：`stockCountInUsageUnit -= actualAmount`，写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`intake`），并将 `stockRollbackState` 置 `'not_rolled_back'`。
> - **`'unknown'` 记录**：恢复时不重扣，仅置 `stockRollbackState = 'not_rolled_back'`，并提示"该记录来自旧版本，库存未回滚，如需修正请手动调整"。
> - **彻底删除（purge）仅清理回收站，不触碰库存**：物理删除。
> - **幂等保障**：任何回滚/重扣操作前先检查 `stockRollbackState` 与 `stockDeducted`，状态已匹配则跳过。
> - **孤儿记录**：若补剂 `stockCountInUsageUnit` 为 null，`stockDeducted = false`，跳过库存逻辑，但 `stockRollbackState` 仍需正确维护。
>
> **【v7新增】补录记录的特殊约定**：
> - 补录记录 `source = manual`。
> - `status` 按用户选择：已服用 → `taken`（若当日有计划则优先 `taken`，量不符则 `partial`）；漏服 → `skipped`；忽略 → 不创建记录。
> - **漏服（`skipped`）不扣库存**，`stockDeducted = false`，`stockRollbackState = 'not_rolled_back'`。
> - 补录若匹配到当日计划，`planId` / `plannedAmount` 按该计划快照；无计划则 `planId = null`。
> - 补录日期在停药期内时，UI 标注"该日为停药日"，但**允许录入**。
> - 补录不计入"今日"统计，按 `date` 计入历史当日统计。

### 2.6 停药期表（PausePeriod）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string? | 关联补剂 ID（为空表示全局停药） |
| startDate | string | 开始日期 (YYYY-MM-DD) |
| endDate | string? | 结束日期 (YYYY-MM-DD)，为空表示持续中 |
| reason | string? | 停药原因（可选） |
| cycleMode | enum | none / oneTime / cyclic |
| cycleStartDate | string? | 周期锚点日期（cycleMode=cyclic 时必填） |
| cycleOnDays | number? | 周期服用天数（整数） |
| cycleOffDays | number? | 周期停药天数（整数） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **周期参数修改语义**：修改 `cycleOnDays`/`cycleOffDays`/`cycleStartDate` **只影响未来计算**，已产生的 DailyIntake 记录不受影响。UI 上修改时提示"此修改仅影响未来的停药计算"。
>
> **startDate 与 cycleStartDate 语义**：
> - `cycleMode = none/oneTime`：仅 `startDate`/`endDate` 生效，`cycleStartDate` 必须为空。
> - `cycleMode = cyclic`：`startDate` 表示停药期**整体生效起点**（date < startDate 不参与计算）；`cycleStartDate` 为周期锚点（取模基准）。通常两者相同，允许不同。
> - cyclic 模式下 `endDate` 若存在，则 `date > endDate` 不再停药；`endDate` 为空表示周期持续有效。
> - 计算顺序：先判断 `startDate <= date <= endDate`（endDate 为空则只判 startDate），再按周期取模。

### 2.7 身体反馈表（BodyFeedback）（P2 可选）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | 日期 (YYYY-MM-DD) |
| energy | number | 精力评分（1-5） |
| sleep | number | 睡眠评分（1-5） |
| digestion | number | 肠胃评分（1-5） |
| mood | number | 情绪评分（1-5） |
| notes | string? | 自由文本备注 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

### 2.8 元数据表（Meta）

| 字段 | 类型 | 说明 |
|------|------|------|
| key | string | 键名 |
| value | any | 值 |

> **预置 Key**：schemaVersion、lastExportAt、appVersion、**backfillWindowDays（【v7新增】补录范围，默认 30）**。
> **导入时不合并 Meta**：导入后保留本地 Meta，仅 `schemaVersion` 按迁移流程更新；`lastExportAt` 由本地导出行为更新。
> **`appVersion` 写入时机**：应用启动时由代码读取构建时注入的 `import.meta.env.VITE_APP_VERSION` 写入 Meta，若已有则更新为当前版本。

### 2.9 库存批次表（StockBatch）（P2 可选）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| quantity | number | 数量（整数，按 unit 计） |
| unit | enum | usage（服用单位）/ stock（库存单位） |
| conversionRate | number? | 换算率（整数，仅当 unit='stock' 时有效） |
| expiryDate | string? | 过期日期 |
| productionDate | string? | 生产日期 |
| isDepleted | boolean | 是否已用完 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

### 2.10 库存流水表（StockLog）（P1）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| deltaInUsageUnit | number | 按服用单位计的变动量（整数，正=入库，负=出库） |
| reason | enum | intake / manual_intake / undo_intake / restock / adjust / purge_rollback（保留兼容） |
| relatedIntakeId | string? | 关联的 DailyIntake ID。单条操作必填；不再产生聚合流水。 |
| note | string? | 手动调整时填原因 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |

> **说明**：
> - 库存流水以**服用单位**为准，与 `stockCountInUsageUnit` 一致，**全整数**。
> - `reason` 枚举语义：
>   - `intake`：打卡 / 恢复（restore）扣减。
>   - `manual_intake`：手动录入扣减（**【v7明确】补录已服用记录使用此 reason**）。
>   - `undo_intake`：软删除（撤销）时回滚。
>   - `restock`：补货入库。
>   - `adjust`：手动调整。
>   - `purge_rollback`：**保留枚举以兼容历史数据，v4 起不再产生新记录**。
> - 流水表默认按时间倒序展示，UI 显示"服用了 2 粒"而非"0.033 瓶"。
> - **放弃批量聚合**：
>   - 每条 DailyIntake 的每次扣减 / 回滚 / 重扣都写**一条独立 StockLog**。
>   - `relatedIntakeId` 始终为该 intake id，单条唯一。
>   - **不再产生"聚合 StockLog"**，不再有 `relatedIntakeId = null` 的批量流水。
>   - 批量操作 = 循环调用单条操作（可置于同一 Dexie 事务）。
>   - 流水量估算：每天 5 条 intake → 每年约 1800～3600 条，10 年约 3.6 万条，IndexedDB 无压力。
> - **幂等**：
>   - 写入 StockLog 前，先检查对应 DailyIntake 的 `stockRollbackState` 与 `stockDeducted`。
>   - `[reason+relatedIntakeId]` **唯一索引**生效，DB 层兜底防止重复写入。
>   - 若违反唯一约束，捕获 `ConstraintError` 并跳过（视为已处理）。
> - **【v7明确】补录的 StockLog 时点**：补录时库存扣减发生在**录入时刻**，`StockLog.createdAt` 为真实写入时间，与 `DailyIntake.date` 无关。
> - **purge 后悬空引用**：purge 物理删除 DailyIntake 后，其 StockLog 保留，`relatedIntakeId` 指向不存在的记录。这是**有意设计**，用于审计追溯。UI 在流水详情中若查不到对应 intake，显示"[记录已彻底删除]"。

### 2.11 单位常量定义（constants/units）

本节为全项目单位体系的**唯一来源**，代码中必须从此处引用，禁止散落魔法字符串。

#### 2.11.1 服用单位（UNIT_TYPE）

```ts
export const UNIT_TYPE = {
  CAPSULE: 'capsule',      // 胶囊
  TABLET: 'tablet',        // 片
  PILL: 'pill',            // 粒 / 丸
  GRANULE: 'granule',      // 颗粒
  BAG: 'bag',              // 袋
  STICK: 'stick',          // 支
  ML: 'ml',                // 毫升
  G: 'g',                  // 克
  SCOOP: 'scoop',          // 勺
  DROP: 'drop',            // 滴
} as const
```

中文显示映射：

```ts
export const UNIT_TYPE_LABEL = {
  capsule: '胶囊',
  tablet: '片',
  pill: '粒',
  granule: '颗粒',
  bag: '袋',
  stick: '支',
  ml: 'ml',
  g: 'g',
  scoop: '勺',
  drop: '滴',
}
```

**规则**：
- 「粒」和「颗粒」为两个独立枚举项，不可混用。
  - 「粒」用于小药丸（鱼油、维E 等）。
  - 「颗粒」用于冲剂颗粒（如小儿感冒颗粒）。
- 所有服用单位一律整数录入，包括 `ml` / `g` / `scoop` / `drop`。
  - 如现实中需 0.5 勺，请以 `g`/`ml` 按整数录入（如 5g），或调整记录方式。
  - Zod 中 `dailyAmount` / `actualAmount` 统一 `z.number().int()`。

#### 2.11.2 成分单位（INGREDIENT_UNIT）

```ts
export const INGREDIENT_UNIT = {
  MG: 'mg',
  MCG: 'μg',       // 存储统一用 μg，不存 mcg
  G: 'g',
  IU: 'IU',
  ML: 'ml',
} as const
```

中文显示映射：

```ts
export const INGREDIENT_UNIT_LABEL = {
  mg: 'mg',
  'μg': 'μg',      // UI 可按用户偏好显示 mcg 或 μg，存储永远是 μg
  g: 'g',
  IU: 'IU',
  ml: 'ml',
}
```

**规则**：
- **存储层只认 `μg`，不存 `mcg`**。
- 用户输入 `mcg` 时，Zod `transform` 自动转为 `μg` 再存。
- UI 显示时可按用户偏好显示 `mcg` 或 `μg`，但存储永远是 `μg`。
- `IU` 不参与 mg/μg 换算，独立累加、独立展示。
- `ml` 用于液体成分，**不参与 μg 归一化，独立累加、独立展示**。
- 成分含量一律整数。若某成分标准单位为 `g` 但实际为 0.5g，请改用 `mg`（500mg）存储。

#### 2.11.3 两个维度的隔离

- `UNIT_TYPE` 与 `INGREDIENT_UNIT` 是**两套独立常量**，代码中不可互相引用或混用。
- 二者都有 `ml` / `g`，但含义不同：
  - `UNIT_TYPE.ml` = 服用形态是液体（每天喝 5ml）。
  - `INGREDIENT_UNIT.ml` = 成分含量以 ml 计（每 5ml 含 10mg 某成分）。
- 建议放在 `constants/units.ts`，导出时明确命名，禁止简写。

---

## 3. 功能模块

### 3.1 今日记录模块

- **计划展示**：按时段分组展示今日服用计划，停药期内置灰并标注"停药中"。
- **一键打卡**：
    - 点击即标记为 taken，即时视觉反馈。
    - **幂等性**：重复点击提示"已打卡"，提供"追加一次"选项。
    - **批量操作**：按时段一键全部打卡。**临期项跳过，不打断批量流程，结果 Toast 中说明**。
    - **库存不足处理**：扣减后 `stockCountInUsageUnit < 0` 时，**允许记录写入**，但 Toast 警告"补剂 X 库存已为负，请补货"。不阻断打卡。
- **手动录入**：记录临时/非计划内服用（source: manual）。同样允许负库存 + 警告。
- **撤销操作**：PC 场景下改为行内撤销按钮——打卡后该行显示"撤销"按钮，不消失，用户随时可撤销。撤销即软删除该记录，会回滚库存（写入 StockLog，reason=undo_intake）并软删除记录。
- **历史修正**：支持对历史记录进行修改、补充、删除。软删除 = 撤销，回滚库存；彻底删除仅清理回收站，不再触碰库存。
- **成分汇总**：实时展示今日各成分摄入总量及来源。**仅统计 `deletedAt === 0` 的记录**。

**【v7新增】补录模块**：

- **入口**：
  - 今日页面顶部"补录昨日"快捷按钮。
  - 手动录入弹窗内含日期选择器，可选过去 30 天。
  - 日历视图点击历史日期，进入详情后可新增记录。
- **日期范围**：默认过去 30 天（`Meta.backfillWindowDays` 可配置），禁止未来日期。
- **次日提醒**：
  - 应用启动时检测过去 7 天"计划内未记录"项（详见 5.8）。
  - 若存在，首页 Banner 提示："过去 7 天有 N 项计划未记录，是否补录？"
  - 点击进入补录弹窗，按日期分组列出未记录项。
- **补录弹窗**：
  - 日期选择器（默认今天，可选过去 30 天）。
  - 按所选日期查询当时有效计划与停药状态。
  - 每项提供三个选项：
    - **已服用** → 创建 `status = taken`（量不符则 `partial`），扣库存。
    - **漏服** → 创建 `status = skipped`，**不扣库存**。
    - **忽略** → 不创建记录。
  - 支持批量选择"全部已服用 / 全部漏服"。
- **重复检测**：
  - 补录时检查该 `date + supplementId + timeSlot` 是否已有未删除记录。
  - 若已有：提示"该时段已有记录"，提供"追加"（`status = extra`）或"修改原记录"。
- **补录展示**：
  - 记录详情同时显示"服用日期"（`date`）与"录入时间"（`createdAt`）。
  - `date` 与 `createdAt` 日期不同时，标注"补录"。
- **库存扣减**：补录"已服用"时按 `actualAmount` 扣当前库存，与 `date` 无关。若扣减后为负，Toast 警告，不阻断。
- **统计归属**：补录记录按 `date` 计入历史当日统计与完成度；不影响"今日"统计。

### 3.2 补剂库管理模块
- **CRUD**：补剂的增删改查。
- **成分关联**：关联多个成分，设定每份含量，支持配方版本管理。
- **库存管理**：
    - 记录库存数量（**按服用单位计，整数**），支持"不记录"模式。
    - **自动扣减**：打卡/保存记录时直接扣减整数；软删除（撤销）时回滚整数；恢复时重新扣减；purge 不触发库存变化。
    - **一致性**：库存变动与 DailyIntake 增删改强一致，统一由 Service 层处理。
    - **库存流水**：每次变动写入 StockLog（按服用单位计）。
    - **负库存展示**：`stockCountInUsageUnit < 0` 时以警告色显示，并在库存列表顶部 Banner 提示。
- **预警系统**：
    - **临期提醒**：默认 30 天内，**单独打卡时**二次确认；批量打卡时跳过并说明。
    - **库存不足**：低于阈值提醒（默认剩余 < 7 天用量）。**负库存始终高亮提醒**。
    - **展示**：库存同时展示服用单位和换算后的库存单位（"98 粒 / 约 1.63 瓶"）。

### 3.3 成分库管理模块
- **CRUD**：成分增删改查。
- **标准设定**：推荐每日摄入量和每日上限。
- **来源追溯**：查看某成分被哪些补剂包含。

### 3.4 服用计划模块
- **计划设定**：每日服用量、时段。
- **启停控制**：启用/停用计划。
- **联动逻辑**：停药期间计划不执行，但保留展示（置灰）。

### 3.5 停药期管理模块
- **全局/局部**：指定补剂或全局的停药期。
- **模式支持**：一次性、周期性（锚点日期 + 吃 N 天停 M 天）。
- **计算逻辑**：`isPaused(supplementId, date)` 纯函数，**补剂级与全局为并集**——任一覆盖该日即停药。返回 `{ paused: boolean, reasons: PausePeriod[] }`，便于 UI 展示触发原因。
- **可视化**：日历或列表展示当前及历史停药期。

### 3.6 每日成分汇总视图
- **总量统计**：按日期查看当天所有成分摄入总量。**仅统计 `deletedAt === 0` 的 DailyIntake**。【v7明确】补录记录按 `date` 计入对应历史日期。
- **合规检查**：与推荐量/上限对比。只提醒超标，不足不主动提示。
- **来源明细**：展示每种成分来源于哪些补剂。
- **计算边界**：仅按标签量累加，不考虑生物利用度；**汇总包含孤儿记录（补剂被软删除但 DailyIntake 未删除），标注"含已删除补剂"**。
- **软删除语义**：已软删除的 DailyIntake **完全不计入**，不显示、不标注。

### 3.7 日历视图
- **热力图**：展示每日服用完成度。
    - **完成度公式**：已服计划项 / (应服计划项 - 停药期排除项)。**仅统计 `deletedAt === 0` 的 DailyIntake**。手动录入不计入完成度；分母为 0 显示"无计划"灰色。
    - **"应服计划项"定义**：按 `DosagePlan.timeSlots` 展开为条目数。一个计划含 morning+evening → 算 2 项。
    - **标记**：漏服（黄）、多服（红）。
    - **【v7明确】补录记录计入对应 `date` 的完成度**，不影响今日。
- **详情交互**：点击某天查看当日详细摄入记录（右侧栏展示）。
- **【v7新增】补录入口**：日期详情右侧栏提供"新增记录"按钮，日期固定为所选日期。

### 3.8 统计报表（P2 可选）
- 周/月服用率统计。
- 各成分平均日摄入量趋势。
- 身体反馈与服用记录关联展示。

### 3.9 数据管理模块
- **导出**：JSON 格式，包含 meta 和 data。导出默认包含已软删除记录。目标为 gzip 压缩后 < 5MB。导出 JSON 中 `deletedAt` 为 `0` 或 ISO 字符串。
- **导入**：
    - **强制备份**：导入前自动导出当前数据备份。
    - **策略选择**："覆盖全部"或"智能合并"（按 updatedAt 较新者为准）。
    - **版本处理**：Schema 版本不一致时先迁移再导入。
    - **软删除尊重**：合并时尊重 deletedAt 状态。
    - **导入结果摘要**：新增 X 条、覆盖 Y 条、保留本地 Z 条。
    - **去掉"跳过已删除记录"选项**：统一走 `mergeRecord`（较新者为准），避免语义歧义。导入文件中已删除的记录会按 updatedAt 参与合并。
    - **Meta 不合并**：导入后保留本地 Meta，仅 schemaVersion 更新。
    - **状态异常记录不静默修正**：列入异常清单，用户人工处理（详见 5.7）。
    - **旧版导入兼容**：若导入文件使用 `deletedAt: null`（v4 及更早），导入时自动转为 `0`。
- **版本迁移**：Schema 变更时自动迁移，迁移函数幂等、可测试。
- **清除数据**：二次确认的全量清除（含物理删除已软删除记录）。
- **回收站**：
    - **分表展示**：按表（补剂/成分/关联/计划/记录/停药期）分 Tab 展示，每 Tab 列出该表所有 `deletedAt !== 0` 的记录。
    - 支持**恢复**（deletedAt 置 `0`）、**彻底删除**（物理删除）、**一键清空**（二次确认）。
    - **彻底删除 DailyIntake 仅清理回收站，不触碰库存**。
    - **批量操作 = 循环单条**：不聚合 StockLog，每条 intake 一条流水。
    - **恢复 DailyIntake 时重新扣减库存**：`stockCountInUsageUnit -= actualAmount`，写入 StockLog（reason=intake），`stockRollbackState` 置 `'not_rolled_back'`。
    - **`'unknown'` 记录在回收站中高亮标注**，恢复时提示库存未回滚。

---

## 4. 页面结构

### 4.1 首页 / 今日
- 今日服用计划清单（按时段分组，停药置灰）。
- 一键打卡入口。
- **【v7新增】"补录昨日"快捷按钮**。
- **【v7新增】次日补录提醒 Banner**（过去 7 天有未记录项时显示）。
- 今日成分摄入汇总卡片。
- 库存/过期预警 Banner（负库存高亮）。
- 空状态引导。

### 4.2 补剂库
- 补剂列表（表格，标记库存/过期状态，负库存警告色）。
- 补剂详情/编辑（侧边抽屉）。
- 成分关联管理（含版本历史）。

### 4.3 成分库
- 成分列表（表格）。
- 成分详情/编辑。

### 4.4 服用计划
- 计划列表。
- 计划编辑。

### 4.5 停药期
- 当前生效的停药期列表。
- 历史停药期记录。
- 新增/编辑停药期。

### 4.6 日历
- 日历视图主页面（热力图）。
- 日期详情右侧栏。
- **【v7新增】日期详情"新增记录"按钮**（补录入口）。

### 4.7 统计（P2）
- 报表页面。

### 4.8 设置
- 数据导出/导入。
- 清除数据。
- 回收站入口（分表 Tab）。
- 默认值集中设置（库存预警天数、临期提醒天数、默认服用时段、**补录范围天数**）。
- 数据统计面板（各表记录数、占用空间估算）。
- 隐私说明。
- 关于/版本信息。

---

## 5. 功能逻辑规范

### 5.1 打卡幂等性与状态
- **默认行为**：同一补剂、同一时段、同一天，重复点击提示"已打卡"，不产生新记录。
- **追加模式**：点击"追加"按钮，创建新记录。追加创建 `source=manual, status=extra`，planId 可空或保留原 planId 仅作参考。
- **状态流转**：
    - taken：正常服用。
    - skipped：计划内漏服（仅手动标记，不自动）。
    - extra：计划外多服。
    - partial：服用量与计划不符。
- **合法组合矩阵**：

| source | 合法 status |
|--------|-------------|
| plan | taken / skipped / partial |
| manual | taken / extra / partial |

（`source=plan` 不允许 extra；`source=manual` 不允许 skipped。）

> **【v7修订】补录的合法组合**：
> - 补录"已服用"且匹配到计划 → `source = manual`，`status = taken`（量不符 → `partial`）。
> - 补录"漏服" → `source = manual`，`status = skipped`。
> - **例外说明**：`source = manual + status = skipped` 是 v7 为补录场景新开的合法组合（v6 中不允许）。因为补录是用户事后手动标记，不是计划自动生成，故 `source` 仍为 `manual`。
> - 更新后的合法组合矩阵：

| source | 合法 status |
|--------|-------------|
| plan | taken / skipped / partial |
| manual | taken / extra / partial / **skipped（仅补录漏服）** |

### 5.2 库存扣减一致性
- **触发时机**：DailyIntake 创建、更新、**软删除（撤销）**、**恢复**时。**彻底删除（purge）不触发库存变化。**
- **事务保障**：库存扣减与记录写入必须在同一逻辑事务中，失败则全部回滚。
- **历史记录**：修改历史记录时，计算 delta = newAmount - oldAmount，更新库存。
- **临时录入**：source: manual 的记录同样扣减库存。
- **不记录模式**：若 `stockCountInUsageUnit` 为 null，`stockDeducted = false`，跳过扣减。
- **负库存允许**：扣减后若为负，**仍写入**，仅警告。
- **软删除（撤销）触发库存回滚**：`stockCountInUsageUnit += actualAmount`（仅 `stockDeducted === true && stockRollbackState === 'not_rolled_back'`），写入 StockLog（`deltaInUsageUnit = +actualAmount`，reason=`undo_intake`），`stockRollbackState` 置 `'rolled_back'`。
- **恢复（restore）触发库存重扣**：`stockCountInUsageUnit -= actualAmount`（仅 `stockDeducted === true && stockRollbackState === 'rolled_back'`），写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`intake`），`stockRollbackState` 置 `'not_rolled_back'`。
- **彻底删除（purge）不触发库存变化**：仅物理删除记录。
- **幂等保障**：任何回滚/重扣前先检查 `stockRollbackState` 与 `stockDeducted`，状态已匹配则跳过。

**【v7新增】补录的库存扣减规则**：

- **补录"已服用"**：扣当前库存（`stockCountInUsageUnit -= actualAmount`），写 StockLog（reason=`manual_intake`），与 `date` 无关。
- **补录"漏服"**：不扣库存，`stockDeducted = false`。
- **补录"忽略"**：不创建记录。
- **库存基准**：始终基于**当前库存**，不回溯历史。若用户今天已补货，补录昨天会从"补货后的库存"里扣，这是正确行为（库存反映当前状态）。
- **负库存**：扣减后为负仍写入，Toast 警告。

### 5.3 停药期计算逻辑
- **纯函数**：`isPaused(supplementId: string, date: string): { paused: boolean, reasons: PausePeriod[] }`。
- **并集语义**：若该补剂存在任一 PausePeriod 覆盖 date，**或**全局存在任一 PausePeriod 覆盖 date，则 `paused = true`。补剂级与全局是**并集**，不存在豁免概念。
- **周期性计算**：
  1. 先判断 `startDate <= date <= endDate`（endDate 为空则只判 startDate）。
  2. 若通过，计算 `diff = date - cycleStartDate`，`remainder = diff % (cycleOnDays + cycleOffDays)`。
  3. 若 `remainder >= cycleOnDays` 则为停药日。`cycleStartDate` 当天算周期第 1 天（吃）。
- **今日计划**：停药期间计划仍展示，但 disabled=true。
- **【v7明确】补录按 `date` 查询停药状态**，不是按录入时刻。若所选日期为停药日，UI 标注"该日为停药日"，但允许录入。

### 5.4 成分汇总计算
- **单位统一**：
    - **重量类成分（mg/μg/g）**：先归一化到 `μg`。
        - `1 mg = 1000 μg`
        - `1 g = 1000000 μg`
        - `1 mcg = 1 μg`（输入时已转）
    - **IU**：独立累加，不与 μg 混算。
    - **ml**：独立累加，**不参与 μg 归一化**（体积不能转质量）。
    - **显示时按数值大小自动选单位（仅重量类）**：
        - `< 1000 μg` → 显示 μg
        - `≥ 1000 μg 且 < 1000000 μg` → 显示 mg
        - `≥ 1000000 μg` → 显示 g
    - 同一天同一成分如果同时有 IU 和 μg 记录，**分两行显示**，不合并。ml 同理。
    - **单位选择按"合计后数值"判定**：同一天同一成分的多条记录先合计，再按合计值选单位。UI 在明细中说明"合计值按大小自动选单位"。
- **版本匹配**：根据 DailyIntake.date 匹配当时有效的 SupplementIngredient。**匹配时 includeDeleted=true**（因为历史记录引用的关联可能已被软删除）。
- **软删除过滤**：仅统计 `DailyIntake.deletedAt === 0` 的记录。
- **孤儿记录处理**：补剂被软删除后，其 DailyIntake 仍计入汇总，标注"含已删除补剂"。
- **关联缺失处理**：若 DailyIntake 对应的 SupplementIngredient 已彻底删除，无法计算成分，UI 标注"成分数据缺失"。
- **边界说明**：UI 上明确告知"数据基于标签含量累加，不代表医学精确摄入量"。
- **【v7明确】补录记录按 `date` 计入汇总**，与普通记录无差别。

### 5.5 数据导入策略
> 合并逻辑统一定义在 5.7，本节仅列流程。

- **预检查**：校验 JSON 结构、Schema 版本，扫描状态异常记录（详见 5.7）。
- **备份**：导入前自动触发 exportData()。
- **策略**：覆盖全部 / 智能合并（详见 5.7）。
- **版本处理**：Schema 版本低时先迁移。
- **结果摘要**：导入完成后展示统计，含异常清单。
- **Meta 处理**：不合并，导入后保留本地 Meta，仅 schemaVersion 按迁移流程更新。
- **旧版兼容**：导入文件若使用 `deletedAt: null`，自动转为 `0`。

### 5.6 库存扣减逻辑

**核心规则**：
- 库存以**服用单位**存储为整数（`stockCountInUsageUnit`），**允许为负**。
- 扣减公式：`新库存 = 原库存 - actualAmount`。
- 回滚公式：`新库存 = 原库存 + actualAmount`。
- 无需换算，无需精度处理。

**状态字段**：
- `stockDeducted`：创建时是否真实扣减。不可变。
- `stockRollbackState`：`'not_rolled_back'` / `'rolled_back'` / `'unknown'`。

**UI 换算显示**：
- 若 `stockUnit` 和 `unitsPerStock` 存在：
    - 显示库存单位数 = `stockCountInUsageUnit / unitsPerStock`（保留 1~2 位小数展示）。
    - 同时显示："98 粒 / 约 1.63 瓶"。
- 若 `stockUnit` 为空或与 `unitType` 相同：
    - 只显示"98 粒"。
- **负库存显示**：显示"-3 粒（库存不足）"，警告色。

**边界情况**：
| 情况 | 处理 |
|------|------|
| `stockCountInUsageUnit` 为 null | `stockDeducted = false`，跳过库存逻辑，但 `stockRollbackState` 仍需维护 |
| `stockUnit != null && stockUnit !== unitType && unitsPerStock == null` | Zod 校验拦截，提示"请填写每瓶含量" |
| 用户只想记"还剩几粒" | `stockUnit` 留空，只填 `stockCountInUsageUnit` |
| 扣减后库存 < 0 | **允许写入**，Toast 警告"库存已为负" |
| `stockUnit === unitType` | `unitsPerStock` 可空，UI 只显示服用单位 |

**打卡扣减流程**：
1. 读取 `stockCountInUsageUnit`。
2. 若为 null：
   - `stockDeducted = false`，跳过库存扣减。
   - `stockRollbackState = 'not_rolled_back'`。
3. 若不为 null：
   - `stockDeducted = true`。
   - 更新 `stockCountInUsageUnit -= actualAmount`（允许变负）。
   - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=intake）。
   - `stockRollbackState = 'not_rolled_back'`。
4. 若扣减后 < 0，Toast 警告。

**【v7新增】补录扣减流程**：

1. 用户选择日期 `date`（过去 30 天内）与补录项。
2. 若选择"漏服"：
   - 创建 `status = skipped`，`source = manual`，`stockDeducted = false`。
   - 不扣库存，不写 StockLog。
3. 若选择"已服用"：
   - 读取 `stockCountInUsageUnit`。
   - 若为 null：`stockDeducted = false`，跳过扣减。
   - 若不为 null：
     - `stockDeducted = true`。
     - `stockCountInUsageUnit -= actualAmount`（允许变负）。
     - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`manual_intake`）。
   - `stockRollbackState = 'not_rolled_back'`。
   - 若扣减后 < 0，Toast 警告。
4. 若选择"忽略"：不创建记录。
5. 库存扣减发生在**录入时刻**，与 `date` 无关。

**软删除（撤销）流程**：
1. 读取 DailyIntake。
2. 若 `deletedAt !== 0`，跳过（幂等）。
3. 若 `stockDeducted === true && stockRollbackState === 'not_rolled_back'`：
   - 读取 `stockCountInUsageUnit`，若不为 null：`+= actualAmount`。
   - 写入 StockLog（`deltaInUsageUnit = +actualAmount`，reason=undo_intake）。
4. 置 `deletedAt = now`，`stockRollbackState = 'rolled_back'`，更新 `updatedAt`。
5. `stockDeducted` 不变。

**恢复（restore）流程**：
1. 读取 DailyIntake。
2. 若 `deletedAt === 0`，跳过（幂等）。
3. 若 `stockRollbackState === 'unknown'`：
   - 不重扣。
   - 置 `deletedAt = 0`，`stockRollbackState = 'not_rolled_back'`，更新 `updatedAt`。
   - Toast 提示"该记录来自旧版本，库存未回滚，如需修正请手动调整"。
4. 若 `stockDeducted === true && stockRollbackState === 'rolled_back'`：
   - 读取 `stockCountInUsageUnit`，若不为 null：`-= actualAmount`。
   - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=intake）。
5. 置 `deletedAt = 0`，`stockRollbackState = 'not_rolled_back'`，更新 `updatedAt`。

**彻底删除（purge）流程**：
1. 物理删除 DailyIntake 记录。
2. **不触碰库存，不写 StockLog**。
3. StockLog 中 `relatedIntakeId` 指向该 intake 的记录**保留**，形成悬空引用。

**批量操作流程**：
- **批量软删除**：循环调用单条软删除逻辑（同一 Dexie 事务）。
- **批量恢复**：循环调用单条恢复逻辑。
- **批量 purge**：仅物理删除，不触碰库存。
- 不做聚合，不合并 StockLog。

**代码示例**：

```ts
// 软删除（撤销）
async function softDeleteIntake(id: string) {
  await db.transaction('rw', db.dailyIntakes, db.supplements, db.stockLogs, async () => {
    const intake = await db.dailyIntakes.get(id);
    if (!intake || intake.deletedAt !== 0) return;

    if (intake.stockDeducted && intake.stockRollbackState === 'not_rolled_back') {
      const supp = await db.supplements.get(intake.supplementId);
      if (supp && supp.stockCountInUsageUnit != null) {
        await db.supplements.update(supp.id, {
          stockCountInUsageUnit: supp.stockCountInUsageUnit + intake.actualAmount,
          updatedAt: new Date().toISOString(),
        });
        await db.stockLogs.add({
          id: crypto.randomUUID(),
          supplementId: supp.id,
          deltaInUsageUnit: intake.actualAmount,
          reason: 'undo_intake',
          relatedIntakeId: intake.id,
          note: null,
          deletedAt: 0,
          createdAt: new Date().toISOString(),
        });
      }
    }

    await db.dailyIntakes.update(id, {
      deletedAt: new Date().toISOString(),
      stockRollbackState: 'rolled_back',
      updatedAt: new Date().toISOString(),
    });
  });
}

// 恢复
async function restoreIntake(id: string) {
  await db.transaction('rw', db.dailyIntakes, db.supplements, db.stockLogs, async () => {
    const intake = await db.dailyIntakes.get(id);
    if (!intake || intake.deletedAt === 0) return;

    if (intake.stockRollbackState === 'unknown') {
      await db.dailyIntakes.update(id, {
        deletedAt: 0,
        stockRollbackState: 'not_rolled_back',
        updatedAt: new Date().toISOString(),
      });
      toast('该记录来自旧版本，库存未回滚，如需修正请手动调整');
      return;
    }

    if (intake.stockDeducted && intake.stockRollbackState === 'rolled_back') {
      const supp = await db.supplements.get(intake.supplementId);
      if (supp && supp.stockCountInUsageUnit != null) {
        await db.supplements.update(supp.id, {
          stockCountInUsageUnit: supp.stockCountInUsageUnit - intake.actualAmount,
          updatedAt: new Date().toISOString(),
        });
        await db.stockLogs.add({
          id: crypto.randomUUID(),
          supplementId: supp.id,
          deltaInUsageUnit: -intake.actualAmount,
          reason: 'intake',
          relatedIntakeId: intake.id,
          note: null,
          deletedAt: 0,
          createdAt: new Date().toISOString(),
        });
      }
    }

    await db.dailyIntakes.update(id, {
      deletedAt: 0,
      stockRollbackState: 'not_rolled_back',
      updatedAt: new Date().toISOString(),
    });
  });
}
```

### 5.7 软删除与回收站逻辑

**设计原则**：
- 所有业务表增加 `deletedAt: string | 0`：`0` = 未删除，ISO 字符串 = 已删除。
- 新建记录必须显式写 `deletedAt: 0`。
- Meta 表不加。
- **软删除 = 撤销（方案A）**：
  - DailyIntake 软删除后不计入任何汇总、统计、完成度，立即回滚库存（仅 `stockDeducted === true && stockRollbackState === 'not_rolled_back'`）。
  - 恢复时重新扣减库存（仅 `stockDeducted === true && stockRollbackState === 'rolled_back'`）。
  - `'unknown'` 记录恢复时不重扣。
  - 彻底删除仅清理回收站，不触碰库存。
  - `stockRollbackState` + `stockDeducted` 保证幂等。
- 非 DailyIntake 表软删除不涉及库存，仅打标记。

**查询过滤**：
- Repository 读操作默认过滤 `deletedAt === 0`。
- 提供 `includeDeleted` 选项。
- **索引策略**：`deletedAt` 直接使用 `0` / ISO 字符串，可进复合索引。存储层与索引层统一，Repository 层零转换。
- **查询示例**：
  - 未删除：`db.dailyIntakes.where('[deletedAt+date]').between([0, startDate], [0, endDate])`
  - 回收站：`db.dailyIntakes.filter(r => r.deletedAt !== 0).toArray()`

**删除与恢复**：

```ts
// 通用软删除（非 DailyIntake）
async function softDelete(table, id) {
  await db[table].update(id, {
    deletedAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  });
}

// 通用恢复（非 DailyIntake）
async function restore(table, id) {
  await db[table].update(id, {
    deletedAt: 0,
    updatedAt: new Date().toISOString(),
  });
}
```

（DailyIntake 软删除/恢复见 5.6 代码示例。）

**级联删除策略**：
| 被删主表 | 关联表 | 策略 |
|----------|--------|------|
| Supplement | DosagePlan | 级联软删除 |
| Supplement | SupplementIngredient | 级联软删除 |
| Supplement | DailyIntake | 不删除（保留孤儿数据） |
| Supplement | StockLog | 保留 |
| Supplement | PausePeriod | 不级联（用户自行决定） |

**级联删除确认交互**：删除补剂时弹确认框，列出影响范围（"将同时删除 2 个计划、3 条成分关联"），用户确认后执行。

**孤儿数据处理**：
- DailyIntake 关联的补剂被软删除后，汇总视图显示"[已删除的补剂]"，但仍计入成分总量。
- StockLog 中补剂被删后，流水保留，补剂名显示"[已删除]"。
- **purge 后 StockLog 的悬空引用**：
  - purge 物理删除 DailyIntake 后，其 StockLog 保留，`relatedIntakeId` 指向不存在的记录。
  - 这是**有意设计**，用于审计追溯。
  - UI 在流水详情中若查不到对应 intake，显示"[记录已彻底删除]"。
  - `[reason+relatedIntakeId]` 唯一索引对此无影响（悬空 id 不会与新 intake 冲突，因 UUID 不复用）。

**导入合并逻辑**：

```ts
function mergeRecord(local, incoming) {
  if (!local) return incoming;
  const localTime = new Date(local.updatedAt).getTime();
  const incomingTime = new Date(incoming.updatedAt).getTime();
  if (incomingTime !== localTime) {
    return incomingTime > localTime ? incoming : local;
  }
  // 时间相同时，优先取 deletedAt !== 0 的一方，避免丢失删除意图
  if (local.deletedAt !== 0 && incoming.deletedAt === 0) return local;
  if (incoming.deletedAt !== 0 && local.deletedAt === 0) return incoming;
  return local;
}
```

- 删除操作更新 `updatedAt`，保证删除状态在合并中被尊重。
- 不再提供"跳过已删除记录"选项，统一走 mergeRecord。
- 导入结果摘要：新增 X 条、覆盖 Y 条、保留本地 Z 条。

**导入时状态异常处理**：

**不静默修正**。导入前预扫描所有 DailyIntake，找出状态不一致记录：

| 类型 | 条件 | 含义 |
|------|------|------|
| A | `deletedAt !== 0 && stockRollbackState === 'not_rolled_back'` | 已删除但未回滚 |
| B | `deletedAt === 0 && stockRollbackState === 'rolled_back'` | 未删除但已回滚 |
| C | `deletedAt === 0 && stockRollbackState === 'unknown'` | 未删除但状态未知 |
| D | `deletedAt !== 0 && stockRollbackState === 'unknown'` | 已删除且状态未知（v3 遗留，正常） |

**处理规则**：
- 类型 A、B、C 记录**按 mergeRecord 正常合并，但不修正标志位**。
- 合并后若 Zod 校验拒绝（因一致性约束），则该记录**跳过导入**，计入异常清单。
- 类型 D 正常合并（`'unknown'` 在 `deletedAt !== 0` 下合法）。
- 导入完成后摘要显示："发现 N 条状态异常记录，建议人工核对"，提供"查看清单"入口。
- 异常清单中每条记录提供"查看详情"跳转，用户可手动处理（重新软删除、恢复、调整库存）。

**导入 DailyIntake 的 `stockDeducted` 处理**：
- 导入文件若含 `stockDeducted` 字段，直接采用。
- 若为旧版文件（v5 及更早，无该字段）：
  - `deletedAt === 0` 的记录：`stockDeducted = (supp.stockCountInUsageUnit != null)`。
  - `deletedAt !== 0` 的记录：`stockDeducted = (stockRollbackState === 'rolled_back')`。
- 在摘要中提示"已为 N 条旧版记录推断 stockDeducted"。

**回收站功能**：
- **分表 Tab 展示**：补剂 / 成分 / 关联 / 计划 / 记录 / 停药期。
- 每 Tab 列出该表 `deletedAt !== 0` 的记录。
- 操作：恢复、彻底删除、一键清空（二次确认）。
- **恢复 DailyIntake 触发库存重扣**（详见 5.6）。
- **彻底删除 DailyIntake 仅物理删除，不触碰库存**。
- **批量操作 = 循环单条**，不聚合 StockLog。
- **`'unknown'` 记录在回收站中高亮标注**，恢复时提示库存未回滚。

### 5.8 补录逻辑（v7 新增）

**核心原则**：
- 补录 = 用户为历史日期手动创建记录。
- 补录不改变 `date` 语义，统计按 `date` 归属。
- 库存扣减发生在**录入时刻**，与 `date` 无关。
- 计划/停药期匹配按 `date` 查询。

**补录范围**：
- 默认过去 30 天（`Meta.backfillWindowDays` 可配置）。
- 禁止未来日期（Zod 拦截）。
- 超出范围：UI 提示"超出补录范围，请前往日历手动修正"或直接禁用。

**次日提醒检测逻辑**：

```ts
// 应用启动时调用
async function detectMissedPlans(): Promise<MissedPlan[]> {
  const today = format(new Date(), 'yyyy-MM-dd');
  const lookbackDays = 7;
  const startDate = subDays(new Date(), lookbackDays);

  // 1. 查询过去 7 天每日应服计划项
  const missed: MissedPlan[] = [];
  for (let i = 1; i <= lookbackDays; i++) {
    const date = format(subDays(new Date(), i), 'yyyy-MM-dd');
    const plans = await getActivePlansForDate(date); // 排除停药日
    for (const plan of plans) {
      for (const timeSlot of plan.timeSlots) {
        // 2. 检查该 date + supplementId + timeSlot 是否有未删除记录
        const existing = await db.dailyIntakes
          .where('[date+supplementId]')
          .equals([date, plan.supplementId])
          .filter(r => r.deletedAt === 0 && r.timeSlot === timeSlot)
          .first();
        if (!existing) {
          missed.push({ date, supplementId: plan.supplementId, timeSlot, plan });
        }
      }
    }
  }
  return missed;
}
```

- 检测范围：过去 7 天（不含今天，今天由今日页面处理）。
- 排除停药日：`isPaused(supplementId, date).paused === true` 的日期不计入。
- 排除已软删除记录：`deletedAt === 0` 才算"已记录"。
- 结果按日期分组展示。

**补录弹窗交互**：
1. 用户选择日期（默认今天，可选过去 30 天）。
2. 系统查询该日有效计划与停药状态。
3. 列出所有计划项与时段，每项提供三选项：
   - **已服用**：输入实际量（默认计划量），创建 `status = taken`（量不符 → `partial`）。
   - **漏服**：创建 `status = skipped`，`source = manual`。
   - **忽略**：不创建记录。
4. 支持"全部已服用 / 全部漏服"批量选择。
5. 提交时对每项执行重复检测：
   - 若已有记录：提示"该时段已有记录"，提供"追加"或"修改原记录"。
   - 无记录：正常创建。

**补录记录的写入规则**：

| 用户选择 | status | source | stockDeducted | 库存操作 | StockLog reason |
|----------|--------|--------|---------------|----------|-----------------|
| 已服用（量匹配计划） | taken | manual | 按库存是否为 null | 扣减 | manual_intake |
| 已服用（量不符计划） | partial | manual | 按库存是否为 null | 扣减 | manual_intake |
| 漏服 | skipped | manual | false | 不扣 | 不写 |
| 忽略 | — | — | — | — | — |

**补录的计划匹配**：
- 按 `date` 查询该日有效 `DosagePlan`（`isActive = true`，且未软删除）。
- 若匹配到计划：
  - `planId` = 该计划 id。
  - `plannedAmount` = 该计划当时的 `dailyAmount`（快照）。
- 若无计划：
  - `planId = null`。
  - `plannedAmount = null`。
  - `status` 仅允许 `taken` / `extra` / `partial`。

**补录的停药期匹配**：
- 按 `date` 调用 `isPaused(supplementId, date)`。
- 若为停药日：UI 标注"该日为停药日"，但**允许录入**（用户可能想补记漏服或额外服用）。
- 停药日补录不计入"应服计划项"（完成度分母不含）。

**补录的展示**：
- 记录详情中同时显示：
  - 服用日期（`date`）。
  - 录入时间（`createdAt`）。
- 若 `date !== format(createdAt, 'yyyy-MM-dd')`，标注"补录"。
- 日历视图中，补录记录正常显示，无特殊标记（按 `date` 归属）。

**补录的库存扣减**：
- 与普通手动录入一致，扣当前库存。
- 扣减后为负仍写入，Toast 警告。
- 不回溯历史库存。

**补录的统计归属**：
- 按 `date` 计入历史当日成分汇总、完成度、日历热力图。
- 不影响"今日"统计。
- 补录"漏服"记录计入完成度分母（作为应服项），但不计入分子（未服用）。

---

## 6. 技术实现规范

### 6.1 数据库与迁移

- **Dexie 版本**：`db.version(n).stores({...}).upgrade(tx => {...})`。
- **迁移脚本**：放在 `db/migrations/`，确保幂等。
- **Meta 同步**：迁移完成后更新 `Meta.schemaVersion`。

**v3 → v4 迁移**：

- DailyIntake 新增 `stockDeducted: boolean`。
- DailyIntake 新增 `stockRollbackState: 'rolled_back' | 'not_rolled_back' | 'unknown'`。
- **v3 已软删除的记录**：`stockRollbackState = 'unknown'`，`stockDeducted = true`。
  - 不修改库存。
  - UI 提示用户"历史软删除记录库存未回滚，恢复时不会自动重扣，如需修正请手动调整"。
- **v3 未删除的记录**：`stockRollbackState = 'not_rolled_back'`，`stockDeducted = (supp.stockCountInUsageUnit != null)`。

**v4 → v5 迁移**：

- 所有表的 `deletedAt` 字段：`null` → `0`。
- 已是 `0` 或 ISO 字符串的跳过（幂等）。

**v5 → v6 迁移**：

- DailyIntake 新增 `stockDeducted: boolean`：
  - `deletedAt === 0` 的记录：`stockDeducted = (supp.stockCountInUsageUnit != null)`。
  - `deletedAt !== 0` 的记录：`stockDeducted = stockRolledBack`。
- DailyIntake 的 `stockRolledBack: boolean` 转换为 `stockRollbackState`：
  - `stockRolledBack === true` → `'rolled_back'`。
  - `stockRolledBack === false` → `'not_rolled_back'`。
- 删除 `stockRolledBack` 字段。
- 幂等：已是 `stockRollbackState` 的记录跳过。

**v6 → v7 迁移（v7 新增）**：

- **无 Schema 变更**。补录能力复用现有字段（`date` / `createdAt` / `source` / `status`），不新增字段。
- Meta 新增 `backfillWindowDays`，默认 `30`，若已存在则跳过。
- 幂等。
- **说明**：v7 的补录是纯功能层新增，不改数据模型。已有 v6 数据无需迁移。

### 6.2 索引设计

- **DailyIntake**：`[date+supplementId]`, `[date+timeSlot]`, `planId`, `date`, `[deletedAt+date]`, `stockRollbackState`, `stockDeducted`
- **DosagePlan**：`supplementId`, `isActive`, `[deletedAt+isActive]`
- **PausePeriod**：`supplementId`, `startDate`, `endDate`, `deletedAt`
- **SupplementIngredient**：`[supplementId+ingredientId]`, `effectiveFrom`, `deletedAt`
- **Supplement**：`[deletedAt+status]`
- **Ingredient**：`deletedAt`
- **StockLog**：`supplementId`, `createdAt`, **`[reason+relatedIntakeId]`（唯一索引，仅对 `relatedIntakeId != null` 生效）**
- **`deletedAt` 索引策略**：直接使用 `0`（未删除）/ ISO 字符串（已删除），**存储层与索引层统一**，Repository 层零转换。
- **【v7新增】补录检测索引**：`[date+supplementId]` 已支持补录重复检测与次日提醒查询。

> **StockLog 唯一索引恢复严格生效**：因放弃批量聚合，每条流水 `relatedIntakeId` 唯一，唯一索引可正常兜底幂等。
>
> **混合类型索引说明**：`[deletedAt+date]` 中 `deletedAt` 为 `0`（number）或 ISO 字符串（string），IndexedDB 允许混合类型共存于同一索引，排序时 number 排在 string 之前。`between([0, start], [0, end])` 依赖 `0` 排在最前，实现前建议用 `fake-indexeddb` 实测确认。

### 6.3 状态管理边界

- **Dexie**：唯一数据源。
- **Zustand**：仅 UI 状态（当前日期、筛选器、弹窗开关、Toast 队列、**补录弹窗状态**）。
- **数据获取**：使用 `dexie-react-hooks` 的 `useLiveQuery`，禁止组件内直接 `await db.table.toArray()`。
- **多标签页一致性**：
  - `useLiveQuery`：负责**同一标签页内**数据变更的响应式更新。
  - BroadcastChannel：负责**跨标签页**通知，收到广播后触发本地 `dataVersion` 自增，`useLiveQuery` 依赖该计数器重新查询。
  - 避免重复触发：写操作后只广播，不手动 invalidate 本地 query；本地变更由 `useLiveQuery` 自然响应。

### 6.4 PWA 缓存策略

- **静态资源**：CacheFirst。
- **数据**：不缓存（依赖 IndexedDB）。
- **更新策略**：`registerType: 'prompt'`，新版本就绪时 Toast 提示"新版本已就绪，点击刷新"，由用户主动刷新。Schema 迁移仅在用户确认刷新、新版本代码接管后执行。

### 6.5 日期处理

- **存储格式**：统一 `YYYY-MM-DD` 字符串。
- **时区**：以用户本地时区为准。
- **工具库**：date-fns。
- **【v7新增】补录日期计算**：
  - 补录范围起点：`subDays(new Date(), backfillWindowDays)`。
  - 禁止未来：`date <= format(new Date(), 'yyyy-MM-dd')`。
  - 补录标注：`date !== format(new Date(createdAt), 'yyyy-MM-dd')`。

### 6.6 错误处理与校验

- **Schema 校验**：所有写入操作前使用 Zod 校验。
- **ErrorBoundary**：组件树顶层包裹。
- **异步捕获**：Dexie 操作统一 try/catch，失败时 Toast 提示。

**单位相关校验规则**：

1. **`Ingredient.unit` 输入转换**：用户输入 `mcg` → Zod `transform` 转为 `μg`；其他单位直接枚举校验。
2. **所有数量字段一律整数**：
   - `dailyAmount` / `actualAmount` / `plannedAmount` / `stockCountInUsageUnit` / `deltaInUsageUnit` / `recommendedDailyIntake` / `upperLimit` / `amountPerServing` 统一 `z.number().int()`。
3. **`SupplementIngredient.amountPerServing`**：整数。
4. **`StockLog.deltaInUsageUnit`**：整数，正=入库，负=出库。
5. **`stockRollbackState` 与 `deletedAt` 一致性**：
   - `deletedAt === 0` → `stockRollbackState === 'not_rolled_back'`
   - `deletedAt !== 0` → `stockRollbackState ∈ { 'rolled_back', 'unknown' }`
   - Zod refine 强制。
6. **`stockDeducted` 校验**：
   - `stockDeducted = false` 时，`stockRollbackState` 允许 `'not_rolled_back'` / `'rolled_back'`（标志位翻转，不涉及库存）。
   - `stockDeducted = true` 时，无额外约束。
7. **`deletedAt` Zod schema**：
   ```ts
   deletedAt: z.union([z.literal(0), z.string().datetime()])
   ```
   旧版兼容：导入时 `null` 自动转 `0`。
8. **`stockRollbackState` Zod schema**：
   ```ts
   stockRollbackState: z.enum(['rolled_back', 'not_rolled_back', 'unknown'])
   ```
9. **`stockDeducted` Zod schema**：
   ```ts
   stockDeducted: z.boolean()
   ```

**【v7新增】补录相关校验**：

10. **`date` 范围校验**：
    ```ts
    date: z.string().date().refine(d => {
      const today = format(new Date(), 'yyyy-MM-dd');
      const min = format(subDays(new Date(), backfillWindowDays), 'yyyy-MM-dd');
      return d <= today && d >= min;
    }, '日期超出补录范围')
    ```
    - 普通录入（今日打卡）：`date === today`。
    - 补录：`date` 在 `[today - backfillWindowDays, today]` 内。
    - 历史修正：若允许修改更早记录，需放宽此校验（见下方说明）。

11. **补录 `source` 与 `status` 组合校验**：
    ```ts
    .refine(d => {
      if (d.source === 'manual' && d.status === 'skipped') {
        // 仅补录漏服允许
        return true; // 由业务层保证是补录场景
      }
      return true;
    })
    ```

12. **补录重复检测**（Service 层，非 Zod）：
    - 写入前查询 `date + supplementId + timeSlot` 是否已有 `deletedAt === 0` 记录。
    - 若已有，抛出业务异常，UI 提示"追加"或"修改"。

> **历史修正的日期范围说明**：
> - 补录（新建历史记录）：限制在过去 30 天。
> - 历史修正（修改已存在记录）：允许修改任意日期的记录（因为记录本身已存在）。
> - 二者区别在 UI 入口和 Service 层校验，Zod schema 可统一放宽，由 Service 层按场景判断。

---

## 7. 体验设计规范

### 7.1 桌面端体验优化
- **导航**：侧边栏导航（今日、补剂库、成分库、服用计划、停药期、日历、统计、设置）。
- **交互**：打卡按钮醒目，表单使用弹窗或侧边抽屉，日历支持日期选择器。
- **反馈**：打卡成功即时动画；打卡后行内显示"撤销"按钮，不消失；库存不足/过期/负库存使用高亮色块。
- **撤销反馈**：撤销后 Toast 提示"已撤销，库存已回滚 2 粒"。
- **【v7新增】补录反馈**：
  - 补录成功 Toast："已补录 N 条记录"。
  - 补录漏服 Toast："已标记 N 条漏服"。
  - 补录含负库存警告时，Toast 中附加"补剂 X 库存已为负"。

### 7.2 空状态与引导
- **无数据**：插画 + 引导按钮。
- **有计划未执行**：高亮今日任务。
- **停药日**：温和提示"今日是休息日"。
- **【v7新增】有未记录历史项**：次日提醒 Banner，引导补录。
- **【v7新增】补录弹窗空状态**：所选日期无计划时，提示"该日无计划，可手动录入临时服用"。

### 7.3 无障碍
- **标签**：所有表单控件必须有 label，图标按钮需有 aria-label。
- **对比度**：预警色符合 WCAG AA。
- **【v7新增】补录弹窗**：键盘可操作，日期选择器支持键盘输入。

---

## 8. 工程化规范

### 8.1 目录结构

```
src/
├── main.tsx
├── App.tsx
├── db/
│   ├── index.ts
│   ├── schema.ts
│   └── migrations/
├── repositories/             # 数据访问层，统一封装软删除过滤
├── services/                 # 业务逻辑层
│   └── backfillService.ts    # 【v7新增】补录逻辑
├── stores/                   # Zustand UI 状态
├── hooks/
│   └── useMissedPlans.ts     # 【v7新增】次日提醒检测
├── schemas/                  # Zod 校验 Schema
├── constants/
│   ├── units.ts              # UNIT_TYPE、INGREDIENT_UNIT、标签映射、换算常量
│   ├── deletedAt.ts          # NOT_DELETED = 0 常量
│   ├── rollbackState.ts      # STOCK_ROLLBACK_STATE 常量
│   └── ...                   # 其他枚举、配置
├── types/
├── utils/
├── pages/
├── components/
│   ├── ui/
│   ├── layout/
│   └── backfill/             # 【v7新增】补录弹窗组件
├── styles/
└── test/
```

> **`constants/deletedAt.ts`**：
> ```ts
> export const NOT_DELETED = 0 as const
> export type DeletedAt = typeof NOT_DELETED | string
> ```
> 全项目引用 `NOT_DELETED` 而非魔法值 `0`。

> **`constants/rollbackState.ts`**：
> ```ts
> export const STOCK_ROLLBACK_STATE = {
>   ROLLED_BACK: 'rolled_back',
>   NOT_ROLLED_BACK: 'not_rolled_back',
>   UNKNOWN: 'unknown',
> } as const
> export type StockRollbackState = typeof STOCK_ROLLBACK_STATE[keyof typeof STOCK_ROLLBACK_STATE]
> ```
> 全项目引用 `STOCK_ROLLBACK_STATE` 而非魔法字符串。

### 8.2 类型系统
- **强制 TypeScript**：禁止 any，数据模型定义 Interface。
- **枚举管理**：所有状态、类型、时段使用 `constants/` 中的对象或 TS Enum。
- **类型别名**：
  ```ts
  export type DeletedAt = 0 | string
  export type StockRollbackState = 'rolled_back' | 'not_rolled_back' | 'unknown'
  ```

### 8.3 测试策略
- **单元测试**：Vitest 覆盖纯函数（成分汇总、停药计算、日期工具、单位换算、**补录日期范围校验**）。
- **集成测试**：`fake-indexeddb` 测试 Dexie 操作及 Service 层逻辑。
- **关键流程**：
    - 打卡、库存扣减（含负库存）
    - 软删除回滚库存、恢复重扣库存、purge 不触碰库存
    - 幂等：重复软删除、重复恢复、`stockRollbackState` 状态校验
    - 批量操作 = 循环单条，验证每条 intake 独立写 StockLog
    - `stockRollbackState` 与 `deletedAt` 一致性校验
    - `stockDeducted = false` 时库存逻辑跳过
    - `'unknown'` 记录恢复时不重扣
    - 导入导出、软删除/恢复
    - v3 → v4 迁移脚本测试
    - v4 → v5 迁移脚本测试（`null` → `0`）
    - v5 → v6 迁移脚本测试（`stockRolledBack` → `stockRollbackState`，`stockDeducted` 回填）
    - 导入异常清单测试（类型 A/B/C/D）
    - 混合类型索引实测（`[deletedAt+date]`）
    - **【v7新增】补录流程测试**：
      - 补录"已服用"扣库存，`reason = manual_intake`。
      - 补录"漏服"不扣库存，`stockDeducted = false`。
      - 补录"忽略"不创建记录。
      - 补录日期范围校验（30 天内、禁止未来）。
      - 补录重复检测（已有记录时提示）。
      - 补录按 `date` 匹配计划与停药期。
      - 补录记录计入历史当日统计，不影响今日。
    - **【v7新增】次日提醒检测测试**：
      - 过去 7 天未记录项正确识别。
      - 停药日排除。
      - 已软删除记录视为"未记录"。

---

## 9. 非功能需求

### 9.1 性能目标
- **首屏加载**：< 1.5s (Lighthouse)。
- **日历渲染**：1000 条记录下 < 200ms。
- **打卡响应**：点击到 UI 反馈 < 100ms。
- **【v7新增】次日提醒检测**：过去 7 天扫描 < 300ms（依赖 `[date+supplementId]` 索引）。

### 9.2 数据容量
- **单表上限**：支持 10000 条记录以上查询响应 < 500ms（依赖 6.2 的 `deletedAt` 索引策略）。
- **导出文件大小**：gzip 压缩后 < 5MB。
- **StockLog 容量**：放弃聚合后流水量增加，10 年约 3.6 万条，IndexedDB 无压力。

### 9.3 浏览器兼容
- **目标浏览器**：Chrome 90+、Firefox 88+、Safari 14+、Edge 90+。
- **IndexedDB**：所有目标浏览器均支持 2.0+。
- **PWA**：Chrome/Edge 完整支持，Safari 部分支持（降级处理）。

### 9.4 边界与异常处理
- **断网**：PWA 缓存页面资源，IndexedDB 读写不受影响。
- **存储配额满**：监听 storage event，超出配额提示用户清理或导出。
- **导入损坏文件**：JSON 解析失败时 Toast 提示，不崩溃。
- **系统时间被修改**：所有日期以本地时区为准。
- **多标签页数据一致**：通过 BroadcastChannel 实现。任何写操作后广播 `{ table, action, id | ids[] }`，Zustand 中维护一个 `dataVersion` 计数器，收到广播后自增，`useLiveQuery` 依赖该计数器触发重新查询（详见 6.3）。
- **【v7新增】补录时系统时间被修改**：
  - 补录范围基于当前系统时间计算，若系统时间被篡改，可能允许/禁止异常日期。
  - 不额外防护，提示用户"请确保系统时间正确"。

---

## 10. 数据字典

### 10.1a 服用单位（unitType）
- capsule（胶囊）
- tablet（片）
- pill（粒）
- granule（颗粒）
- bag（袋）
- stick（支）
- ml（毫升）
- g（克）
- scoop（勺）
- drop（滴）

### 10.1b 成分单位（Ingredient.unit）
- mg
- μg（**存储统一，不存 mcg**；用户输入 mcg 自动转换）
- g
- IU（不参与 mg/μg 换算）
- ml（液体成分，**不参与 μg 归一化**）

### 10.2 服用时段（timeSlot）
morning（早）、noon（中）、evening（晚）、bedtime（睡前）

### 10.3 摄入记录状态（status）
taken（已服用）、skipped（漏服）、extra（多服）、partial（部分服用）

### 10.4 记录来源（source）
plan（计划内）、manual（手动录入）

### 10.5 停药模式（cycleMode）
none、oneTime、cyclic

### 10.6 补剂状态（status）
- active：使用中，出现在今日计划。
- inactive：暂时停用，不出现在今日，保留在补剂库。
- finished：已用完，不出现在今日，库存为 0，保留历史。
- （软删除）：进回收站，不在补剂库显示。

### 10.7 评分范围
精力/睡眠/肠胃/情绪：1（很差）至 5（很好）

### 10.8 库存流水原因（StockLog.reason）
- intake：打卡扣减、恢复（restore）重扣
- manual_intake：手动录入扣减（**【v7明确】补录已服用记录使用此 reason**）
- undo_intake：软删除（撤销）时回滚
- restock：补货入库
- adjust：手动调整
- purge_rollback：**保留枚举以兼容历史数据，v4 起不再产生新记录**

### 10.9 DailyIntake 库存状态字段

**stockDeducted**：
- `true`：创建时补剂 `stockCountInUsageUnit != null`，该记录曾真实扣减库存。
- `false`：创建时补剂不记录库存，未发生扣减。
- 不可变，创建后不修改。

**stockRollbackState**：
- `'not_rolled_back'`：库存已扣减，未回滚（记录可见或已恢复）。
- `'rolled_back'`：库存已回滚，未扣减（记录已软删除）。
- `'unknown'`：v3 及更早版本迁移遗留，库存状态未知，恢复时不重扣。

**与 deletedAt 的一致性**：
- `deletedAt === 0` ⇔ `stockRollbackState === 'not_rolled_back'`
- `deletedAt !== 0` ⇔ `stockRollbackState ∈ { 'rolled_back', 'unknown' }`

### 10.10 deletedAt
- `0`：未删除。
- ISO 字符串：已删除时间。
- **存储层、索引层、导出层统一使用，Repository 层零转换。**
- 全项目引用 `constants/deletedAt.ts` 中的 `NOT_DELETED = 0`，禁止散落魔法值。

### 10.11 【v7新增】补录相关
- **补录（backfill）**：用户为历史日期手动创建 DailyIntake 记录。
- **补录范围（backfillWindowDays）**：默认 30 天，可配置，存于 Meta。
- **次日提醒（missed plan detection）**：应用启动时检测过去 7 天计划内未记录项。
- **补录选项**：
  - 已服用：创建 `taken` / `partial`，扣库存。
  - 漏服：创建 `skipped`，不扣库存。
  - 忽略：不创建记录。
- **补录标注**：`date !== format(createdAt, 'yyyy-MM-dd')` 时，UI 标注"补录"。

---

## 11. 隐私说明

- 本应用为纯前端应用，所有数据仅存储在用户浏览器本地（IndexedDB），不上传至任何服务器。
- 数据导出文件（JSON）包含用户的健康补剂摄入记录，属于敏感个人信息，用户应妥善保管。
- 应用不提供数据云端同步功能，用户需自行定期导出备份。
- 应用不使用任何第三方分析或追踪服务。

---

## 12. 优先级规划

### P0 - 核心功能（MVP）
- 补剂库 CRUD
- 成分库 CRUD
- 补剂-成分关联（含配方版本管理）
- 每日摄入记录（含 status、planId、plannedAmount 快照）
- 服用计划设定
- 基础校验（Zod schema）
- 软删除基础设施：所有表加 `deletedAt`、Repository 统一过滤、`softDelete`/`restore` 方法、Zod schema 强制 `deletedAt: string | 0`
- `constants/deletedAt.ts` 定义 `NOT_DELETED = 0`
- `constants/rollbackState.ts` 定义 `STOCK_ROLLBACK_STATE`
- 单位常量定义（constants/units.ts）：UNIT_TYPE、INGREDIENT_UNIT、标签映射、换算常量
- 所有数量字段整数校验
- **【v7新增】手动录入支持历史日期选择（补录基础能力）**

### P1 - 重要功能
- 库存管理与提醒（整数版扣减一致性，**允许负库存 + 警告**）
- 软删除 = 撤销：软删除回滚库存、恢复重扣库存、purge 不触碰库存
- `stockDeducted` + `stockRollbackState` 幂等标记与 Zod 一致性校验
- StockLog 增加 `[reason+relatedIntakeId]` 唯一索引
- StockLog 库存流水（**每条 intake 一条，不聚合**）
- 过期提醒
- 每日成分摄入汇总（含单位统一，ml 独立累加）
- 停药期管理（含周期性计算，并集语义）
- 日历视图（含完成度计算）
- 数据导出/导入（含强制备份 + 合并策略 + 结果摘要 + **异常清单**）
- 回收站（分表 Tab）
- v3 → v4 迁移脚本
- v4 → v5 迁移脚本（`null` → `0`）
- v5 → v6 迁移脚本（`stockRolledBack` → `stockRollbackState`，`stockDeducted` 回填）
- 旧版导入兼容（`stockDeducted` 推断）
- **【v7新增】补录弹窗（含"已服用/漏服/忽略"三选项）**
- **【v7新增】次日提醒检测与 Banner**
- **【v7新增】补录重复检测**
- **【v7新增】补录范围配置（Meta.backfillWindowDays）**
- **【v7新增】补录展示（"补录"标注）**

### P2 - 增强功能
- 身体反馈记录
- 统计报表
- 成分冲突/超标提醒
- 快速录入（常用组合）
- 库存批次管理（StockBatch）

---

## 13. 待决策项

- [x] UI 组件库选型 — shadcn/ui
- [x] 状态管理方案 — Zustand（不加 TanStack Query）
- [x] 路由方案 — React Router
- [x] IndexedDB 封装库选型 — Dexie.js
- [x] 是否需要 PWA 离线支持 — 需要
- [x] 是否需要暗色模式 — 不需要
- [x] 是否需要国际化（i18n） — 不需要
- [x] 类型系统 — TypeScript
- [x] 数据校验 — Zod
- [x] 日期处理库 — date-fns
- [x] 目标平台 — PC 网页优先
- [x] 库存存储方式 — 整数（按服用单位），**允许负库存**
- [x] 软删除 DailyIntake 语义 — 方案A：软删除 = 撤销，立即回滚库存；恢复重扣；purge 不触碰库存
- [x] 幂等保障 — `stockDeducted` + `stockRollbackState` 标记 + StockLog `[reason+relatedIntakeId]` 唯一索引
- [x] 批量操作 — 放弃聚合，每条 intake 一条 StockLog，批量 = 循环单条
- [x] 回收站展示 — 分表 Tab
- [x] 自动本地快照 — 不做
- [x] `deletedAt` 未删除值 — 统一用 `0`（存储层、索引层、导出层一致，Repository 零转换）
- [x] `constants/deletedAt.ts` — 定义 `NOT_DELETED = 0`，全项目引用
- [x] μg / mcg 统一 — 存储统一 `μg`，输入接受 `mcg` 自动转换
- [x] 「颗粒」独立枚举 — 与「粒」「袋」并列，不可混用
- [x] unitType 清单 — capsule/tablet/pill/granule/bag/stick/ml/g/scoop/drop
- [x] Ingredient.unit 清单 — mg/μg/g/IU/ml
- [x] 汇总统一到 μg — IU / ml 独立累加，显示时按数值自动选单位
- [x] 所有数量字段一律整数，不接受小数（含 ml/g）
- [x] 允许负库存 + 警告，不硬拒绝
- [x] isPaused 为并集语义，补剂级与全局任一覆盖即停
- [x] 去掉导入"跳过已删除记录"选项，统一 mergeRecord
- [x] PWA registerType 改为 'prompt'
- [x] mergeRecord 时间相同时优先取 `deletedAt !== 0` 的一方
- [x] 导入 DailyIntake 时状态一致性处理
- [x] v4 → v5 迁移脚本设计（`null` → `0`）
- [x] 旧版导入兼容（`deletedAt: null` 自动转 `0`）
- [x] 库存扣减判断依据 — 新增 `stockDeducted` 字段
- [x] 批量操作与 StockLog — 放弃聚合，每条 intake 一条流水
- [x] 导入状态异常 — 不静默修正，列入异常清单
- [x] v3 → v4 迁移 — 引入 `stockRollbackState` 三态，`'unknown'` 表达历史遗留
- [x] `stockRolledBack` 字段 — 被 `stockRollbackState` 取代
- [x] `[reason+relatedIntakeId]` 唯一索引 — 恢复严格生效
- [x] purge 后 StockLog 悬空引用 — 有意保留，UI 标注"[记录已彻底删除]"
- [x] BodyFeedback 补充 `createdAt` / `updatedAt`
- [x] 多标签页一致性 — 明确 useLiveQuery 与 BroadcastChannel 分工
- [x] 成分汇总单位选择 — 按合计后数值判定
- [x] Meta.appVersion 写入时机 — 启动时注入
- [x] **【v7新增】补录能力 — 支持（方案 A + B）**
- [x] **【v7新增】补录范围 — 过去 30 天（可配置）**
- [x] **【v7新增】次日提醒 — 支持（过去 7 天检测）**
- [x] **【v7新增】补录弹窗选项 — 已服用 / 漏服 / 忽略**
- [x] **【v7新增】漏服是否扣库存 — 否**
- [x] **【v7新增】是否加 `takenAt` 字段 — 否**
- [x] **【v7新增】补录库存基准 — 当前库存，不回溯**
- [x] **【v7新增】补录计划/停药匹配 — 按 `date` 查询**
- [x] **【v7新增】补录统计归属 — 按 `date` 计入历史**
- [x] **【v7新增】补录展示 — `date !== createdAt` 日期时标注"补录"**
- [x] **【v7新增】v6 → v7 迁移 — 无 Schema 变更，仅 Meta 新增 `backfillWindowDays`**
- [x] **【v7新增】`source=manual + status=skipped` 合法组合 — 允许（仅补录场景）**

---

## 附录：v7 修订要点汇总

1. **新增补录能力**：支持过去 30 天内历史日期手动录入，解决"晚上服用时不在电脑前"场景。
2. **新增次日提醒**：应用启动时检测过去 7 天"计划内未记录"项，首页 Banner 提示补录。
3. **补录弹窗三选项**：已服用 / 漏服 / 忽略。漏服写 `status = skipped`，不扣库存。
4. **补录范围可配置**：`Meta.backfillWindowDays`，默认 30 天。
5. **补录重复检测**：检查 `date + supplementId + timeSlot` 是否已有记录，提供"追加"或"修改"。
6. **补录展示**：`date !== format(createdAt, 'yyyy-MM-dd')` 时标注"补录"。
7. **补录库存基准明确**：始终基于当前库存，不回溯历史。
8. **补录计划/停药匹配明确**：按 `date` 查询，不是按录入时刻。
9. **补录统计归属明确**：按 `date` 计入历史当日统计与完成度，不影响今日。
10. **合法组合矩阵扩展**：`source = manual + status = skipped` 为补录漏服新开合法组合。
11. **无 Schema 变更**：v7 复用现有字段，仅 Meta 新增 `backfillWindowDays`。
12. **补录 StockLog reason**：补录"已服用"使用 `manual_intake`。
13. **次日提醒检测范围**：过去 7 天，排除停药日，已软删除记录视为"未记录"。
14. **测试策略补充**：补录流程、次日提醒检测、日期范围校验。

### 版本演进对照

| 版本 | 核心变更 |
|------|----------|
| v2 | 初版框架 |
| v3 | 整数化、负库存、软删除语义初步明确 |
| v4 | 软删除 = 撤销（方案A）、`stockRolledBack` 幂等、批量聚合 |
| v5 | `deletedAt` 统一用 `0`，废除 `null` 双轨制，Repository 零转换 |
| v6 | `stockDeducted` + `stockRollbackState` 三态、放弃批量聚合、导入异常清单 |
| v7 | 补录能力、次日提醒、漏服标记、补录范围可配置 |

---

以上为完整 v7 全文。若还需我再补充 **v7 的补录 Service 层函数签名**、**次日提醒 Hook 完整实现** 或 **补录弹窗组件结构**，可以继续。
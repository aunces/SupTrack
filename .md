# 补剂摄入记录系统 - 需求框架（修订版 v10）

> 本版在 v9 基础上修订以下决策（对应 v9 审查中的 3 个结构性问题 + 6 个中等问题 + 6 个文档一致性问题）：
> - **【v10修订·P-A】修正 `DailyIntakeCreateSchema` 与"今日打卡"的冲突**：Create schema **不含任何补录窗口语义的 refine**，日期范围校验全部下放到 Service 层 `validateIntakeDate(date, mode)`，按 `mode = 'today' | 'backfill' | 'update'` 分支。
> - **【v10修订·P-B】废除 `stockState` 三态，改为四态 `StockState`**：`'deducted' | 'not_deducted' | 'was_deducted' | 'unknown'`。软删除时 `deducted → was_deducted`，恢复时 `was_deducted → deducted` 并重扣。语义从"当前库存事实"改为"**当前库存事实 + 恢复意图**"，消除软删除态下 `deducted` 字面矛盾。
> - **【v10修订·P-C】`unknown` 记录恢复时提供二选一对话框**：用户明确选择"视为未扣（不重扣）"或"视为已扣（重扣 actualAmount）"，不再单方面置 `not_deducted`。
> - **【v10修订·M-1】`mergeRecord` 时间相同时默认取 local**；"删除意图优先"仅适用于非 DailyIntake 表（无库存副作用）。
> - **【v10修订·M-2】补录不写 `plannedAmount` 快照，改写入 `plannedAmountSnapshot`**：新增字段 `plannedAmountSource: 'current_plan' | 'unavailable'`，让统计层知道该值可信度。
> - **【v10修订·M-3】`detectMissedPlans` 排除今天；软删除/恢复后主动 invalidate `useMissedPlans` 缓存**，不等 5 分钟。
> - **【v10修订·M-4】批量补录 Toast 区分跳过原因（已有记录 / 临期），提供"查看并处理跳过项"入口**，支持逐条追加/修改/忽略。
> - **【v10修订·M-5】导入时 `purge_rollback` 原样保留，UI 显示"历史版本回滚（已废弃）"**；数据字典注明。
> - **【v10修订·M-6】`skipped + stockState='deducted'` 校验只在最终写入 DailyIntake 时执行**；中间态全部放在内存变量，事务内只做一次最终写入。
> - **【v10修订·L-1】3.1 补充"（Zod 层允许，Service 层按场景拒绝）"**。
> - **【v10修订·L-2】2.5 与 5.6 的 `stockState` 语义表全部对齐为四态**。
> - **【v10修订·L-3】v8→v9 迁移回填规则与 P-B 的四态一致**。
> - **【v10修订·L-4】次日提醒批量查询优化从"建议"改为"必须实现"**。
> - **【v10修订·L-5】`stockService.applyTransition` 作为库存状态机唯一入口**，`intakeService` 调用之，状态机逻辑不再散落。
> - **【v10修订·L-6】13 待决策项新增 v10 的 3 个决策项**。
> 变更处用 `【v10修订】` / `【v10新增】` 标注。

---

## 1. 项目概述

### 1.1 项目目标
构建一个纯前端补剂摄入记录工具，帮助用户追踪每日补剂服用情况、管理补剂库存、分析成分摄入。系统需具备数据安全性、离线可用性。

### 1.2 技术选型
- **构建工具**：Vite
- **前端框架**：React + TypeScript
- **数据存储**：IndexedDB（本地持久化），封装库选用 Dexie.js
- **UI 方案**：shadcn/ui
- **状态管理**：Zustand（仅管理 UI 状态与派生缓存，Dexie 为数据唯一源）
- **路由方案**：React Router
- **PWA 支持**：需要（vite-plugin-pwa，配置 `registerType: 'prompt'`）
- **日期处理**：date-fns（统一使用 YYYY-MM-DD 字符串存储）
- **数据校验**：Zod
- **暗色模式**：不需要
- **国际化**：不需要
- **TanStack Query**：不加
- **目标平台**：PC 网页优先

> **PWA 更新策略**：使用 `registerType: 'prompt'`，新版本就绪时 Toast 提示"新版本已就绪，点击刷新"，由用户主动刷新，避免打卡时被静默打断。Schema 迁移仅在用户确认刷新、新版本代码接管后执行。

### 1.3 核心约束
- **纯前端应用**：无后端服务，所有逻辑在浏览器端运行。
- **本地存储**：所有数据存储在浏览器 IndexedDB 中。
- **数据安全**：数据版本管理与迁移机制；导入前强制备份；全量导出/导入；所有业务表支持软删除（deletedAt）及回收站恢复。
- **离线优先**：PWA 缓存静态资源，数据天然离线可用。
- **隐私合规**：数据仅存本地，不上传任何服务器，需在设置页明确告知用户。

---

## 2. 数据模型

> **通用约定**：
> - **所有业务表均含 `deletedAt: string | 0`**：`0` = 未删除，ISO 时间字符串 = 已删除。**新建记录必须显式写 `0`**，不允许 `undefined`、`null` 或省略。
> - **使用 `0` 而非 `null` 的原因**：IndexedDB 不索引 `null`，若用 `null` 则 `[deletedAt+date]` 等复合索引无法命中未删除记录。`0` 作为哨兵值可正常进索引，且存储层、索引层、导出层统一，Repository 层零转换。
> - Meta 表不加 `deletedAt`（key-value 配置）。
> - 所有日期字段统一 `YYYY-MM-DD` 字符串；所有时间戳统一 ISO 字符串。
> - **【整数修订】所有数量字段（dailyAmount、actualAmount、plannedAmount、stockCountInUsageUnit、amountPerServing 对应的服用维度、deltaInUsageUnit 等）一律为整数（`z.number().int()`）。系统不接受小数服用量。**
>   - 若现实中需要"半片"，请改用更小服用单位（如把 `tablet` 改为 `half_tablet` 不推荐；推荐直接以 `mg` 记成分，或用户自行按整数记录）。
>   - `ml` / `g` 服用单位同样按整数录入（如 5ml、10g），不接受 5.5ml。
>   - 成分含量 `amountPerServing` 若成分单位为 `g` 且实际为 0.5g，请换算为 500mg 并调整成分单位，或直接以 `mg` 为单位录入。
> - **【v10修订】库存状态统一为单一字段 `stockState`，四态互斥**：`'deducted' | 'not_deducted' | 'was_deducted' | 'unknown'`。语义为"**当前库存事实 + 恢复意图**"。详见 2.5 与 5.6。

### 2.1 补剂表（Supplement）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 (UUID) |
| name | string | 补剂名称 |
| brand | string? | 品牌（可选） |
| description | string? | 描述/备注（可选） |
| unitType | enum | 服用单位（capsule/tablet/pill/granule/bag/stick/ml/g/scoop/drop） |
| stockCountInUsageUnit | number? | 当前库存数量（按服用单位计，整数，可为负数），可选，支持不记录 |
| stockUnit | string? | 库存展示单位（瓶/盒/袋等），仅用于 UI 换算显示 |
| unitsPerStock | number? | 换算率：1 个 stockUnit 等于多少服用单位（整数）。例：1瓶=60粒 → 60 |
| productionDate | string? | 生产日期 (YYYY-MM-DD) |
| expiryDate | string? | 过期日期 (YYYY-MM-DD) |
| status | enum | active/inactive/finished |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除，ISO 字符串 = 已删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **库存整数化说明**：
> - 库存以**服用单位**存储为整数，**允许为负**（表示"已超支/未补录入库"）。
> - `stockUnit` 和 `unitsPerStock` **仅用于 UI 展示换算**，不参与存储计算。
> - 示例：库存 98 粒，`stockUnit = "瓶"`，`unitsPerStock = 60` → UI 显示"约 1.63 瓶（98 粒）"。
> - 若用户不关心瓶数，`stockUnit` 可留空，UI 只显示"98 粒"。
> - 扣减逻辑：直接 `stockCountInUsageUnit - actualAmount`，无换算，永远整数。
> - **负库存**：当 `stockCountInUsageUnit < 0` 时，UI 以警告色显示，提示"库存为负，请补货或调整"。

### 2.2 成分表（Ingredient）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 成分名称 |
| unit | enum | 成分标准单位（mg/μg/g/IU/ml）。**存储层统一用 `μg`，不接受 `mcg`**；用户输入 `mcg` 时由 Zod 自动转换为 `μg`。IU 不参与 mg/μg 换算。 |
| recommendedDailyIntake | number? | 推荐每日摄入量（可选，整数） |
| upperLimit | number? | 每日上限（可选，整数） |
| description | string? | 描述/备注（可选） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |

> **成分含量说明**：
> - `recommendedDailyIntake` / `upperLimit` / `amountPerServing` 均为整数。
> - 若某成分标准单位为 `g` 但实际推荐 0.5g，请改用 `mg`（500mg）存储。
> - `μg` 为最小单位，若实际小于 1μg，请改用其他表达或合并处理（超出本系统精度范围）。

### 2.3 补剂-成分关联表（SupplementIngredient）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| ingredientId | string | 关联成分 ID |
| amountPerServing | number | 每份含该成分的量（整数） |
| effectiveFrom | string | 生效日期 (YYYY-MM-DD) |
| effectiveTo | string? | 失效日期 (YYYY-MM-DD)，为空表示当前有效 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **配方变更**：不修改原记录，将原记录 `effectiveTo` 设为昨日，新建一条 `effectiveFrom` 为今日的记录。

### 2.4 服用计划表（DosagePlan）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| dailyAmount | number | 每日计划服用量（整数） |
| timeSlots | string[] | 服用时段（morning/noon/evening/bedtime） |
| withMeal | boolean | 是否随餐服用 |
| isActive | boolean | 是否启用 |
| notes | string? | 备注（可选） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **【v8修订】计划无版本历史**：`DosagePlan` 不保留历史版本，`dailyAmount` 修改后旧值丢失。这影响补录的 `plannedAmount` 快照（详见 5.8）。计划版本管理列为 P2 增强功能（`DosagePlanHistory`），不在 v10 范围内。

### 2.5 每日摄入记录表（DailyIntake）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | **服用归属日期** (YYYY-MM-DD)。补录时为用户所选历史日期，不是录入时刻。 |
| supplementId | string | 关联补剂 ID |
| planId | string? | 关联 DosagePlan ID（为空表示临时录入） |
| plannedAmount | number? | 计划量快照（整数）。按 `date` 查询当时有效计划快照。**【v10修订】补录时此字段不再写入计划量，改由 `plannedAmountSnapshot` 承载。** |
| **plannedAmountSnapshot** | **number?** | **【v10新增】补录时的计划量参考值**。取自**当前**计划的 `dailyAmount`，**不代表 `date` 当天的真实计划量**。配合 `plannedAmountSource` 使用。 |
| **plannedAmountSource** | **enum** | **【v10新增】`'plan_snapshot'`（打卡/手动录入，计划量可信） / `'current_plan'`（补录，计划量为当前值，不可信） / `'unavailable'`（无计划或无法确定）。** |
| actualAmount | number | 实际服用量（整数） |
| timeSlot | string | 服用时段 |
| status | enum | taken / skipped / extra / partial |
| source | enum | plan / manual |
| notes | string? | 备注（可选） |
| **stockState** | **enum** | **【v10修订】库存状态机，四态**。`'deducted'` / `'not_deducted'` / `'was_deducted'` / `'unknown'`。语义为"当前库存事实 + 恢复意图"。详见 5.6。 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除，ISO 字符串 = 已删除。 |
| createdAt | string | **录入时间** (ISO)。补录时仍为真实写入时间，与 `date` 可不同。 |
| updatedAt | string | 更新时间 (ISO) |

> **【v10修订】`stockState` 语义（四态，单一状态机）**：
>
> | 状态 | 含义 | 当前库存是否已扣 | 恢复时是否重扣 | 记录是否可见 |
> |------|------|------------------|----------------|--------------|
> | `'deducted'` | 已扣库存，未软删除 | 是 | —（未删除，无需恢复） | 可见（deletedAt === 0） |
> | `'not_deducted'` | 未扣库存（无库存记录，或 status=skipped），未软删除 | 否 | —（未删除，无需恢复） | 可见（deletedAt === 0） |
> | `'was_deducted'` | **曾经扣过库存，软删除时已回滚；恢复时需重扣** | 否（已回滚） | 是 | 不可见（deletedAt !== 0） |
> | `'unknown'` | v3 及更早版本迁移遗留，库存状态未知 | 未知 | 用户二选一（详见 5.6） | 不可见（deletedAt !== 0） |
>
> **四态互斥，无组合爆炸**。`'was_deducted'` 取代 v9 方案 A 中"软删除态仍保留 `deducted`"的歧义写法——`'deducted'` 现在**严格表示当前库存已扣**，软删除后必然回滚，因此状态必然变为 `'was_deducted'`。
>
> **一致性约束（Zod refine 强制）**：
> - `deletedAt === 0` → `stockState ∈ { 'deducted', 'not_deducted' }`
> - `deletedAt !== 0` → `stockState ∈ { 'was_deducted', 'unknown' }`
> - **注意**：`deletedAt !== 0 && stockState === 'deducted'` 和 `deletedAt === 0 && stockState === 'was_deducted'` 均为非法。
>
> **`date` 与 `createdAt` 的关系**：
> - `date` = 服用归属日期，决定统计、汇总、完成度、计划/停药期匹配。
> - `createdAt` = 真实录入时间，用于审计与"补录"标注。
> - 两者不同时（`date < createdAt 的日期`），UI 标注"补录"。
>
> **软删除语义（方案A：软删除 = 撤销）**：
> - `deletedAt !== 0` 的 DailyIntake **不计入任何汇总、统计、完成度**。
> - **软删除时立即回滚库存**（仅当 `stockState === 'deducted'`），并将 `stockState` 置为 `'was_deducted'`。
> - **恢复（restore）时重新扣减库存**（仅当 `stockState === 'was_deducted'`），并将 `stockState` 置回 `'deducted'`。
> - **`'unknown'` 记录**：恢复时弹出对话框让用户选择"视为未扣"或"视为已扣"，详见 5.6。
> - **彻底删除（purge）仅清理回收站，不触碰库存**。
> - **幂等保障**：任何回滚/重扣操作前先检查 `stockState`，状态已匹配则跳过。
>
> **【v9修订】软删除记录禁止直接历史修正**：若 `deletedAt !== 0`，历史修正 API 抛出业务异常，提示"请先恢复该记录再修改"。
>
> **补录记录的特殊约定**：
> - 补录记录 `source = manual`。
> - `status` 按用户选择：已服用 → `taken`（用户主动选部分服用 → `partial`；无计划 → `extra`）；漏服 → `skipped`。
> - **漏服（`skipped`）不扣库存**，`stockState = 'not_deducted'`。
> - 补录若匹配到当日计划，`planId` 按该计划；`plannedAmountSnapshot` 取**当前**计划的 `dailyAmount`，`plannedAmountSource = 'current_plan'`；无计划则 `planId = null`、`plannedAmountSnapshot = null`、`plannedAmountSource = 'unavailable'`。
> - 补录日期在停药期内时，UI 标注"该日为停药日"，但**允许录入**。
> - 补录不计入"今日"统计，按 `date` 计入历史当日统计。

### 2.6 停药期表（PausePeriod）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string? | 关联补剂 ID（为空表示全局停药） |
| startDate | string | 开始日期 (YYYY-MM-DD) |
| endDate | string? | 结束日期 (YYYY-MM-DD)，为空表示持续中 |
| reason | string? | 停药原因（可选） |
| cycleMode | enum | none / oneTime / cyclic |
| cycleStartDate | string? | 周期锚点日期（cycleMode=cyclic 时必填） |
| cycleOnDays | number? | 周期服用天数（整数） |
| cycleOffDays | number? | 周期停药天数（整数） |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

> **周期参数修改语义**：修改 `cycleOnDays`/`cycleOffDays`/`cycleStartDate` **只影响未来计算**，已产生的 DailyIntake 记录不受影响。UI 上修改时提示"此修改仅影响未来的停药计算"。
>
> **startDate 与 cycleStartDate 语义**：
> - `cycleMode = none/oneTime`：仅 `startDate`/`endDate` 生效，`cycleStartDate` 必须为空。
> - `cycleMode = cyclic`：`startDate` 表示停药期**整体生效起点**（date < startDate 不参与计算）；`cycleStartDate` 为周期锚点（取模基准）。通常两者相同，允许不同。
> - cyclic 模式下 `endDate` 若存在，则 `date > endDate` 不再停药；`endDate` 为空表示周期持续有效。
> - 计算顺序：先判断 `startDate <= date <= endDate`（endDate 为空则只判 startDate），再按周期取模。

### 2.7 身体反馈表（BodyFeedback）（P2 可选）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| date | string | 日期 (YYYY-MM-DD) |
| energy | number | 精力评分（1-5） |
| sleep | number | 睡眠评分（1-5） |
| digestion | number | 肠胃评分（1-5） |
| mood | number | 情绪评分（1-5） |
| notes | string? | 自由文本备注 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

### 2.8 元数据表（Meta）

| 字段 | 类型 | 说明 |
|------|------|------|
| key | string | 键名 |
| value | any | 值 |

> **预置 Key**：schemaVersion、lastExportAt、appVersion、backfillWindowDays（补录范围，默认 30）。
> **导入时不合并 Meta**：导入后保留本地 Meta，仅 `schemaVersion` 按迁移流程更新；`lastExportAt` 由本地导出行为更新。
> **`backfillWindowDays` 导入策略**：归入"Meta 不合并"，导入后保留**本地值**，忽略导入文件中的值。与 `schemaVersion` / `lastExportAt` / `appVersion` 一致，均为本地偏好。
> **【v9修订】`backfillWindowDays` 运行时读取**：Zod refine 与 Service 层校验均通过 `metaService.get('backfillWindowDays')` **运行时动态读取**，禁止模块级闭包捕获。用户在设置页修改后立即生效。
> **`appVersion` 写入时机**：应用启动时由代码读取构建时注入的 `import.meta.env.VITE_APP_VERSION` 写入 Meta，若已有则更新为当前版本。
> **次日提醒检测范围固定 7 天**，不放入 Meta（避免配置项膨胀）。如需配置，列为 P2。

### 2.9 库存批次表（StockBatch）（P2 可选）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| quantity | number | 数量（整数，按 unit 计） |
| unit | enum | usage（服用单位）/ stock（库存单位） |
| conversionRate | number? | 换算率（整数，仅当 unit='stock' 时有效） |
| expiryDate | string? | 过期日期 |
| productionDate | string? | 生产日期 |
| isDepleted | boolean | 是否已用完 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |
| updatedAt | string | 更新时间 (ISO) |

### 2.10 库存流水表（StockLog）（P1）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| supplementId | string | 关联补剂 ID |
| deltaInUsageUnit | number | 按服用单位计的变动量（整数，正=入库，负=出库） |
| reason | enum | intake / manual_intake / undo_intake / restock / adjust / purge_rollback（保留兼容） |
| relatedIntakeId | string? | 关联的 DailyIntake ID。单条操作必填；不再产生聚合流水。 |
| note | string? | 手动调整时填原因 |
| **deletedAt** | **string \| 0** | 软删除标记。`0` = 未删除。 |
| createdAt | string | 创建时间 (ISO) |

> **说明**：
> - 库存流水以**服用单位**为准，与 `stockCountInUsageUnit` 一致，**全整数**。
> - **`reason` 语义与使用场景（唯一权威表）**：
>
> | reason | 使用场景 | 库存方向 | 参与唯一索引 | 参与幂等检查 |
> |--------|----------|----------|--------------|--------------|
> | `intake` | 打卡扣减、**恢复（restore）重扣** | 负（出库） | 否 | 靠 `stockState` 应用层 |
> | `manual_intake` | 手动录入扣减、**补录"已服用"扣减** | 负（出库） | 否 | 靠 `stockState` 应用层 |
> | `undo_intake` | 软删除（撤销）回滚 | 正（入库） | 否 | 靠 `stockState` 应用层 |
> | `restock` | 补货入库 | 正（入库） | 否 | 否 |
> | `adjust` | 手动调整、**历史修正改状态（补扣/回滚）**、**历史修正改量（delta）**、**`unknown` 恢复时用户选"视为已扣"** | 正或负 | 否 | 靠 `stockState` 应用层 |
> | `purge_rollback` | **保留枚举以兼容历史数据，v4 起不再产生新记录**。导入时原样保留，UI 显示"历史版本回滚（已废弃）" | — | — | — |
>
> - **历史修正改状态统一用 `adjust`**：正文、代码示例、本节全部对齐。`undo_intake` / `intake` 仅用于软删除/恢复；`manual_intake` 仅用于首次扣减。
> - 流水表默认按时间倒序展示，UI 显示"服用了 2 粒"而非"0.033 瓶"。
> - **放弃批量聚合**：
>   - 每条 DailyIntake 的每次扣减 / 回滚 / 重扣都写**一条独立 StockLog**。
>   - `relatedIntakeId` 始终为该 intake id，单条唯一。
>   - **不再产生"聚合 StockLog"**。
>   - 批量操作 = 循环调用单条操作（可置于同一 Dexie 事务）。
> - **幂等完全由应用层保证**：
>   - `[reason+relatedIntakeId]` 为**普通索引**（非唯一）。
>   - 写入 StockLog 前，先检查对应 DailyIntake 的 `stockState`，状态已匹配则跳过。
>   - **所有库存操作必须在单个 Dexie `readwrite` 事务内完成读-判断-写**，杜绝跨标签页竞态。
>   - DB 层唯一索引列为 P2（`idempotencyKey` 方案），v10 不实现。
> - **补录的 StockLog 时点**：补录时库存扣减发生在**录入时刻**，`StockLog.createdAt` 为真实写入时间，与 `DailyIntake.date` 无关。
> - **purge 后悬空引用**：purge 物理删除 DailyIntake 后，其 StockLog 保留，`relatedIntakeId` 指向不存在的记录。这是**有意设计**，用于审计追溯。UI 在流水详情中若查不到对应 intake，显示"[记录已彻底删除]"。

### 2.11 单位常量定义（constants/units）

本节为全项目单位体系的**唯一来源**，代码中必须从此处引用，禁止散落魔法字符串。

#### 2.11.1 服用单位（UNIT_TYPE）

```ts
export const UNIT_TYPE = {
  CAPSULE: 'capsule',      // 胶囊
  TABLET: 'tablet',        // 片
  PILL: 'pill',            // 粒 / 丸
  GRANULE: 'granule',      // 颗粒
  BAG: 'bag',              // 袋
  STICK: 'stick',          // 支
  ML: 'ml',                // 毫升
  G: 'g',                  // 克
  SCOOP: 'scoop',          // 勺
  DROP: 'drop',            // 滴
} as const
```

中文显示映射：

```ts
export const UNIT_TYPE_LABEL = {
  capsule: '胶囊',
  tablet: '片',
  pill: '粒',
  granule: '颗粒',
  bag: '袋',
  stick: '支',
  ml: 'ml',
  g: 'g',
  scoop: '勺',
  drop: '滴',
}
```

**规则**：
- 「粒」和「颗粒」为两个独立枚举项，不可混用。
- 所有服用单位一律整数录入，包括 `ml` / `g` / `scoop` / `drop`。

#### 2.11.2 成分单位（INGREDIENT_UNIT）

```ts
export const INGREDIENT_UNIT = {
  MG: 'mg',
  MCG: 'μg',       // 存储统一用 μg，不存 mcg
  G: 'g',
  IU: 'IU',
  ML: 'ml',
} as const
```

中文显示映射：

```ts
export const INGREDIENT_UNIT_LABEL = {
  mg: 'mg',
  'μg': 'μg',
  g: 'g',
  IU: 'IU',
  ml: 'ml',
}
```

**规则**：
- **存储层只认 `μg`，不存 `mcg`**。
- 用户输入 `mcg` 时，Zod `transform` 自动转为 `μg` 再存。
- `IU` 不参与 mg/μg 换算，独立累加、独立展示。
- `ml` 用于液体成分，**不参与 μg 归一化，独立累加、独立展示**。
- 成分含量一律整数。

#### 2.11.3 两个维度的隔离

- `UNIT_TYPE` 与 `INGREDIENT_UNIT` 是**两套独立常量**，代码中不可互相引用或混用。
- 二者都有 `ml` / `g`，但含义不同。
- 建议放在 `constants/units.ts`，导出时明确命名，禁止简写。

### 2.12 库存状态常量（constants/stockState）【v10新增】

```ts
export const STOCK_STATE = {
  DEDUCTED: 'deducted',           // 已扣库存，未软删除
  NOT_DEDUCTED: 'not_deducted',   // 未扣库存，未软删除
  WAS_DEDUCTED: 'was_deducted',   // 曾扣过库存，软删除时已回滚，恢复时需重扣
  UNKNOWN: 'unknown',             // v3 及更早迁移遗留，恢复时用户二选一
} as const

export type StockState = typeof STOCK_STATE[keyof typeof STOCK_STATE]

export const STOCK_STATE_LABEL = {
  deducted: '已扣库存',
  not_deducted: '未扣库存',
  was_deducted: '曾扣库存（已回滚）',
  unknown: '库存状态未知',
}
```

---

## 3. 功能模块

### 3.1 今日记录模块

- **计划展示**：按时段分组展示今日服用计划，停药期内置灰并标注"停药中"。
- **一键打卡**：
    - 点击即标记为 taken，即时视觉反馈。
    - **幂等性**：重复点击提示"已打卡"，提供"追加一次"选项。
    - **批量操作**：按时段一键全部打卡。**临期项跳过，不打断批量流程，结果 Toast 中说明**。
        - **批量 Toast 收集临期项**：批量结果 Toast 中列出被跳过的临期补剂名，如"已打卡 5 项，跳过 2 项临期补剂：维生素D、鱼油"。
    - **库存不足处理**：扣减后 `stockCountInUsageUnit < 0` 时，**允许记录写入**，但 Toast 警告"补剂 X 库存已为负，请补货"。不阻断打卡。
- **手动录入**：记录临时/非计划内服用（source: manual）。同样允许负库存 + 警告。
    - **今日手动录入不允许 `status = skipped`**（**Zod 层允许，Service 层按场景拒绝**）：Service 层拒绝，提示"**今日漏服请明日通过补录功能标记**"。
- **撤销操作**：PC 场景下改为行内撤销按钮——打卡后该行显示"撤销"按钮，不消失，用户随时可撤销。撤销即软删除该记录，会回滚库存（写入 StockLog，reason=undo_intake）并软删除记录，`stockState` 置为 `was_deducted`。
- **历史修正**：支持对历史记录进行修改、补充、删除。软删除 = 撤销，回滚库存；彻底删除仅清理回收站，不再触碰库存。
    - **修改 `status` 时 Service 重算 `stockState`**（详见 5.6）。
    - **软删除记录禁止直接历史修正**：必须先恢复，再修改。API 层抛出业务异常。
- **成分汇总**：实时展示今日各成分摄入总量及来源。**仅统计 `deletedAt === 0` 的记录**。

**补录模块**：

- **入口**：
  - 今日页面顶部"补录昨日"快捷按钮。
  - 手动录入弹窗内含日期选择器，可选过去 30 天（**不含今天**）。
  - 日历视图点击历史日期，进入详情后可新增记录（**同受 30 天限制且不含今天**）。
- **日期范围**：**默认过去 30 天且不含今天**（`Meta.backfillWindowDays` 可配置），禁止未来日期，禁止今天。
  - 今日漏服需次日补录，与次日提醒设计意图一致。
- **次日提醒**：
  - 应用启动时 + 每次进入首页时检测过去 7 天"计划内未记录"项（缓存 5 分钟，详见 5.8）。
  - **【v10修订】检测范围排除今天**。
  - 若存在，首页 Banner 提示："过去 7 天有 N 项计划未记录，是否补录？"
  - 点击进入补录弹窗，按日期分组列出未记录项。
- **补录弹窗**：
  - 日期选择器（**默认昨天，可选过去 30 天且不含今天**）。
  - 按所选日期查询当时有效计划与停药状态。
  - **每项提供两个选项**：
    - **已服用** → 创建 `status = taken`（用户主动选部分服用 → `partial`；无计划 → `extra`），扣库存。
    - **漏服** → 创建 `status = skipped`，**不扣库存**。
    - **不提交即视为未处理**，但次日提醒仍会提示（因无记录）。
  - 支持批量"全部已服用 / 全部漏服"。
  - **批量操作跳过已有记录**：若某项 `date + supplementId + timeSlot` 已有未删除记录，跳过并在 Toast 说明。
  - **【v10修订】批量 Toast 区分跳过原因**：
    - "已跳过 N 项（已有记录）"
    - "已跳过 M 项（临期补剂：XXX、YYY）"
    - 提供"**查看并处理跳过项**"入口，支持逐条追加 / 修改原记录 / 忽略。
  - **部分提交语义明确**：用户点击"提交"时，仅提交**本次弹窗中已勾选/已填写**的项；未操作的项视为未处理，不产生记录。弹窗关闭不自动提交。
- **重复检测**：
  - 补录时在 **Dexie 事务内二次校验** `date + supplementId + timeSlot` 是否已有未删除记录（详见 5.8）。
  - 若已有：提示"该时段已有记录"，提供"追加"或"修改原记录"。
- **补录展示**：
  - 记录详情同时显示"服用日期"（`date`）与"录入时间"（`createdAt`）。
  - `date` 与 `createdAt` 日期不同时，标注"补录"。
- **库存扣减**：补录"已服用"时按 `actualAmount` 扣当前库存，与 `date` 无关。若扣减后为负，Toast 警告，不阻断。
- **统计归属**：补录记录按 `date` 计入历史当日统计与完成度；不影响"今日"统计。**成分汇总中不特殊标注**（与普通记录一致）。

### 3.2 补剂库管理模块
- **CRUD**：补剂的增删改查。
- **成分关联**：关联多个成分，设定每份含量，支持配方版本管理。
- **库存管理**：
    - 记录库存数量（**按服用单位计，整数**），支持"不记录"模式。
    - **自动扣减**：打卡/保存记录时直接扣减整数；软删除（撤销）时回滚整数；恢复时重新扣减；purge 不触发库存变化。
    - **一致性**：库存变动与 DailyIntake 增删改强一致，统一由 Service 层处理。
    - **库存流水**：每次变动写入 StockLog（按服用单位计）。
    - **负库存展示**：`stockCountInUsageUnit < 0` 时以警告色显示，并在库存列表顶部 Banner 提示。
- **预警系统**：
    - **临期提醒**：默认 30 天内，**单独打卡时**二次确认；批量打卡时跳过并在结果 Toast 中列出。
    - **库存不足**：低于阈值提醒（默认剩余 < 7 天用量）。**负库存始终高亮提醒**。
    - **展示**：库存同时展示服用单位和换算后的库存单位（"98 粒 / 约 1.63 瓶"）。

### 3.3 成分库管理模块
- **CRUD**：成分增删改查。
- **标准设定**：推荐每日摄入量和每日上限。
- **来源追溯**：查看某成分被哪些补剂包含。

### 3.4 服用计划模块
- **计划设定**：每日服用量、时段。
- **启停控制**：启用/停用计划。
- **联动逻辑**：停药期间计划不执行，但保留展示（置灰）。

### 3.5 停药期管理模块
- **全局/局部**：指定补剂或全局的停药期。
- **模式支持**：一次性、周期性（锚点日期 + 吃 N 天停 M 天）。
- **计算逻辑**：`isPaused(supplementId, date)` 纯函数，**补剂级与全局为并集**——任一覆盖该日即停药。返回 `{ paused: boolean, reasons: PausePeriod[] }`，便于 UI 展示触发原因。
- **可视化**：日历或列表展示当前及历史停药期。

### 3.6 每日成分汇总视图
- **总量统计**：按日期查看当天所有成分摄入总量。**仅统计 `deletedAt === 0` 的 DailyIntake**。补录记录按 `date` 计入对应历史日期。
- **合规检查**：与推荐量/上限对比。只提醒超标，不足不主动提示。
- **来源明细**：展示每种成分来源于哪些补剂。
- **计算边界**：仅按标签量累加，不考虑生物利用度；**汇总包含孤儿记录（补剂被软删除但 DailyIntake 未删除），标注"含已删除补剂"**。
- **软删除语义**：已软删除的 DailyIntake **完全不计入**，不显示、不标注。

### 3.7 日历视图
- **热力图**：展示每日服用完成度。
    - **完成度公式**：已服计划项 / (应服计划项 - 停药期排除项)。**仅统计 `deletedAt === 0` 的 DailyIntake**。手动录入不计入完成度；分母为 0 显示"无计划"灰色。
    - **"应服计划项"定义**：按 `DosagePlan.timeSlots` 展开为条目数。一个计划含 morning+evening → 算 2 项。
    - **标记**：漏服（黄）、多服（红）。
    - **补录记录计入对应 `date` 的完成度**，不影响今日。
- **详情交互**：点击某天查看当日详细摄入记录（右侧栏展示）。
- **补录入口**：日期详情右侧栏提供"新增记录"按钮，日期固定为所选日期（**同受 30 天限制且不含今天**）。

### 3.8 统计报表（P2 可选）
- 周/月服用率统计。
- 各成分平均日摄入量趋势。
- 身体反馈与服用记录关联展示。
- **【v10修订】计划依从性统计**：基于 `plannedAmountSource = 'plan_snapshot'` 的记录才计算偏差；`'current_plan'` 的记录标注"计划量参考值不可信"，不计入依从性指标。

### 3.9 数据管理模块
- **导出**：JSON 格式，包含 meta 和 data。导出默认包含已软删除记录。目标为 gzip 压缩后 < 5MB。导出 JSON 中 `deletedAt` 为 `0` 或 ISO 字符串。
- **导入**：
    - **强制备份**：导入前自动导出当前数据备份。
    - **策略选择**："覆盖全部"或"智能合并"（按 updatedAt 较新者为准）。
    - **版本处理**：Schema 版本不一致时先迁移再导入。
    - **软删除尊重**：合并时尊重 deletedAt 状态。
    - **导入结果摘要**：新增 X 条、覆盖 Y 条、保留本地 Z 条。
    - **去掉"跳过已删除记录"选项**：统一走 `mergeRecord`（较新者为准），避免语义歧义。导入文件中已删除的记录会按 updatedAt 参与合并。
    - **Meta 不合并**：导入后保留本地 Meta，仅 schemaVersion 更新。**`backfillWindowDays` 同样保留本地值**。
    - **状态异常记录不静默修正**：列入异常清单，用户人工处理（详见 5.7）。
    - **旧版导入兼容**：若导入文件使用 `deletedAt: null`（v4 及更早），导入时自动转为 `0`。
- **版本迁移**：Schema 变更时自动迁移，迁移函数幂等、可测试。
- **清除数据**：二次确认的全量清除（含物理删除已软删除记录）。
- **回收站**：
    - **分表展示**：按表（补剂/成分/关联/计划/记录/停药期）分 Tab 展示，每 Tab 列出该表所有 `deletedAt !== 0` 的记录。
    - 支持**恢复**（deletedAt 置 `0`）、**彻底删除**（物理删除）、**一键清空**（二次确认）。
    - **彻底删除 DailyIntake 仅清理回收站，不触碰库存**。
    - **批量操作 = 循环单条**：不聚合 StockLog，每条 intake 一条流水。
    - **恢复 DailyIntake 时重新扣减库存**（仅当 `stockState === 'was_deducted'`，置回 `deducted` 并重扣；`unknown` 弹出二选一对话框，详见 5.6）。
    - **`'unknown'` 记录在回收站中高亮标注**，恢复时弹出对话框让用户选择"视为未扣"或"视为已扣"。

---

## 4. 页面结构

### 4.1 首页 / 今日
- 今日服用计划清单（按时段分组，停药置灰）。
- 一键打卡入口。
- "补录昨日"快捷按钮。
- 次日补录提醒 Banner（过去 7 天有未记录项时显示，**排除今天**）。
- 今日成分摄入汇总卡片。
- 库存/过期预警 Banner（负库存高亮）。
- 空状态引导。

### 4.2 补剂库
- 补剂列表（表格，标记库存/过期状态，负库存警告色）。
- 补剂详情/编辑（侧边抽屉）。
- 成分关联管理（含版本历史）。

### 4.3 成分库
- 成分列表（表格）。
- 成分详情/编辑。

### 4.4 服用计划
- 计划列表。
- 计划编辑。

### 4.5 停药期
- 当前生效的停药期列表。
- 历史停药期记录。
- 新增/编辑停药期。

### 4.6 日历
- 日历视图主页面（热力图）。
- 日期详情右侧栏。
- 日期详情"新增记录"按钮（补录入口，同受 30 天限制且不含今天）。

### 4.7 统计（P2）
- 报表页面。

### 4.8 设置
- 数据导出/导入。
- 清除数据。
- 回收站入口（分表 Tab）。
- 默认值集中设置（库存预警天数、临期提醒天数、默认服用时段、**补录范围天数**）。
- 数据统计面板（各表记录数、占用空间估算）。
- 隐私说明。
- 关于/版本信息。

---

## 5. 功能逻辑规范

### 5.1 打卡幂等性与状态

- **默认行为**：同一补剂、同一时段、同一天，重复点击提示"已打卡"，不产生新记录。
- **追加模式**：点击"追加"按钮，创建新记录。追加创建 `source=manual, status=extra`，planId 可空或保留原 planId 仅作参考。
- **状态流转**：
    - taken：正常服用。
    - skipped：计划内漏服（仅手动标记，不自动）。
    - extra：计划外多服。
    - partial：**用户主动标记部分服用**（不再由系统对比 `plannedAmount` 自动判定）。
- **合法组合矩阵（Zod 层允许）**：

| source | 合法 status（Zod 允许） |
|--------|-------------------------|
| plan | taken / skipped / partial |
| manual | taken / extra / partial / skipped |

**Zod 与 Service 分层校验**：

- **Zod 层**：仅校验字段级合法性，**允许 `manual + skipped` 组合**（因补录漏服需要）。
- **Service 层按场景拒绝**：
  - **今日手动录入**：拒绝 `manual + skipped`，提示"**今日漏服请明日通过补录功能标记**"。
  - **补录**：允许 `manual + skipped`。
  - **历史修正**：允许改 `skipped`（用户可修正误标）。
- 此设计使 v6→v7→v8→v9→v10 无 Schema 变更（仅校验放宽），迁移脚本无需改动。

### 5.2 库存扣减一致性
- **触发时机**：DailyIntake 创建、更新、**软删除（撤销）**、**恢复**、**历史修正改状态**时。**彻底删除（purge）不触发库存变化。**
- **事务保障**：**所有库存相关操作必须在单个 Dexie `readwrite` 事务内完成读-判断-写**，杜绝跨标签页竞态。
- **历史记录**：修改历史记录时，计算 delta = newAmount - oldAmount，更新库存（`actualAmount` 变化）。
- **临时录入**：source: manual 的记录同样扣减库存。
- **不记录模式**：若 `stockCountInUsageUnit` 为 null，`stockState = 'not_deducted'`，跳过扣减。
- **负库存允许**：扣减后若为负，**仍写入**，仅警告。
- **软删除（撤销）触发库存回滚**：详见 5.6 状态转移表。
- **恢复（restore）触发库存重扣**：详见 5.6 状态转移表。
- **彻底删除（purge）不触发库存变化**：仅物理删除记录。
- **历史修正改状态触发库存变化**：详见 5.6 状态转移表。
- **幂等保障**：任何回滚/重扣前先检查 `stockState`，状态已匹配则跳过。

**补录的库存扣减规则**：

- **补录"已服用"**：扣当前库存（`stockCountInUsageUnit -= actualAmount`），写 StockLog（reason=`manual_intake`），与 `date` 无关。
- **补录"漏服"**：不扣库存，`stockState = 'not_deducted'`。
- **库存基准**：始终基于**当前库存**，不回溯历史。若用户今天已补货，补录昨天会从"补货后的库存"里扣，这是正确行为。
- **负库存**：扣减后为负仍写入，Toast 警告。

### 5.3 停药期计算逻辑
- **纯函数**：`isPaused(supplementId: string, date: string): { paused: boolean, reasons: PausePeriod[] }`。
- **并集语义**：若该补剂存在任一 PausePeriod 覆盖 date，**或**全局存在任一 PausePeriod 覆盖 date，则 `paused = true`。
- **周期性计算**：
  1. 先判断 `startDate <= date <= endDate`（endDate 为空则只判 startDate）。
  2. 若通过，计算 `diff = date - cycleStartDate`，`remainder = diff % (cycleOnDays + cycleOffDays)`。
  3. 若 `remainder >= cycleOnDays` 则为停药日。`cycleStartDate` 当天算周期第 1 天（吃）。
- **今日计划**：停药期间计划仍展示，但 disabled=true。
- **补录按 `date` 查询停药状态**，不是按录入时刻。

### 5.4 成分汇总计算
- **单位统一**：
    - **重量类成分（mg/μg/g）**：先归一化到 `μg`。
        - `1 mg = 1000 μg`
        - `1 g = 1000000 μg`
        - `1 mcg = 1 μg`（输入时已转）
    - **IU**：独立累加，不与 μg 混算。
    - **ml**：独立累加，**不参与 μg 归一化**。
    - **显示时按数值大小自动选单位（仅重量类）**：
        - `< 1000 μg` → 显示 μg
        - `≥ 1000 μg 且 < 1000000 μg` → 显示 mg
        - `≥ 1000000 μg` → 显示 g
    - 同一天同一成分如果同时有 IU 和 μg 记录，**分两行显示**，不合并。ml 同理。
    - **单位选择按"合计后数值"判定**：同一天同一成分的多条记录先合计，再按合计值选单位。
- **版本匹配**：根据 DailyIntake.date 匹配当时有效的 SupplementIngredient。**匹配时 includeDeleted=true**。
- **与 `getActivePlansForDate` 的不对称性为有意设计**：
  - `DosagePlan` **无版本历史** → 补录时 `plannedAmountSnapshot` 取**当前**计划量。
  - `SupplementIngredient` **有 `effectiveFrom/To` 版本** → 成分汇总按 `date` 匹配**当时**配方。
  - 原因：补剂的成分含量是硬事实（历史可考），而计划量是用户意图（当前设置更有参考价值）。
  - **UI 在补录弹窗中明示**："计划量基于当前计划设置，成分含量按历史配方计算。"
- **软删除过滤**：仅统计 `DailyIntake.deletedAt === 0` 的记录。
- **孤儿记录处理**：补剂被软删除后，其 DailyIntake 仍计入汇总，标注"含已删除补剂"。
- **关联缺失处理**：若 DailyIntake 对应的 SupplementIngredient 已彻底删除，无法计算成分，UI 标注"成分数据缺失"。
- **边界说明**：UI 上明确告知"数据基于标签含量累加，不代表医学精确摄入量"。
- **补录记录按 `date` 计入汇总**，**不特殊标注**（与普通记录一致）。

### 5.5 数据导入策略
> 合并逻辑统一定义在 5.7，本节仅列流程。

- **预检查**：校验 JSON 结构、Schema 版本，扫描状态异常记录（详见 5.7）。
- **备份**：导入前自动触发 exportData()。
- **策略**：覆盖全部 / 智能合并（详见 5.7）。
- **版本处理**：Schema 版本低时先迁移。
- **结果摘要**：导入完成后展示统计，含异常清单。
- **Meta 处理**：不合并，导入后保留本地 Meta，仅 schemaVersion 按迁移流程更新。**`backfillWindowDays` 同样保留本地值**。
- **旧版兼容**：导入文件若使用 `deletedAt: null`，自动转为 `0`。

### 5.6 库存扣减逻辑

**核心规则**：
- 库存以**服用单位**存储为整数（`stockCountInUsageUnit`），**允许为负**。
- 扣减公式：`新库存 = 原库存 - actualAmount`。
- 回滚公式：`新库存 = 原库存 + actualAmount`。

**状态字段**：
- `stockState`：**单一状态机，四态**，`'deducted' | 'not_deducted' | 'was_deducted' | 'unknown'`。
- 语义：**当前库存事实 + 恢复意图**。`'deducted'` 严格表示"当前库存已扣"；`'was_deducted'` 表示"曾扣过、已回滚、恢复时需重扣"。

**【v10修订】完整状态转移表**：

| 当前 `stockState` | 当前 `deletedAt` | 操作 | 新 `stockState` | 新 `deletedAt` | 库存动作 | StockLog reason |
|-------------------|------------------|------|-----------------|----------------|----------|-----------------|
| （新建，有库存） | 0 | 创建 taken/partial/extra | `deducted` | 0 | `-= actualAmount` | `intake` / `manual_intake` |
| （新建，无库存） | 0 | 创建 taken/partial/extra | `not_deducted` | 0 | 无 | 无 |
| （新建） | 0 | 创建 skipped | `not_deducted` | 0 | 无 | 无 |
| `deducted` | 0 | 软删除 | `was_deducted` | now | `+= actualAmount` | `undo_intake` |
| `not_deducted` | 0 | 软删除 | `not_deducted` | now | 无 | 无 |
| `was_deducted` | now | 恢复 | `deducted` | 0 | `-= actualAmount` | `intake` |
| `not_deducted` | now | 恢复 | `not_deducted` | 0 | 无 | 无 |
| `unknown` | now | 恢复（用户选"视为未扣"） | `not_deducted` | 0 | 无 | 无（UI 警告） |
| `unknown` | now | 恢复（用户选"视为已扣"） | `deducted` | 0 | `-= actualAmount` | `adjust`（note: "unknown 恢复时用户选视为已扣"） |
| `deducted` | 0 | 修正改 skipped | `not_deducted` | 0 | `+= actualAmount` | `adjust` |
| `not_deducted` | 0 | 修正改 taken/partial/extra（有库存） | `deducted` | 0 | `-= actualAmount` | `adjust` |
| `not_deducted` | 0 | 修正改 taken/partial/extra（无库存） | `not_deducted` | 0 | 无 | 无 |
| `deducted` | 0 | 修正改量 | `deducted` | 0 | `-= (new - old)` | `adjust` |
| `not_deducted` | 0 | 修正改量 | `not_deducted` | 0 | 无 | 无 |
| `was_deducted` | now | 修正（禁止） | — | — | 抛异常 | — |
| `unknown` | now | 修正（禁止） | — | — | 抛异常 | — |

> **【v10修订】`unknown` 记录恢复时如何处置？**
>
> v9 直接置为 `not_deducted`，等于单方面宣布"没扣过"，不可逆。v10 改为**用户二选一对话框**：
>
> ```
> 该记录来自旧版本，无法确定当时是否扣过库存。
> 请选择恢复方式：
>   [ A ] 视为未扣（不重扣库存）
>   [ B ] 视为已扣（重扣 actualAmount = N 粒）
> ```
>
> - 选 A：`stockState = 'not_deducted'`，`deletedAt = 0`，不重扣，不写 StockLog。
> - 选 B：`stockState = 'deducted'`，`deletedAt = 0`，扣减 `actualAmount`，写 StockLog（reason=`adjust`，note 说明）。
> - 两种选择都应在 Toast 中提示"已恢复，库存处理方式：视为未扣/视为已扣"，并提供"如需修正请手动调整"入口。

**一致性约束（最终版）**：
- `stockState ∈ { 'deducted', 'not_deducted', 'was_deducted', 'unknown' }`
- `deletedAt === 0` → `stockState ∈ { 'deducted', 'not_deducted' }`（不允许 `was_deducted` / `unknown`）
- `deletedAt !== 0` → `stockState ∈ { 'was_deducted', 'unknown' }`（不允许 `deducted` / `not_deducted`）
- 由 Zod refine 强制。
- **注意**：`not_deducted` 记录软删除后仍为 `not_deducted`（而非 `was_deducted`），因为从未扣过，恢复时也不需重扣。`was_deducted` 仅用于"曾经扣过、软删除时已回滚"的记录。

**UI 换算显示**：
- 若 `stockUnit` 和 `unitsPerStock` 存在：
    - 显示库存单位数 = `stockCountInUsageUnit / unitsPerStock`（保留 1~2 位小数展示）。
    - 同时显示："98 粒 / 约 1.63 瓶"。
- 若 `stockUnit` 为空或与 `unitType` 相同：只显示"98 粒"。
- **负库存显示**：显示"-3 粒（库存不足）"，警告色。

**边界情况**：
| 情况 | 处理 |
|------|------|
| `stockCountInUsageUnit` 为 null | `stockState = 'not_deducted'`，跳过库存逻辑 |
| `stockUnit != null && stockUnit !== unitType && unitsPerStock == null` | Zod 校验拦截，提示"请填写每瓶含量" |
| 用户只想记"还剩几粒" | `stockUnit` 留空，只填 `stockCountInUsageUnit` |
| 扣减后库存 < 0 | **允许写入**，Toast 警告"库存已为负" |
| `stockUnit === unitType` | `unitsPerStock` 可空，UI 只显示服用单位 |

**打卡扣减流程**：
1. 在单个 Dexie `readwrite` 事务内：
2. 读取 `stockCountInUsageUnit`。
3. 若为 null：`stockState = 'not_deducted'`，跳过库存扣减。
4. 若不为 null 且 `status ∈ { taken, partial, extra }`：
   - `stockState = 'deducted'`。
   - 更新 `stockCountInUsageUnit -= actualAmount`。
   - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`intake`）。
5. 若 `status === skipped`：`stockState = 'not_deducted'`，跳过扣减。
6. 若扣减后 < 0，Toast 警告。

**补录扣减流程**：

1. 用户选择日期 `date`（**过去 30 天且不含今天**）与补录项。
2. 若选择"漏服"：
   - 创建 `status = skipped`，`source = manual`，`stockState = 'not_deducted'`。
   - 不扣库存，不写 StockLog。
3. 若选择"已服用"：
   - 读取 `stockCountInUsageUnit`。
   - 若为 null：`stockState = 'not_deducted'`，跳过扣减。
   - 若不为 null：
     - `stockState = 'deducted'`。
     - `stockCountInUsageUnit -= actualAmount`。
     - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`manual_intake`）。
   - 若扣减后 < 0，Toast 警告。
4. 库存扣减发生在**录入时刻**，与 `date` 无关。

**历史修正流程**：

用户修改已有 DailyIntake 时，Service 层按以下规则处理库存：

**前置检查**：
- 若 `deletedAt !== 0`，抛出业务异常"请先恢复该记录再修改"。

**情况 1：仅修改 `actualAmount`（`status` 不变）**：
- `delta = newActualAmount - oldActualAmount`。
- 若 `stockState === 'deducted'`：`stockCountInUsageUnit -= delta`，写 StockLog（reason=`adjust`，delta=`-delta`）。
- 若 `stockState === 'not_deducted'`：跳过。

**情况 2：修改 `status`**：
- 计算 `newNeedDeduct = (newStatus ∈ { taken, partial, extra }) && (supp.stockCountInUsageUnit != null)`。
- 若 `newNeedDeduct === true && stockState === 'not_deducted'`：
  - `stockCountInUsageUnit -= actualAmount`（补扣）。
  - 写 StockLog（reason=`adjust`，delta=`-actualAmount`）。
  - 置 `stockState = 'deducted'`。
- 若 `newNeedDeduct === false && stockState === 'deducted'`：
  - `stockCountInUsageUnit += actualAmount`（回滚）。
  - 写 StockLog（reason=`adjust`，delta=`+actualAmount`）。
  - 置 `stockState = 'not_deducted'`。
- 若 `newNeedDeduct === (stockState === 'deducted')`：不变。

**情况 3：同时修改 `status` 与 `actualAmount`**：
- 简化实现：先回滚旧状态，再按新状态扣减。
  1. 若 `stockState === 'deducted'`：回滚 `+= oldActualAmount`，写 StockLog（reason=`adjust`）。
  2. 计算 `newNeedDeduct`。
  3. 若 `newNeedDeduct === true`：扣减 `-= newActualAmount`，写 StockLog（reason=`adjust`）。
  4. 置 `stockState = newNeedDeduct ? 'deducted' : 'not_deducted'`。

> **【v10修订·M-6】校验时机**：`skipped + stockState='deducted'` 的 Service 层校验**只在最终写入 DailyIntake 时执行**。上述中间态（回滚后、扣减前）全部放在内存变量中，事务内只做一次最终写入，避免误伤中间态。

**软删除（撤销）流程**：
1. 在单个 Dexie `readwrite` 事务内：
2. 读取 DailyIntake。
3. 若 `deletedAt !== 0`，跳过（幂等）。
4. 若 `stockState === 'deducted'`：
   - 读取 `stockCountInUsageUnit`，若不为 null：`+= actualAmount`。
   - 写入 StockLog（`deltaInUsageUnit = +actualAmount`，reason=`undo_intake`）。
   - **置 `stockState = 'was_deducted'`**。
5. 若 `stockState === 'not_deducted'`：`stockState` 不变。
6. 置 `deletedAt = now`，更新 `updatedAt`。

**恢复（restore）流程**：
1. 在单个 Dexie `readwrite` 事务内：
2. 读取 DailyIntake。
3. 若 `deletedAt === 0`，跳过（幂等）。
4. 若 `stockState === 'unknown'`：
   - **弹出二选一对话框**（在事务外收集用户选择，事务内执行）：
     - 选"视为未扣"：置 `stockState = 'not_deducted'`，不重扣。
     - 选"视为已扣"：置 `stockState = 'deducted'`，重扣 `actualAmount`，写 StockLog（reason=`adjust`，note 说明）。
   - 置 `deletedAt = 0`。
5. 若 `stockState === 'was_deducted'`：
   - 读取 `stockCountInUsageUnit`，若不为 null：`-= actualAmount`。
   - 写入 StockLog（`deltaInUsageUnit = -actualAmount`，reason=`intake`）。
   - 置 `stockState = 'deducted'`。
6. 若 `stockState === 'not_deducted'`：不重扣，`stockState` 不变。
7. 置 `deletedAt = 0`，更新 `updatedAt`。

**彻底删除（purge）流程**：
1. 物理删除 DailyIntake 记录。
2. **不触碰库存，不写 StockLog**。
3. StockLog 中 `relatedIntakeId` 指向该 intake 的记录**保留**，形成悬空引用。

**批量操作流程**：
- **批量软删除**：循环调用单条软删除逻辑（同一 Dexie 事务）。
- **批量恢复**：循环调用单条恢复逻辑。**若批次中含 `unknown` 记录，恢复前统一弹出对话框列出所有 `unknown` 记录，让用户为每条选择"视为未扣/视为已扣"**，然后事务内执行。
- **批量 purge**：仅物理删除，不触碰库存。
- 不做聚合，不合并 StockLog。

**【v10修订·L-5】`stockService.applyTransition` 作为唯一入口**：

```ts
// services/stockService.ts
export type StockOperation =
  | { type: 'create'; intake: NewIntake; source: 'plan' | 'manual' }
  | { type: 'softDelete'; intakeId: string }
  | { type: 'restore'; intakeId: string; unknownChoice?: 'not_deducted' | 'deducted' }
  | { type: 'updateStatus'; intakeId: string; newStatus: Status; newActualAmount: number }
  | { type: 'updateAmount'; intakeId: string; newActualAmount: number }

// 单一入口：所有库存相关操作都调用此函数
export async function applyTransition(op: StockOperation): Promise<void> {
  await db.transaction('rw', db.dailyIntakes, db.supplements, db.stockLogs, async () => {
    switch (op.type) {
      case 'create': /* ... */ break
      case 'softDelete': /* ... */ break
      case 'restore': /* ... */ break
      case 'updateStatus': /* ... */ break
      case 'updateAmount': /* ... */ break
    }
  })
}
```

- `intakeService` 不直接操作 `supplements` / `stockLogs`，全部通过 `stockService.applyTransition`。
- `backfillService` 的"已服用"项也调用 `applyTransition({ type: 'create', source: 'manual' })`。
- 好处：状态机逻辑集中一处，测试覆盖一个函数即可，避免散落。

**代码示例**（`stockService.applyTransition` 内部实现，软删除分支）：

```ts
async function handleSoftDelete(intakeId: string) {
  const intake = await db.dailyIntakes.get(intakeId);
  if (!intake || intake.deletedAt !== 0) return;

  const now = new Date().toISOString();

  if (intake.stockState === 'deducted') {
    const supp = await db.supplements.get(intake.supplementId);
    if (supp && supp.stockCountInUsageUnit != null) {
      await db.supplements.update(supp.id, {
        stockCountInUsageUnit: supp.stockCountInUsageUnit + intake.actualAmount,
        updatedAt: now,
      });
      await db.stockLogs.add({
        id: crypto.randomUUID(),
        supplementId: supp.id,
        deltaInUsageUnit: intake.actualAmount,
        reason: 'undo_intake',
        relatedIntakeId: intake.id,
        note: null,
        deletedAt: 0,
        createdAt: now,
      });
    }
    await db.dailyIntakes.update(intakeId, {
      stockState: 'was_deducted',
      deletedAt: now,
      updatedAt: now,
    });
  } else {
    // not_deducted：不改 stockState
    await db.dailyIntakes.update(intakeId, {
      deletedAt: now,
      updatedAt: now,
    });
  }
}
```

### 5.7 软删除与回收站逻辑

**设计原则**：
- 所有业务表增加 `deletedAt: string | 0`：`0` = 未删除，ISO 字符串 = 已删除。
- 新建记录必须显式写 `deletedAt: 0`。
- Meta 表不加。
- **软删除 = 撤销（方案A）**：
  - DailyIntake 软删除后不计入任何汇总、统计、完成度，立即回滚库存（仅 `stockState === 'deducted'`）。
  - **【v10修订】软删除时 `deducted → was_deducted`**，`not_deducted` 保持不变。恢复时 `was_deducted → deducted` 并重扣，`not_deducted` 不重扣。
  - `'unknown'` 记录恢复时**用户二选一**（详见 5.6）。
  - 彻底删除仅清理回收站，不触碰库存。
  - `stockState` + `deletedAt` 保证幂等。
- 非 DailyIntake 表软删除不涉及库存，仅打标记。

**查询过滤**：
- Repository 读操作默认过滤 `deletedAt === 0`。
- 提供 `includeDeleted` 选项。
- **索引策略**：`deletedAt` 直接使用 `0` / ISO 字符串，可进复合索引。存储层与索引层统一，Repository 层零转换。
- **查询示例**：
  - 未删除：`db.dailyIntakes.where('[deletedAt+date]').between([0, startDate], [0, endDate])`
  - 回收站：`db.dailyIntakes.filter(r => r.deletedAt !== 0).toArray()`

**删除与恢复**：

```ts
// 通用软删除（非 DailyIntake）
async function softDelete(table, id) {
  await db[table].update(id, {
    deletedAt: new Date().toISOString(),
    updatedAt: new Date().toISOString(),
  });
}

// 通用恢复（非 DailyIntake）
async function restore(table, id) {
  await db[table].update(id, {
    deletedAt: 0,
    updatedAt: new Date().toISOString(),
  });
}
```

**级联删除策略**：
| 被删主表 | 关联表 | 策略 |
|----------|--------|------|
| Supplement | DosagePlan | 级联软删除 |
| Supplement | SupplementIngredient | 级联软删除 |
| Supplement | DailyIntake | 不删除（保留孤儿数据） |
| Supplement | StockLog | 保留 |
| Supplement | PausePeriod | 不级联（用户自行决定） |

**级联删除确认交互**：删除补剂时弹确认框，列出影响范围，用户确认后执行。

**孤儿数据处理**：
- DailyIntake 关联的补剂被软删除后，汇总视图显示"[已删除的补剂]"，但仍计入成分总量。
- StockLog 中补剂被删后，流水保留，补剂名显示"[已删除]"。
- **purge 后 StockLog 的悬空引用**：保留，UI 显示"[记录已彻底删除]"。

**导入合并逻辑（【v10修订·M-1】）**：

```ts
function mergeRecord(local, incoming, tableName) {
  if (!local) return incoming;
  const localTime = new Date(local.updatedAt).getTime();
  const incomingTime = new Date(incoming.updatedAt).getTime();
  if (incomingTime !== localTime) {
    return incomingTime > localTime ? incoming : local;
  }
  // 【v10修订】时间相同时默认取 local
  if (tableName === 'dailyIntakes') {
    // DailyIntake 涉及库存副作用，不能简单偏向删除意图
    return local;
  }
  // 非 DailyIntake 表：删除意图优先，避免丢失删除
  if (local.deletedAt !== 0 && incoming.deletedAt === 0) return local;
  if (incoming.deletedAt !== 0 && local.deletedAt === 0) return incoming;
  return local;
}
```

**导入时状态异常处理**：

**不静默修正**。导入前预扫描所有 DailyIntake，找出状态不一致记录：

| 类型 | 条件 | 含义 |
|------|------|------|
| A | `deletedAt !== 0 && stockState === 'deducted'` | **【v10修订】非法**（v9 合法，v10 非法）。列入异常清单，**跳过导入** |
| B | `deletedAt === 0 && stockState ∈ { 'was_deducted', 'unknown' }` | **【v10修订】非法**。列入异常清单，**跳过导入** |
| C | `status === 'skipped' && stockState === 'deducted'` | 漏服但标记已扣库存（矛盾）。列入异常清单，**跳过导入** |
| D | `deletedAt !== 0 && stockState === 'unknown'` | 已删除且状态未知（v3 遗留，正常）。正常合并 |
| E | `deletedAt !== 0 && stockState === 'was_deducted'` | 已删除且曾扣过（v10 正常）。正常合并 |

**处理规则**：
- 类型 A、B、C：**跳过导入**，计入异常清单，提示用户"该记录状态不合法，已跳过。请联系开发者或手动修正"。
- 类型 D、E：正常合并。
- **【v10修订】导入摘要显示**："发现 N 条状态异常记录（已跳过），建议人工核对"，提供"查看清单"入口。

**导入 DailyIntake 的 `stockState` 处理**：
- 导入文件若含 `stockState` 字段（v10 格式）：直接采用，按上表校验。
- 若为 v9 格式（三态 `deducted` / `not_deducted` / `unknown`）：
  - `deletedAt === 0 && stockState === 'deducted'` → `deducted`
  - `deletedAt === 0 && stockState === 'not_deducted'` → `not_deducted`
  - `deletedAt !== 0 && stockState === 'deducted'` → **`was_deducted`**（v9 软删除态保留 deducted，v10 转换为 was_deducted）
  - `deletedAt !== 0 && stockState === 'not_deducted'` → `not_deducted`
  - `stockState === 'unknown'` → `unknown`
  - 摘要提示"已为 N 条 v9 记录转换 stockState（deducted → was_deducted）"。
- 若为 v8 格式（含 `stockDeducted` + `stockRollbackState`）：
  - `stockDeducted === true && stockRollbackState === 'not_rolled_back'` → `deducted`
  - `stockDeducted === false && stockRollbackState === 'not_rolled_back'` → `not_deducted`
  - `stockDeducted === true && stockRollbackState === 'rolled_back'` → **`was_deducted`**
  - `stockDeducted === false && stockRollbackState === 'rolled_back'` → `not_deducted`
  - `stockRollbackState === 'unknown'` → `unknown`
- 若为 v5 及更早（无这两个字段）：
  - `deletedAt === 0` 的记录：`stockState = (supp.stockCountInUsageUnit != null) ? 'deducted' : 'not_deducted'`。
  - `deletedAt !== 0` 的记录：`stockState = 'unknown'`。
- 在摘要中提示"已为 N 条旧版记录推断 stockState"。

**回收站功能**：
- **分表 Tab 展示**：补剂 / 成分 / 关联 / 计划 / 记录 / 停药期。
- 每 Tab 列出该表 `deletedAt !== 0` 的记录。
- 操作：恢复、彻底删除、一键清空（二次确认）。
- **恢复 DailyIntake 触发库存重扣**（`was_deducted → deducted` 并重扣；`unknown` 用户二选一）。
- **彻底删除 DailyIntake 仅物理删除，不触碰库存**。
- **批量操作 = 循环单条**，不聚合 StockLog。
- **`'unknown'` 记录在回收站中高亮标注**，恢复时弹出对话框让用户选择。

### 5.8 补录逻辑

**核心原则**：
- 补录 = 用户为历史日期手动创建记录。
- 补录不改变 `date` 语义，统计按 `date` 归属。
- 库存扣减发生在**录入时刻**，与 `date` 无关。
- 计划/停药期匹配按 `date` 查询。

**补录范围**：
- **默认过去 30 天且不含今天**（`Meta.backfillWindowDays` 可配置）。
- 禁止未来日期。
- **禁止今天**（今日漏服请明日补录）。
- 超出范围：UI 提示"超出补录范围，请前往日历手动修正"或直接禁用。
- **历史修正（修改已存在记录）不受 30 天限制，也不受"不含今天"限制**。补录（新建）受限制。

**`getActivePlansForDate(date)` 语义**：
- 查询 `DosagePlan` 表中 `isActive === true` 且 `deletedAt === 0` 的计划。
- 不按 `effectiveFrom`/`effectiveTo` 过滤（DosagePlan 无版本管理）。
- 返回当日"应服计划项"列表。

**次日提醒检测逻辑（【v10修订·M-3】）**：

```ts
// 应用启动时 + 进入首页时调用（缓存 5 分钟，软删除/恢复后主动 invalidate）
async function detectMissedPlans(): Promise<MissedPlan[]> {
  const today = format(new Date(), 'yyyy-MM-dd');
  const lookbackDays = 7; // 固定 7 天
  const missed: MissedPlan[] = [];

  // 【v10修订·L-4】批量查询优化：一次性查出 7 天内所有记录，内存分组比对
  const startDate = format(subDays(new Date(), lookbackDays), 'yyyy-MM-dd');
  const endDate = format(subDays(new Date(), 1), 'yyyy-MM-dd'); // 排除今天
  const allIntakes = await db.dailyIntakes
    .where('[deletedAt+date]')
    .between([0, startDate], [0, endDate])
    .toArray();
  const intakeMap = new Map<string, Set<string>>(); // key: `${date}|${supplementId}`, value: Set<timeSlot>
  for (const r of allIntakes) {
    const key = `${r.date}|${r.supplementId}`;
    if (!intakeMap.has(key)) intakeMap.set(key, new Set());
    intakeMap.get(key)!.add(r.timeSlot);
  }

  for (let i = 1; i <= lookbackDays; i++) {
    const date = format(subDays(new Date(), i), 'yyyy-MM-dd');
    if (date === today) continue; // 排除今天
    const plans = await getActivePlansForDate(date);
    for (const plan of plans) {
      for (const timeSlot of plan.timeSlots) {
        const key = `${date}|${plan.supplementId}`;
        const recorded = intakeMap.get(key)?.has(timeSlot) ?? false;
        const { paused } = isPaused(plan.supplementId, date);
        if (!recorded && !paused) {
          missed.push({ date, supplementId: plan.supplementId, timeSlot, plan });
        }
      }
    }
  }
  return missed;
}
```

- **检测范围**：过去 7 天（**不含今天**）。
- **排除停药日**。
- **已标记 `skipped` 的记录视为"已处理"**，不再提醒（因有记录）。
- **已软删除记录视为"未记录"**，会重新提醒。
- 结果按日期分组展示。
- **触发时机**：应用启动时一次 + 每次进入首页时重新检测（缓存 5 分钟）。
- **【v10修订·M-3】软删除 / 恢复 DailyIntake 后主动 invalidate 缓存**：调用 `useMissedPlans.invalidate()`，不等 5 分钟。
- **【v10修订·L-4】批量查询优化必须实现**：一次性查出 7 天内所有记录，内存分组，避免 560 次索引查询。目标 < 100ms。

**补录弹窗交互**：
1. 用户选择日期（**默认昨天，可选过去 30 天且不含今天**）。
2. 系统查询该日有效计划与停药状态。
3. 列出所有计划项与时段，每项提供两选项：
   - **已服用**：输入实际量（默认计划量，无计划则默认 1），创建 `status = taken`（用户主动选部分服用 → `partial`；无计划 → `extra`）。
   - **漏服**：创建 `status = skipped`，`source = manual`。
4. 支持"全部已服用 / 全部漏服"批量选择。
5. **批量操作跳过已有记录**：若某项已有未删除记录，跳过并在 Toast 说明"已跳过 N 项已有记录"。
6. 提交时在 Dexie 事务内对每项**二次校验重复**（避免并发）。
7. **部分提交语义**：仅提交已勾选/已填写的项，未操作的项视为未处理。
8. **【v10修订·M-4】批量 Toast 区分跳过原因**：
   - "成功 N 条，跳过 M 条（已有记录），跳过 K 条（临期补剂：XXX、YYY）"
   - 提供"**查看并处理跳过项**"入口，支持逐条追加 / 修改原记录 / 忽略。

**补录记录的写入规则**：

| 用户选择 | 匹配计划 | status | source | stockState | 库存操作 | StockLog reason | plannedAmountSnapshot | plannedAmountSource |
|----------|----------|--------|--------|------------|----------|-----------------|----------------------|---------------------|
| 已服用（量匹配计划） | 是 | taken | manual | 按库存是否为 null | 扣减 | manual_intake | 当前计划量 | current_plan |
| 已服用（用户主动选部分） | 是 | partial | manual | 按库存是否为 null | 扣减 | manual_intake | 当前计划量 | current_plan |
| 已服用（无计划） | 否 | extra | manual | 按库存是否为 null | 扣减 | manual_intake | null | unavailable |
| 漏服 | 是/否 | skipped | manual | not_deducted | 不扣 | 不写 | 当前计划量或 null | current_plan / unavailable |

> **`partial` 判定**：不再由系统对比 `plannedAmount` 自动判定。用户在补录弹窗中**主动选择"部分服用"**时才标 `partial`。若用户选择"已服用"且输入量与计划量不同，仍标 `taken`（用户认为已按自己意愿服用）。

**补录的计划匹配**：
- 按 `date` 查询该日有效 `DosagePlan`（`getActivePlansForDate(date)`）。
- 若匹配到计划：
  - `planId` = 该计划 id。
  - `plannedAmountSnapshot` = 该计划**当前**的 `dailyAmount`。
  - `plannedAmountSource = 'current_plan'`。
  - **文档明确**：`DosagePlan` 无版本历史，若计划已被修改，`plannedAmountSnapshot` 快照的是**当前值**，可能与当时实际计划不符。UI 在补录弹窗中提示"计划量基于当前计划设置"。
- 若无计划：
  - `planId = null`。
  - `plannedAmountSnapshot = null`。
  - `plannedAmountSource = 'unavailable'`。
  - `status = extra`（补录无计划项默认 `extra`）。

**打卡/手动录入的计划匹配（【v10新增】）**：
- 打卡/手动录入时，`plannedAmount` 直接取当天计划量（如有），`plannedAmountSource = 'plan_snapshot'`。
- 此值可信，因记录创建时计划就是当天的计划。

**补录的停药期匹配**：
- 按 `date` 调用 `isPaused(supplementId, date)`。
- 若为停药日：UI 标注"该日为停药日"，但**允许录入**。
- 停药日补录不计入"应服计划项"（完成度分母不含）。

**补录的展示**：
- 记录详情中同时显示"服用日期"（`date`）与"录入时间"（`createdAt`）。
- 若 `date !== format(new Date(createdAt), 'yyyy-MM-dd')`，标注"补录"。
- 日历视图中，补录记录正常显示。

**补录的库存扣减**：
- 与普通手动录入一致，扣当前库存。
- 扣减后为负仍写入，Toast 警告。
- 不回溯历史库存。

**补录的统计归属**：
- 按 `date` 计入历史当日成分汇总、完成度、日历热力图。
- 不影响"今日"统计。
- 补录"漏服"记录计入完成度分母（作为应服项），但不计入分子。

**补录的 `actualAmount` 默认值**：
- 匹配到计划：默认 = `plan.dailyAmount`。
- 无计划：默认 = `1`。
- 用户可修改。

**补录结果 Toast**：
- 全部成功："已补录 N 条记录"。
- 部分成功："成功 N 条，跳过 M 条（已有记录），跳过 K 条（临期补剂：XXX、YYY）"，并提供"查看并处理跳过项"入口。
- 漏服："已标记 N 条漏服"。
- 负库存警告："补剂 X 库存已为负"。

---

## 6. 技术实现规范

### 6.1 数据库与迁移

- **Dexie 版本**：`db.version(n).stores({...}).upgrade(tx => {...})`。
- **迁移脚本**：放在 `db/migrations/`，确保幂等。
- **Meta 同步**：迁移完成后更新 `Meta.schemaVersion`。

**v3 → v4 迁移**：
- DailyIntake 新增 `stockDeducted: boolean`。
- DailyIntake 新增 `stockRollbackState: 'rolled_back' | 'not_rolled_back' | 'unknown'`。
- **v3 已软删除的记录**：`stockRollbackState = 'unknown'`，`stockDeducted = true`。
- **v3 未删除的记录**：`stockRollbackState = 'not_rolled_back'`，`stockDeducted = (supp.stockCountInUsageUnit != null)`。

**v4 → v5 迁移**：
- 所有表的 `deletedAt` 字段：`null` → `0`。

**v5 → v6 迁移**：
- DailyIntake 新增 `stockDeducted`，回填规则见上。
- `stockRolledBack: boolean` 转换为 `stockRollbackState`。
- 删除 `stockRolledBack` 字段。

**v6 → v7 迁移**：
- **无 Schema 变更**。补录能力复用现有字段。
- Meta 新增 `backfillWindowDays`，默认 `30`。

**v7 → v8 迁移**：
- **无 Schema 变更**。
- Zod schema 放宽 `manual + skipped` 组合。
- `stockDeducted` 语义变更（不涉及数据迁移）。
- `[reason+relatedIntakeId]` 唯一索引改为普通索引。

**v8 → v9 迁移**：
- DailyIntake 字段合并：删除 `stockDeducted` 和 `stockRollbackState`，新增 `stockState` 三态。
- 回填规则见 v9 文档。

**v9 → v10 迁移（【v10新增】）**：
- **DailyIntake `stockState` 从三态改为四态**：
  - `deletedAt === 0 && stockState === 'deducted'` → `deducted`
  - `deletedAt === 0 && stockState === 'not_deducted'` → `not_deducted`
  - `deletedAt !== 0 && stockState === 'deducted'` → **`was_deducted`**（v9 软删除态保留 deducted，v10 转换为 was_deducted）
  - `deletedAt !== 0 && stockState === 'not_deducted'` → `not_deducted`
  - `deletedAt !== 0 && stockState === 'unknown'` → `unknown`
- **DailyIntake 新增 `plannedAmountSnapshot` 和 `plannedAmountSource`**：
  - `plannedAmountSnapshot = plannedAmount`（迁移时把原 plannedAmount 复制过来）
  - `plannedAmountSource = plannedAmount != null ? 'plan_snapshot' : 'unavailable'`（历史记录无法区分打卡/补录，默认按打卡处理）
  - `plannedAmount` 保留但标记为 deprecated（P2 再清理）
- **StockLog 索引**：不变。
- **幂等**。

### 6.2 索引设计

- **DailyIntake**：`[date+supplementId]`, `[date+timeSlot]`, `planId`, `date`, `[deletedAt+date]`, `stockState`, `plannedAmountSource`
- **DosagePlan**：`supplementId`, `isActive`, `[deletedAt+isActive]`
- **PausePeriod**：`supplementId`, `startDate`, `endDate`, `deletedAt`
- **SupplementIngredient**：`[supplementId+ingredientId]`, `effectiveFrom`, `deletedAt`
- **Supplement**：`[deletedAt+status]`
- **Ingredient**：`deletedAt`
- **StockLog**：`supplementId`, `createdAt`, `[reason+relatedIntakeId]`（**普通索引，非唯一**）
- **`deletedAt` 索引策略**：直接使用 `0` / ISO 字符串，存储层与索引层统一，Repository 层零转换。
- **补录检测索引**：`[date+supplementId]` 已支持补录重复检测与次日提醒查询。

> **StockLog 唯一索引**：
> - `[reason+relatedIntakeId]` 为**普通索引**（非唯一）。
> - 幂等由应用层 `stockState` 检查保证。
> - 若需 DB 层幂等，可引入 `idempotencyKey` 字段（`${reason}:${relatedIntakeId}:${sequence}`），唯一索引建在该字段上。列为 P2 优化，v10 不实现。

### 6.3 状态管理边界

- **Dexie**：唯一数据源。
- **Zustand**：仅 UI 状态（当前日期、筛选器、弹窗开关、Toast 队列、补录弹窗状态）。
- **数据获取**：使用 `dexie-react-hooks` 的 `useLiveQuery`，禁止组件内直接 `await db.table.toArray()`。
- **多标签页一致性**：
  - `useLiveQuery`：负责**同一标签页内**数据变更的响应式更新。
  - BroadcastChannel：负责**跨标签页**通知，收到广播后触发本地 `dataVersion` 自增，`useLiveQuery` 依赖该计数器重新查询。
  - 避免重复触发：写操作后只广播，不手动 invalidate 本地 query。

### 6.4 PWA 缓存策略

- **静态资源**：CacheFirst。
- **数据**：不缓存（依赖 IndexedDB）。
- **更新策略**：`registerType: 'prompt'`，新版本就绪时 Toast 提示，由用户主动刷新。

### 6.5 日期处理

- **存储格式**：统一 `YYYY-MM-DD` 字符串。
- **时区**：以用户本地时区为准。
- **工具库**：date-fns。
- **补录日期计算**：
  - 补录范围起点：`subDays(new Date(), backfillWindowDays)`。
  - 补录范围终点：昨天（`subDays(new Date(), 1)`），不含今天。
  - 禁止未来：`date <= format(subDays(new Date(), 1), 'yyyy-MM-dd')`。
  - 补录标注：`date !== format(new Date(createdAt), 'yyyy-MM-dd')`。
  - **历史修正不受此限制**。

### 6.6 错误处理与校验

- **Schema 校验**：所有写入操作前使用 Zod 校验。
- **ErrorBoundary**：组件树顶层包裹。
- **异步捕获**：Dexie 操作统一 try/catch，失败时 Toast 提示。

**【v10修订·P-A】Zod Schema 拆分 + 日期范围校验全部下放 Service 层**：

```ts
// 新建 DailyIntake（不含任何补录窗口语义的 refine）
export const DailyIntakeCreateSchema = z.object({
  id: z.string().uuid(),
  date: z.string().date(), // 仅校验格式，范围由 Service 层按 mode 校验
  supplementId: z.string(),
  planId: z.string().nullable(),
  plannedAmount: z.number().int().nullable(),          // deprecated，P2 清理
  plannedAmountSnapshot: z.number().int().nullable(),  // 【v10新增】
  plannedAmountSource: z.enum(['plan_snapshot', 'current_plan', 'unavailable']), // 【v10新增】
  actualAmount: z.number().int(),
  timeSlot: z.string(),
  status: z.enum(['taken', 'skipped', 'extra', 'partial']),
  source: z.enum(['plan', 'manual']),
  notes: z.string().nullable(),
  stockState: z.enum(['deducted', 'not_deducted', 'was_deducted', 'unknown']),
  deletedAt: z.union([z.literal(0), z.string().datetime()]),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
})
.refine(d => {
  if (d.source === 'plan' && d.status === 'extra') return false;
  return true;
}, '计划来源不允许 extra 状态')
.refine(d => {
  // stockState 与 deletedAt 一致性
  if (d.deletedAt === 0) {
    return d.stockState === 'deducted' || d.stockState === 'not_deducted';
  }
  return d.stockState === 'was_deducted' || d.stockState === 'unknown';
}, 'stockState 与 deletedAt 不一致')
.refine(d => {
  // skipped 必须 not_deducted（v10 强约束）
  if (d.status === 'skipped' && d.stockState === 'deducted') return false;
  if (d.status === 'skipped' && d.stockState === 'was_deducted') return false;
  return true;
}, 'skipped 状态不能标记为已扣库存');

// 更新 DailyIntake（历史修正用，日期范围由 Service 层按 mode 校验）
export const DailyIntakeUpdateSchema = DailyIntakeCreateSchema
  .omit({ id: true, createdAt: true })
  .partial()
  .extend({
    date: z.string().date().optional(),
  });
```

**Service 层日期范围校验（【v10修订·P-A】）**：

```ts
// services/intakeService.ts
type IntakeDateMode = 'today' | 'backfill' | 'update'

export async function validateIntakeDate(date: string, mode: IntakeDateMode): Promise<void> {
  const today = format(new Date(), 'yyyy-MM-dd')
  const yesterday = format(subDays(new Date(), 1), 'yyyy-MM-dd')

  switch (mode) {
    case 'today':
      if (date !== today) throw new Error('今日录入的日期必须是今天')
      return
    case 'backfill': {
      const backfillWindowDays = await metaService.get('backfillWindowDays') // 运行时读取
      const min = format(subDays(new Date(), backfillWindowDays), 'yyyy-MM-dd')
      if (date > yesterday) throw new Error('补录日期不能是未来或今天')
      if (date < min) throw new Error(`补录日期不能早于 ${min}`)
      return
    }
    case 'update':
      // 历史修正不受范围限制
      return
  }
}
```

**调用约定**：
- 今日打卡 / 今日手动录入：`validateIntakeDate(date, 'today')`
- 补录：`validateIntakeDate(date, 'backfill')`
- 历史修正：`validateIntakeDate(date, 'update')`（实际上直接跳过，但保留显式调用以便审计）

**单位相关校验规则**：

1. **`Ingredient.unit` 输入转换**：用户输入 `mcg` → Zod `transform` 转为 `μg`。
2. **所有数量字段一律整数**：`dailyAmount` / `actualAmount` / `plannedAmount` / `plannedAmountSnapshot` / `stockCountInUsageUnit` / `deltaInUsageUnit` / `recommendedDailyIntake` / `upperLimit` / `amountPerServing` 统一 `z.number().int()`。
3. **`SupplementIngredient.amountPerServing`**：整数。
4. **`StockLog.deltaInUsageUnit`**：整数。
5. **`stockState` 与 `deletedAt` 一致性**：
   - `deletedAt === 0` → `stockState ∈ { 'deducted', 'not_deducted' }`
   - `deletedAt !== 0` → `stockState ∈ { 'was_deducted', 'unknown' }`
   - Zod refine 强制。
6. **`stockState` 校验**：
   - Zod 校验 `stockState` 为合法枚举值 + 与 `deletedAt` 一致性 + 与 `status` 一致性（`skipped` 不能 `deducted` / `was_deducted`）。
   - Service 层在**最终写入 DailyIntake 时**再次校验（防止中间态误伤）。
7. **`deletedAt` Zod schema**：
   ```ts
   deletedAt: z.union([z.literal(0), z.string().datetime()])
   ```
   旧版兼容：导入时 `null` 自动转 `0`。
8. **`stockState` Zod schema**：
   ```ts
   stockState: z.enum(['deducted', 'not_deducted', 'was_deducted', 'unknown'])
   ```
9. **`StockLog.reason` Zod schema**：
   ```ts
   reason: z.enum(['intake', 'manual_intake', 'undo_intake', 'restock', 'adjust', 'purge_rollback'])
   ```
   `purge_rollback` 保留以兼容旧导入文件，新建时不产生。

**补录相关校验**：

10. **`date` 范围校验（Service 层）**：
    - 普通录入（今日打卡）：`date === today`。
    - 补录：`date` 在 `[today - backfillWindowDays, yesterday]` 内。
    - **历史修正（改已有记录）不受此校验限制**。

11. **`source` 与 `status` 组合校验（Zod 层）**：
    ```ts
    .refine(d => {
      if (d.source === 'plan' && d.status === 'extra') return false;
      return true;
    })
    ```

12. **`source` 与 `status` 组合校验（Service 层）**：
    - 今日手动录入：拒绝 `manual + skipped`。
    - 补录：允许 `manual + skipped`。
    - 历史修正：允许 `manual + skipped`。

13. **补录重复检测（Service 层，事务内）**：
    - 写入前在 Dexie 事务内查询 `date + supplementId + timeSlot` 是否已有 `deletedAt === 0` 记录。
    - 若已有，抛出业务异常，UI 提示"追加"或"修改"。

14. **软删除记录禁止历史修正**：
    - Service 层 `updateIntake` 开头检查 `deletedAt !== 0`，抛出"请先恢复该记录再修改"。

**历史修正的日期范围说明**：
- 补录（新建历史记录）：限制在 `[today - backfillWindowDays, yesterday]`。
- 历史修正（修改已存在记录）：允许修改任意日期的记录。
- Service 层按 `mode` 区分，Zod schema 不再承担范围校验。

### 6.7 Service 层函数签名清单（【v10新增】）

```ts
// services/stockService.ts —— 库存状态机唯一入口
export type StockOperation =
  | { type: 'create'; intake: NewIntake; source: 'plan' | 'manual' }
  | { type: 'softDelete'; intakeId: string }
  | { type: 'restore'; intakeId: string; unknownChoice?: 'not_deducted' | 'deducted' }
  | { type: 'updateStatus'; intakeId: string; newStatus: Status; newActualAmount: number }
  | { type: 'updateAmount'; intakeId: string; newActualAmount: number }

export function applyTransition(op: StockOperation): Promise<void>

// services/intakeService.ts —— 业务编排
export function createIntake(input: DailyIntakeCreateInput, mode: IntakeDateMode): Promise<DailyIntake>
export function updateIntake(id: string, patch: DailyIntakeUpdateInput, mode: IntakeDateMode): Promise<DailyIntake>
export function softDeleteIntake(id: string): Promise<void>
export function restoreIntake(id: string, unknownChoice?: 'not_deducted' | 'deducted'): Promise<void>
export function purgeIntake(id: string): Promise<void>
export function validateIntakeDate(date: string, mode: IntakeDateMode): Promise<void>

// services/backfillService.ts —— 补录
export function getActivePlansForDate(date: string): Promise<DosagePlan[]>
export function backfillBatch(items: BackfillItem[], date: string): Promise<BackfillResult>
export function detectMissedPlans(): Promise<MissedPlan[]>

// services/metaService.ts
export function get(key: string): Promise<any>
export function set(key: string, value: any): Promise<void>
```

- `intakeService` 不直接操作 `supplements` / `stockLogs`，全部通过 `stockService.applyTransition`。
- `backfillService` 的"已服用"项也调用 `applyTransition({ type: 'create', source: 'manual' })`。

---

## 7. 体验设计规范

### 7.1 桌面端体验优化
- **导航**：侧边栏导航（今日、补剂库、成分库、服用计划、停药期、日历、统计、设置）。
- **交互**：打卡按钮醒目，表单使用弹窗或侧边抽屉，日历支持日期选择器。
- **反馈**：打卡成功即时动画；打卡后行内显示"撤销"按钮；库存不足/过期/负库存使用高亮色块。
- **撤销反馈**：撤销后 Toast 提示"已撤销，库存已回滚 2 粒"。
- **补录反馈**：
  - 补录成功 Toast："已补录 N 条记录"。
  - **部分成功 Toast**："成功 N 条，跳过 M 条（已有记录），跳过 K 条（临期补剂：XXX、YYY）"，并提供"查看并处理跳过项"入口。
  - 补录漏服 Toast："已标记 N 条漏服"。
  - 补录含负库存警告时，Toast 中附加"补剂 X 库存已为负"。
- **补录弹窗提示**：计划量基于当前计划设置，可能与当时实际不符；成分含量按历史配方计算（明示不对称性）。
- **【v10修订·P-C】`unknown` 记录恢复对话框**：
  - 标题："该记录来自旧版本"
  - 正文："无法确定当时是否扣过库存。请选择恢复方式："
  - 选项 A："视为未扣（不重扣库存）"
  - 选项 B："视为已扣（重扣 N 粒）"
  - 确认后 Toast："已恢复，库存处理方式：视为未扣/视为已扣"

### 7.2 空状态与引导
- **无数据**：插画 + 引导按钮。
- **有计划未执行**：高亮今日任务。
- **停药日**：温和提示"今日是休息日"。
- **有未记录历史项**：次日提醒 Banner，引导补录。
- **补录弹窗空状态**：所选日期无计划时，提示"该日无计划，可手动录入临时服用"。

### 7.3 无障碍
- **标签**：所有表单控件必须有 label，图标按钮需有 aria-label。
- **对比度**：预警色符合 WCAG AA。
- **补录弹窗**：键盘可操作，日期选择器支持键盘输入。
- **`unknown` 恢复对话框**：键盘可操作，默认焦点在选项 A。

---

## 8. 工程化规范

### 8.1 目录结构

```
src/
├── main.tsx
├── App.tsx
├── db/
│   ├── index.ts
│   ├── schema.ts
│   └── migrations/
├── repositories/             # 数据访问层，统一封装软删除过滤
├── services/                 # 业务逻辑层
│   ├── intakeService.ts      # DailyIntake 业务编排（调用 stockService）
│   ├── backfillService.ts    # 补录逻辑
│   ├── stockService.ts       # 【v10修订】库存状态机唯一入口 applyTransition
│   └── metaService.ts        # Meta 读写（运行时读取 backfillWindowDays）
├── stores/                   # Zustand UI 状态
├── hooks/
│   └── useMissedPlans.ts     # 次日提醒检测（支持 invalidate）
├── schemas/                  # Zod 校验 Schema
├── constants/
│   ├── units.ts              # UNIT_TYPE、INGREDIENT_UNIT、标签映射、换算常量
│   ├── deletedAt.ts          # NOT_DELETED = 0 常量
│   ├── stockState.ts         # 【v10修订】STOCK_STATE 四态常量
│   └── ...                   # 其他枚举、配置
├── types/
├── utils/
├── pages/
├── components/
│   ├── ui/
│   ├── layout/
│   └── backfill/             # 补录弹窗组件
├── styles/
└── test/
```

### 8.2 类型系统
- **强制 TypeScript**：禁止 any，数据模型定义 Interface。
- **枚举管理**：所有状态、类型、时段使用 `constants/` 中的对象或 TS Enum。
- **类型别名**：
  ```ts
  export type DeletedAt = 0 | string
  export type StockState = 'deducted' | 'not_deducted' | 'was_deducted' | 'unknown'
  export type PlannedAmountSource = 'plan_snapshot' | 'current_plan' | 'unavailable'
  ```

### 8.3 测试策略
- **单元测试**：Vitest 覆盖纯函数（成分汇总、停药计算、日期工具、单位换算、补录日期范围校验、`validateIntakeDate` 三分支）。
- **集成测试**：`fake-indexeddb` 测试 Dexie 操作及 Service 层逻辑。
- **关键流程**：
    - 打卡、库存扣减（含负库存）
    - 软删除回滚库存（`deducted → was_deducted`）、恢复重扣库存（`was_deducted → deducted`）、purge 不触碰库存
    - 历史修正改状态触发库存补扣/回滚
    - `skipped → taken` 补扣库存
    - `taken → skipped` 回滚库存
    - 幂等：重复软删除、重复恢复、`stockState` 状态校验
    - 批量操作 = 循环单条，验证每条 intake 独立写 StockLog
    - `stockState` 与 `deletedAt` 一致性校验
    - `stockState = 'not_deducted'` 时库存逻辑跳过
    - **`unknown` 记录恢复时用户二选一（视为未扣/视为已扣）**
    - **`unknown` 记录恢复选"视为已扣"时正确扣减并写 `adjust` StockLog**
    - 导入导出、软删除/恢复
    - v3 → v4 迁移脚本测试
    - v4 → v5 迁移脚本测试（`null` → `0`）
    - v5 → v6 迁移脚本测试（`stockRolledBack` → `stockRollbackState`，`stockDeducted` 回填）
    - v8 → v9 迁移脚本测试（字段合并）
    - **v9 → v10 迁移脚本测试（三态 → 四态、`deducted` → `was_deducted`、新增 plannedAmountSnapshot/Source）**
    - 导入异常清单测试（类型 A/B/C/D/E）
    - 混合类型索引实测（`[deletedAt+date]`）
    - StockLog 普通索引（非唯一）验证
    - 补录流程测试：
      - 补录"已服用"扣库存，`reason = manual_intake`。
      - 补录"漏服"不扣库存，`stockState = 'not_deducted'`。
      - 补录日期范围校验（30 天内、不含今天、禁止未来）。
      - 补录重复检测（事务内二次校验）。
      - 补录按 `date` 匹配计划与停药期。
      - 补录记录计入历史当日统计，不影响今日。
      - 补录无计划时 `status = extra`。
      - 批量补录跳过已有记录，Toast 区分跳过原因。
      - **补录 `plannedAmountSource = 'current_plan'`，打卡 `= 'plan_snapshot'`**。
    - 次日提醒检测测试：
      - 过去 7 天未记录项正确识别。
      - **排除今天**。
      - 停药日排除。
      - 已软删除记录视为"未记录"。
      - 已标记 `skipped` 的记录不再提醒。
      - 缓存 5 分钟生效。
      - **软删除/恢复后缓存主动 invalidate**。
      - **批量查询优化：7 天数据一次性查询，内存分组**。
    - `manual + skipped` 组合在 Zod 层允许、Service 层按场景拒绝。
    - 软删除记录禁止历史修正（抛异常）。
    - `skipped + stockState='deducted'` 被 Zod + Service 双层拒绝。
    - 历史修正 StockLog reason 统一为 `adjust`。
    - 所有库存操作在单个 Dexie 事务内完成。
    - **`mergeRecord` 时间相同时 DailyIntake 取 local，非 DailyIntake 表取删除意图优先**。
    - **`validateIntakeDate` 三分支（today / backfill / update）**。
    - **`stockService.applyTransition` 单一入口覆盖所有库存操作类型**。

---

## 9. 非功能需求

### 9.1 性能目标
- **首屏加载**：< 1.5s (Lighthouse)。
- **日历渲染**：1000 条记录下 < 200ms。
- **打卡响应**：点击到 UI 反馈 < 100ms。
- **次日提醒检测**：过去 7 天扫描 < 100ms（**批量查询优化必须实现**：一次性查 7 天内所有记录，内存分组比对）。

### 9.2 数据容量
- **单表上限**：支持 10000 条记录以上查询响应 < 500ms。
- **导出文件大小**：gzip 压缩后 < 5MB。
- **StockLog 容量**：10 年约 3.6 万条，IndexedDB 无压力。

### 9.3 浏览器兼容
- **目标浏览器**：Chrome 90+、Firefox 88+、Safari 14+、Edge 90+。
- **IndexedDB**：所有目标浏览器均支持 2.0+。
- **PWA**：Chrome/Edge 完整支持，Safari 部分支持（降级处理）。

### 9.4 边界与异常处理
- **断网**：PWA 缓存页面资源，IndexedDB 读写不受影响。
- **存储配额满**：监听 storage event，超出配额提示用户清理或导出。
- **导入损坏文件**：JSON 解析失败时 Toast 提示，不崩溃。
- **系统时间被修改**：所有日期以本地时区为准。
- **多标签页数据一致**：通过 BroadcastChannel 实现。
- **补录时系统时间被修改**：补录范围基于当前系统时间计算，不额外防护，提示用户"请确保系统时间正确"。

---

## 10. 数据字典

### 10.1a 服用单位（unitType）
- capsule（胶囊）
- tablet（片）
- pill（粒）
- granule（颗粒）
- bag（袋）
- stick（支）
- ml（毫升）
- g（克）
- scoop（勺）
- drop（滴）

### 10.1b 成分单位（Ingredient.unit）
- mg
- μg（**存储统一，不存 mcg**；用户输入 mcg 自动转换）
- g
- IU（不参与 mg/μg 换算）
- ml（液体成分，**不参与 μg 归一化**）

### 10.2 服用时段（timeSlot）
morning（早）、noon（中）、evening（晚）、bedtime（睡前）

### 10.3 摄入记录状态（status）
taken（已服用）、skipped（漏服）、extra（多服）、partial（**用户主动标记部分服用**）

### 10.4 记录来源（source）
plan（计划内）、manual（手动录入）

### 10.5 停药模式（cycleMode）
none、oneTime、cyclic

### 10.6 补剂状态（status）
- active：使用中，出现在今日计划。
- inactive：暂时停用，不出现在今日，保留在补剂库。
- finished：已用完，不出现在今日，库存为 0，保留历史。
- （软删除）：进回收站，不在补剂库显示。

### 10.7 评分范围
精力/睡眠/肠胃/情绪：1（很差）至 5（很好）

### 10.8 库存流水原因（StockLog.reason）
- intake：打卡扣减、恢复（restore）重扣
- manual_intake：手动录入扣减、补录已服用记录
- undo_intake：软删除（撤销）时回滚
- restock：补货入库
- adjust：手动调整、历史修正改状态（补扣/回滚）、历史修正改量（delta）、**`unknown` 恢复时用户选"视为已扣"**
- purge_rollback：保留枚举以兼容历史数据，v4 起不再产生新记录。**导入时原样保留，UI 显示"历史版本回滚（已废弃）"**

### 10.9 DailyIntake 库存状态字段

**stockState（【v10修订】单一状态机，四态）**：
- `'deducted'`：当前库存已扣（仅 `deletedAt === 0`）。
- `'not_deducted'`：当前库存未扣（补剂无库存，或 status=skipped；`deletedAt === 0` 或软删除后保持）。
- `'was_deducted'`：曾扣过库存，软删除时已回滚，恢复时需重扣（仅 `deletedAt !== 0`）。
- `'unknown'`：v3 及更早版本迁移遗留，库存状态未知，恢复时用户二选一（仅 `deletedAt !== 0`）。

**与 deletedAt 的一致性**：
- `deletedAt === 0` → `stockState ∈ { 'deducted', 'not_deducted' }`
- `deletedAt !== 0` → `stockState ∈ { 'was_deducted', 'unknown' }`

**软删除/恢复语义**：
- 软删除时：`deducted → was_deducted`（回滚库存），`not_deducted` 保持不变。
- 恢复时：`was_deducted → deducted`（重扣库存），`not_deducted` 保持不变，`unknown` 用户二选一。
- **`'was_deducted'` 是"恢复意图"标记，不是"当前库存事实"**。UI 在回收站中显示为"曾扣库存（已回滚）"。

### 10.10 deletedAt
- `0`：未删除。
- ISO 字符串：已删除时间。
- **存储层、索引层、导出层统一使用，Repository 层零转换。**
- 全项目引用 `constants/deletedAt.ts` 中的 `NOT_DELETED = 0`。

### 10.11 补录相关
- **补录（backfill）**：用户为历史日期手动创建 DailyIntake 记录。
- **补录范围（backfillWindowDays）**：默认 30 天，可配置，存于 Meta。**导入时不合并，保留本地值**。**范围终点为昨天，不含今天**。
- **次日提醒（missed plan detection）**：应用启动时 + 进入首页时检测过去 7 天计划内未记录项，缓存 5 分钟。**排除今天**。**软删除/恢复后主动 invalidate 缓存**。
- **补录选项**：
  - 已服用：创建 `taken` / `partial` / `extra`，扣库存。
  - 漏服：创建 `skipped`，不扣库存。
- **补录标注**：`date !== format(new Date(createdAt), 'yyyy-MM-dd')` 时，UI 标注"补录"。
- **补录无计划时 `status = extra`**。
- **历史修正不受 30 天限制，也不受"不含今天"限制**。
- **`partial` 由用户主动标记，不自动判定**。

### 10.12 计划量快照字段（【v10新增】）
- **plannedAmount**（deprecated）：v9 及更早的计划量快照，补录时写入失真值。v10 起新记录不再写入此字段，保留以兼容历史数据，P2 清理。
- **plannedAmountSnapshot**：补录时的计划量参考值，取自当前计划的 `dailyAmount`，**不代表 `date` 当天的真实计划量**。
- **plannedAmountSource**：
  - `'plan_snapshot'`：打卡/手动录入，计划量可信。
  - `'current_plan'`：补录，计划量为当前值，不可信。
  - `'unavailable'`：无计划或无法确定。
- **统计层使用约定**：计划依从性统计只使用 `plannedAmountSource = 'plan_snapshot'` 的记录；`'current_plan'` 的记录标注"计划量参考值不可信"，不计入依从性指标。

### 10.13 库存状态恢复选择（【v10新增】）
- **unknown 恢复选项**：
  - `'not_deducted'`：视为未扣（不重扣库存）。
  - `'deducted'`：视为已扣（重扣 actualAmount，写 `adjust` StockLog）。
- **批量恢复时**：统一弹出对话框列出所有 `unknown` 记录，逐条选择。

---

## 11. 隐私说明

- 本应用为纯前端应用，所有数据仅存储在用户浏览器本地（IndexedDB），不上传至任何服务器。
- 数据导出文件（JSON）包含用户的健康补剂摄入记录，属于敏感个人信息，用户应妥善保管。
- 应用不提供数据云端同步功能，用户需自行定期导出备份。
- 应用不使用任何第三方分析或追踪服务。

---

## 12. 优先级规划

### P0 - 核心功能（MVP）
- 补剂库 CRUD
- 成分库 CRUD
- 补剂-成分关联（含配方版本管理）
- 每日摄入记录（含 status、planId、plannedAmountSnapshot 快照、plannedAmountSource）
- 服用计划设定
- 基础校验（Zod schema）
- 软删除基础设施：所有表加 `deletedAt`、Repository 统一过滤、`softDelete`/`restore` 方法、Zod schema 强制 `deletedAt: string | 0`
- `constants/deletedAt.ts` 定义 `NOT_DELETED = 0`
- `constants/stockState.ts` 定义 `STOCK_STATE`（**【v10修订】四态**）
- 单位常量定义（constants/units.ts）
- 所有数量字段整数校验
- 手动录入支持历史日期选择（补录基础能力）

### P1 - 重要功能
- 库存管理与提醒（整数版扣减一致性，**允许负库存 + 警告**）
- 软删除 = 撤销：软删除回滚库存、恢复重扣库存、purge 不触碰库存
- **【v10修订】`stockState` 四态单一状态机**
- **【v10新增】`stockService.applyTransition` 单一入口**
- **【v10新增】所有库存操作在单个 Dexie 事务内完成**
- **【v10新增】软删除记录禁止历史修正**
- **【v10新增】`unknown` 记录恢复时用户二选一对话框**
- 历史修正改状态触发库存补扣/回滚
- StockLog 库存流水（每条 intake 一条，不聚合）
- 过期提醒
- 每日成分摄入汇总（含单位统一，ml 独立累加）
- 停药期管理（含周期性计算，并集语义）
- 日历视图（含完成度计算）
- 数据导出/导入（含强制备份 + 合并策略 + 结果摘要 + 异常清单）
- 回收站（分表 Tab）
- v3 → v4 迁移脚本
- v4 → v5 迁移脚本（`null` → `0`）
- v5 → v6 迁移脚本（`stockRolledBack` → `stockRollbackState`，`stockDeducted` 回填）
- v7 → v8 迁移脚本（StockLog 索引重建）
- v8 → v9 迁移脚本（字段合并为 stockState）
- **【v10新增】v9 → v10 迁移脚本（三态 → 四态、`deducted` → `was_deducted`、新增 plannedAmountSnapshot/Source）**
- 旧版导入兼容（`stockState` 推断 + v9 格式转换）
- 补录弹窗（含"已服用/漏服"两选项）
- 次日提醒检测与 Banner（**排除今天**）
- 补录重复检测（事务内二次校验）
- 补录范围配置（Meta.backfillWindowDays）
- 补录展示（"补录"标注）
- Zod 与 Service 分层校验（manual + skipped）
- 补录无计划时 `status = extra`
- 批量补录跳过已有记录（**Toast 区分跳过原因 + "查看并处理跳过项"入口**）
- `useMissedPlans` 缓存 5 分钟 + **主动 invalidate**
- 补录日期范围不含今天
- **【v10新增】`validateIntakeDate(date, mode)` 三分支**
- **【v10新增】`skipped + stockState='deducted'` Zod + Service 双层拒绝**
- **【v10新增】历史修正 StockLog reason 统一为 `adjust`**
- **【v10新增】次日提醒批量查询优化（必须实现）**
- **【v10新增】`mergeRecord` 时间相同时 DailyIntake 取 local，非 DailyIntake 表取删除意图优先**
- **【v10新增】`purge_rollback` 导入时原样保留，UI 显示"历史版本回滚（已废弃）"**

### P2 - 增强功能
- 身体反馈记录
- 统计报表
- 成分冲突/超标提醒
- 快速录入（常用组合）
- 库存批次管理（StockBatch）
- DosagePlan 版本管理（DosagePlanHistory）
- StockLog `idempotencyKey` 唯一索引（DB 层幂等）
- 次日提醒范围可配置
- **`plannedAmount` 字段清理（deprecated 字段移除）**

---

## 13. 待决策项

- [x] UI 组件库选型 — shadcn/ui
- [x] 状态管理方案 — Zustand（不加 TanStack Query）
- [x] 路由方案 — React Router
- [x] IndexedDB 封装库选型 — Dexie.js
- [x] 是否需要 PWA 离线支持 — 需要
- [x] 是否需要暗色模式 — 不需要
- [x] 是否需要国际化（i18n） — 不需要
- [x] 类型系统 — TypeScript
- [x] 数据校验 — Zod
- [x] 日期处理库 — date-fns
- [x] 目标平台 — PC 网页优先
- [x] 库存存储方式 — 整数（按服用单位），**允许负库存**
- [x] 软删除 DailyIntake 语义 — 方案A：软删除 = 撤销，立即回滚库存；恢复重扣；purge 不触碰库存
- [x] 幂等保障 — `stockState` + `deletedAt` 标记 + 应用层检查
- [x] 批量操作 — 放弃聚合，每条 intake 一条 StockLog，批量 = 循环单条
- [x] 回收站展示 — 分表 Tab
- [x] 自动本地快照 — 不做
- [x] `deletedAt` 未删除值 — 统一用 `0`
- [x] `constants/deletedAt.ts` — 定义 `NOT_DELETED = 0`
- [x] μg / mcg 统一 — 存储统一 `μg`
- [x] 「颗粒」独立枚举 — 与「粒」「袋」并列
- [x] unitType 清单 — capsule/tablet/pill/granule/bag/stick/ml/g/scoop/drop
- [x] Ingredient.unit 清单 — mg/μg/g/IU/ml
- [x] 汇总统一到 μg — IU / ml 独立累加
- [x] 所有数量字段一律整数
- [x] 允许负库存 + 警告
- [x] isPaused 为并集语义
- [x] 去掉导入"跳过已删除记录"选项
- [x] PWA registerType 改为 'prompt'
- [x] mergeRecord 时间相同时优先取 `deletedAt !== 0` 的一方 — **【v10修订】改为：DailyIntake 取 local，非 DailyIntake 表取删除意图优先**
- [x] 导入 DailyIntake 时状态一致性处理
- [x] v4 → v5 迁移脚本设计
- [x] 旧版导入兼容
- [x] 库存扣减判断依据 — `stockState`
- [x] 批量操作与 StockLog — 放弃聚合
- [x] 导入状态异常 — 不静默修正
- [x] v3 → v4 迁移 — `stockRollbackState` 三态
- [x] `stockRolledBack` 字段 — 被 `stockRollbackState` 取代
- [x] purge 后 StockLog 悬空引用 — 有意保留
- [x] BodyFeedback 补充 `createdAt` / `updatedAt`
- [x] 多标签页一致性 — useLiveQuery 与 BroadcastChannel 分工
- [x] 成分汇总单位选择 — 按合计后数值判定
- [x] Meta.appVersion 写入时机 — 启动时注入
- [x] 补录能力 — 支持
- [x] 补录范围 — 过去 30 天（可配置）
- [x] 次日提醒 — 支持（过去 7 天检测）
- [x] 补录弹窗选项 — 已服用 / 漏服
- [x] 漏服是否扣库存 — 否
- [x] 是否加 `takenAt` 字段 — 否
- [x] 补录库存基准 — 当前库存，不回溯
- [x] 补录计划/停药匹配 — 按 `date` 查询
- [x] 补录统计归属 — 按 `date` 计入历史
- [x] 补录展示 — `date !== createdAt` 日期时标注"补录"
- [x] v6 → v7 迁移 — 无 Schema 变更
- [x] `source=manual + status=skipped` 合法组合 — Zod 允许，Service 按场景拒绝
- [x] `plannedAmount` 快照时点 — **【v10修订】改为 `plannedAmountSnapshot` + `plannedAmountSource`**
- [x] `backfillWindowDays` 导入策略 — Meta 不合并，保留本地值
- [x] 补录无计划时 `status` — `extra`
- [x] 补录重复检测 — Dexie 事务内二次校验
- [x] StockLog `[reason+relatedIntakeId]` — 改为普通索引
- [x] `getActivePlansForDate` 语义 — 查 isActive 且未软删除
- [x] 批量补录跳过已有记录
- [x] 历史修正不受 30 天限制
- [x] 次日提醒 7 天固定
- [x] 补录 `actualAmount` 默认值 — 计划量或 1
- [x] 日历补录入口同受 30 天限制
- [x] 补录汇总不特殊标注
- [x] `useMissedPlans` 触发时机 — 启动 + 进入首页，缓存 5 分钟
- [x] 已标记 `skipped` 的记录不再被次日提醒
- [x] `stockDeducted` Zod 层不校验与 `status` 一致性
- [x] `stockState` 与 `deletedAt` 一致性 — Zod refine 强制
- [x] **【v9修订】`stockDeducted` 与 `stockRollbackState` 合并为 `stockState`**
- [x] **【v9修订】软删除记录禁止直接历史修正**
- [x] **【v9修订】历史修正 StockLog reason 统一为 `adjust`**
- [x] **【v9修订】补录日期范围改为"过去 30 天且不含今天"**
- [x] **【v9修订】今日手动录入拒绝 skipped 提示改为"今日漏服请明日通过补录功能标记"**
- [x] **【v9修订】`getActivePlansForDate` 与 `SupplementIngredient` 版本匹配不对称性为有意设计，UI 明示**
- [x] **【v9修订】`partial` 由用户主动标记，不自动判定**
- [x] **【v9修订】Zod schema 拆分为 Create / Update 两套**
- [x] **【v9修订】所有库存操作必须在单个 Dexie `readwrite` 事务内完成**
- [x] **【v10修订·P-A】`DailyIntakeCreateSchema` 不含补录窗口 refine，日期范围校验全部下放 Service 层 `validateIntakeDate(date, mode)`**
- [x] **【v10修订·P-B】`stockState` 三态改为四态，新增 `was_deducted`，消除软删除态 `deducted` 字面矛盾**
- [x] **【v10修订·P-C】`unknown` 记录恢复时用户二选一对话框（视为未扣/视为已扣），不再单方面置 `not_deducted`**
- [x] **【v10修订·M-1】`mergeRecord` 时间相同时 DailyIntake 取 local，非 DailyIntake 表取删除意图优先**
- [x] **【v10修订·M-2】补录不写 `plannedAmount` 快照，改用 `plannedAmountSnapshot` + `plannedAmountSource`**
- [x] **【v10修订·M-3】`detectMissedPlans` 排除今天；软删除/恢复后主动 invalidate `useMissedPlans` 缓存**
- [x] **【v10修订·M-4】批量补录 Toast 区分跳过原因，提供"查看并处理跳过项"入口**
- [x] **【v10修订·M-5】导入时 `purge_rollback` 原样保留，UI 显示"历史版本回滚（已废弃）"**
- [x] **【v10修订·M-6】`skipped + stockState='deducted'` 校验只在最终写入时执行**
- [x] **【v10修订·L-5】`stockService.applyTransition` 作为库存状态机唯一入口**
- [x] **【v10修订·L-4】次日提醒批量查询优化从"建议"改为"必须实现"**

---

## 附录：v10 修订要点汇总

1. **`DailyIntakeCreateSchema` 修正**：不含任何补录窗口语义的 refine，日期范围校验全部下放 Service 层 `validateIntakeDate(date, mode)`，按 `today` / `backfill` / `update` 三分支。
2. **`stockState` 四态**：`deducted` / `not_deducted` / `was_deducted` / `unknown`。`was_deducted` 严格表示"曾扣过、已回滚、恢复时需重扣"，消除 v9 软删除态 `deducted` 的字面矛盾。
3. **`unknown` 恢复二选一对话框**：用户明确选择"视为未扣"或"视为已扣"，不再单方面置 `not_deducted`。
4. **`mergeRecord` 时间相同时**：DailyIntake 取 local（避免库存副作用被反向复活删除），非 DailyIntake 表取删除意图优先。
5. **补录不写 `plannedAmount`**：改用 `plannedAmountSnapshot` + `plannedAmountSource`，统计层可识别可信度。
6. **`detectMissedPlans` 排除今天**；软删除/恢复后主动 invalidate 缓存。
7. **批量补录 Toast 区分跳过原因**，提供"查看并处理跳过项"入口。
8. **`purge_rollback` 导入时原样保留**，UI 显示"历史版本回滚（已废弃）"。
9. **`skipped + stockState='deducted'` 校验只在最终写入时执行**，避免误伤中间态。
10. **`stockService.applyTransition` 单一入口**：库存状态机逻辑集中一处，`intakeService` 不直接操作库存表。
11. **次日提醒批量查询优化必须实现**：一次性查 7 天内所有记录，内存分组比对。
12. **v9 → v10 迁移脚本**：三态 → 四态、`deducted` → `was_deducted`、新增 `plannedAmountSnapshot` / `plannedAmountSource`。
13. **13 待决策项新增 v10 的 12 个决策项**。

### 版本演进对照

| 版本 | 核心变更 |
|------|----------|
| v2 | 初版框架 |
| v3 | 整数化、负库存、软删除语义初步明确 |
| v4 | 软删除 = 撤销（方案A）、`stockRolledBack` 幂等、批量聚合 |
| v5 | `deletedAt` 统一用 `0`，废除 `null` 双轨制，Repository 零转换 |
| v6 | `stockDeducted` + `stockRollbackState` 三态、放弃批量聚合、导入异常清单 |
| v7 | 补录能力、次日提醒、漏服标记、补录范围可配置 |
| v8 | Zod/Service 分层校验、`stockDeducted` 派生字段、历史修正改状态、忽略合并入漏服、StockLog 索引调整 |
| v9 | `stockState` 单一状态机、软删除不改状态、禁止修正软删除记录、StockLog reason 统一、补录不含今天、`partial` 用户主动标记、Zod schema 拆分、Service 层矛盾校验 |
| v10 | `stockState` 四态（`was_deducted`）、`unknown` 恢复二选一、Create schema 去除范围 refine、`validateIntakeDate` 三分支、`plannedAmountSnapshot` + `plannedAmountSource`、`mergeRecord` 表差异化、次日提醒排除今天 + 批量查询、`stockService.applyTransition` 单一入口、v9→v10 迁移脚本 |

---

以上为完整 v10 全文。核心收敛点：

- **`stockState` 从 v9 的"三态 + 软删除不改状态"收敛为 v10 的"四态 + 软删除改状态为 `was_deducted`"**——`deducted` 现在严格表示"当前库存已扣"，歧义消除。
- **`unknown` 从 v9 的"单方面置 not_deducted"收敛为 v10 的"用户二选一"**——不再有不可逆的信息丢失。
- **`DailyIntakeCreateSchema` 从 v9 的"含范围 refine"收敛为 v10 的"仅格式校验 + Service 层 `validateIntakeDate` 三分支"**——消除 Schema 与 Service 层规则冲突。
- **`plannedAmount` 从 v9 的"补录时写入失真值"收敛为 v10 的"`plannedAmountSnapshot` + `plannedAmountSource` 双字段"**——统计层可识别可信度。
- **库存状态机从 v9 的"逻辑散落在多个函数"收敛为 v10 的"`stockService.applyTransition` 单一入口"**——测试覆盖一个函数即可。

这五点消除了 v9 审查中发现的全部结构性问题和中等问题。v10 可以作为动工基线。

若还需补充 **状态机图（Mermaid）**、**完整 Zod schema 文件** 或 **v9→v10 迁移脚本伪代码**，可以继续。
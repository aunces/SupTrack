
# 补剂摄入记录系统 - 需求框架

## 1. 项目概述

### 1.1 项目目标
构建一个纯前端补剂摄入记录工具，帮助用户追踪每日补剂服用情况、管理补剂库存、分析成分摄入。

### 1.2 技术选型
- **构建工具**：Vite
- **前端框架**：React
- **数据存储**：IndexedDB（本地持久化），封装库选用 Dexie.js
- **UI 方案**：shadcn/ui
- **状态管理**：Zustand
- **路由方案**：React Router
- **PWA 支持**：需要（vite-plugin-pwa）
- **暗色模式**：不需要
- **国际化**：不需要
- **TanStack Query**：不加（纯前端无后端 API，无需服务端状态管理）

### 1.3 核心约束
- 纯前端应用，无后端服务
- 所有数据存储在浏览器 IndexedDB 中
- 需考虑数据版本管理与迁移机制
- 需支持数据导出/导入，防止数据丢失

---

## 2. 数据模型

### 2.1 补剂表（Supplement）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| name | 补剂名称 |
| brand | 品牌（可选） |
| description | 描述/备注（可选） |
| unitType | 单位类型（粒/片/胶囊/克/ml 等） |
| stockCount | 当前库存数量（可选，支持不记录） |
| stockUnit | 库存单位（瓶/粒/片等） |
| productionDate | 生产日期（可选） |
| expiryDate | 过期日期（可选） |
| status | 状态（使用中/已停用/已用完） |
| createdAt | 创建时间 |
| updatedAt | 更新时间 |

### 2.2 成分表（Ingredient）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| name | 成分名称 |
| unit | 标准单位（mg/μg/IU 等） |
| recommendedDailyIntake | 推荐每日摄入量（可选） |
| upperLimit | 每日上限（可选） |
| description | 描述/备注（可选） |

### 2.3 补剂-成分关联表（SupplementIngredient）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| supplementId | 关联补剂 ID |
| ingredientId | 关联成分 ID |
| amountPerServing | 每份（每粒/每片）含该成分的量 |

### 2.4 服用计划表（DosagePlan）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| supplementId | 关联补剂 ID |
| dailyAmount | 每日计划服用量 |
| timeSlots | 服用时段（早/中/晚/睡前，可多选） |
| withMeal | 是否随餐服用 |
| isActive | 是否启用 |
| notes | 备注（可选） |

### 2.5 每日摄入记录表（DailyIntake）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| date | 日期（YYYY-MM-DD） |
| supplementId | 关联补剂 ID |
| actualAmount | 实际服用量 |
| timeSlot | 服用时段 |
| notes | 备注（可选） |
| createdAt | 记录时间 |
| updatedAt | 更新时间 |

### 2.6 停药期表（PausePeriod）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| supplementId | 关联补剂 ID（为空表示全局停药） |
| startDate | 开始日期 |
| endDate | 结束日期（可为空表示持续中） |
| reason | 停药原因（可选） |
| cycleMode | 是否周期性（如吃5停2） |
| cycleOnDays | 周期服用天数（可选） |
| cycleOffDays | 周期停药天数（可选） |

### 2.7 身体反馈表（BodyFeedback）（可选）

| 字段 | 说明 |
|------|------|
| id | 唯一标识 |
| date | 日期 |
| energy | 精力评分（1-5） |
| sleep | 睡眠评分（1-5） |
| digestion | 肠胃评分（1-5） |
| mood | 情绪评分（1-5） |
| notes | 自由文本备注 |

### 2.8 数据版本表（Meta）

| 字段 | 说明 |
|------|------|
| key | 键名（如 schemaVersion） |
| value | 值 |

---

## 3. 功能模块

### 3.1 今日记录模块
- 展示今日服用计划清单
- 一键打卡：按计划在对应时段标记已服用
- 手动录入：记录临时/非计划内的补剂服用
- 支持对历史记录进行修改、补充、删除
- 展示今日各成分摄入汇总

### 3.2 补剂库管理模块
- 补剂的增删改查
- 每个补剂关联多个成分，设定每份含量
- 记录库存数量（瓶数/粒数），支持不记录
- 库存扣减：每次服用自动扣减库存
- 过期日期记录与临期提醒
- 库存不足提醒（可设定阈值）

### 3.3 成分库管理模块
- 成分的增删改查
- 设定推荐每日摄入量和每日上限
- 查看某成分被哪些补剂包含

### 3.4 服用计划模块
- 为每个补剂设定每日服用计划（用量、时段）
- 启用/停用计划
- 计划与停药期联动：停药期间不提醒

### 3.5 停药期管理模块
- 设定指定补剂或全局的停药期
- 支持一次性停药（开始日期 + 结束日期）
- 支持周期性停药（如吃5天停2天）
- 停药原因备注
- 停药期可视化展示

### 3.6 每日成分汇总视图
- 按日期查看当天所有成分摄入总量
- 与推荐量/上限对比，超标或不足时给出提示
- 展示每种成分来源于哪些补剂

### 3.7 日历视图
- 日历热力图展示每日服用完成度
- 点击某天可查看当日详细摄入记录
- 标记漏服/多服的天数

### 3.8 统计报表（可选，低优先级）
- 周/月服用率统计
- 各成分平均日摄入量趋势
- 身体反馈与服用记录的关联展示

### 3.9 数据管理模块
- 数据导出：支持导出为 JSON 文件
- 数据导入：支持从 JSON 文件恢复
- 数据版本管理：schema 变更时自动迁移
- 清除数据：提供二次确认的全量清除功能

---

## 4. 页面结构

### 4.1 首页 / 今日
- 今日服用计划清单（按时段分组）
- 一键打卡入口
- 今日成分摄入汇总卡片
- 库存/过期预警提示

### 4.2 补剂库
- 补剂列表（卡片或表格）
- 补剂详情/编辑页
- 成分关联管理

### 4.3 成分库
- 成分列表
- 成分详情/编辑页

### 4.4 服用计划
- 计划列表
- 计划编辑页

### 4.5 停药期
- 当前生效的停药期列表
- 历史停药期记录
- 新增/编辑停药期

### 4.6 日历
- 日历视图主页面
- 日期详情弹窗/侧栏

### 4.7 统计（可选）
- 报表页面

### 4.8 设置
- 数据导出/导入
- 清除数据
- 关于/版本信息

---

## 5. 建议目录结构

```
src/
├── main.jsx
├── App.jsx
├── db/
│   ├── index.js              # Dexie.js 初始化与连接
│   ├── schema.js             # 数据表定义与版本迁移
│   └── migrations/           # 各版本迁移脚本
├── stores/                   # Zustand 状态管理
├── hooks/                    # 自定义 hooks
├── pages/                    # 页面组件
│   ├── Today/
│   ├── SupplementLibrary/
│   ├── IngredientLibrary/
│   ├── DosagePlan/
│   ├── PausePeriod/
│   ├── Calendar/
│   ├── Statistics/
│   └── Settings/
├── components/               # 通用组件
│   ├── ui/                   # shadcn/ui 基础组件
│   └── layout/               # 布局组件
├── utils/                    # 工具函数
│   ├── date.js
│   ├── export.js
│   └── calculation.js        # 成分汇总计算等
└── styles/                   # 全局样式
```

---

## 6. 优先级规划

### P0 - 核心功能（MVP）
- 补剂库 CRUD
- 成分库 CRUD
- 补剂-成分关联
- 每日摄入记录（增删改）
- 服用计划设定
- 数据导出/导入

### P1 - 重要功能
- 库存管理与提醒
- 过期提醒
- 每日成分摄入汇总
- 停药期管理
- 日历视图

### P2 - 增强功能
- 身体反馈记录
- 统计报表
- 成分冲突/超标提醒
- 周期性停药
- 快速录入优化

---

## 7. 待决策项

- [x] UI 组件库选型 — **shadcn/ui**
- [x] 状态管理方案 — **Zustand**（不加 TanStack Query）
- [x] 路由方案 — **React Router**
- [x] IndexedDB 封装库选型 — **Dexie.js**
- [x] 是否需要 PWA 离线支持 — **需要**
- [x] 是否需要暗色模式 — **不需要**
- [x] 是否需要国际化（i18n） — **不需要**
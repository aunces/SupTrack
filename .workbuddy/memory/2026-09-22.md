# 2026-09-22 工作日志

## 全项目体检（代码 + docs + Ardot 设计稿）

**触发**：用户要求「盘一下这个工作空间的项目，还有 UI『Ardot 邀请加入《SupTrack · UI 设计稿 v1.0》』(https://ardot.tencent.com/file/727847477232991)，一些文档在 docs 中，有没有可以优化的」。背景重申：自用工具，记录补剂吃没吃、剂量大的隔天吃怕记错、记录停药期。

### 实跑结论（可复现）

- `pnpm test` → **24 文件 / 345 用例全通过**（5.19s）
- `pnpm typecheck` → 0 错误
- `pnpm lint` → 0 错误
- 源码 134 文件（81 ts + 53 tsx）；6 个页面（今日/补剂/停药/日历/成分库/设置）；11 个 service；7 张表

### 核心判断（本次最重要的结论）

**代码质量远好于文档描述的状态。真正的问题是「文档三套并行、彼此打架」。**

- `IMPLEMENTATION.md` 描述的还是 v10.1 施工方案（库存 4 态状态机 / StockLog / 软删除回收站 / 8 跳迁移链 / 110 用例），而 v12.2 的 D1（余量降单一数字）+ D2（砍回收站）已把这套全删除。`src/` 里既无 `stockService.ts` 也无 `deletedAt`。它 §9 那张「Phase 0–8 全 ✅」的进度表会误导接手者按一套不存在的架构理解代码。
- `REQUIREMENTS.md`(v11.3) 的 DEV-01~21 偏差清单，多数条目随 v12 重写已自然消失。
- `v12.0-需求重梳理.md`(v12.2) + `实施指导书-AI执行版.md` + `DECISIONS.md` 才是当前有效基线，代码确实在按后两者走。

### 代码侧值得肯定（已核实，非推测）

- `utils/rate.ts` `matchesRate` 用 `diff % (on+off) < on`，`nextRateDate` 给「下次 X 日」——P2 硬规则落地。
- `utils/pause.ts` `findActivePauses` 对周期方案在**吃段直接 continue**，只让停用段产出 `paused`——这是根治 v11.1 旧 bug（用户自设「隔天吃」被渲染成「停药中」）的关键设计，注释里写明了意图。
- 失败方向统一「不隐藏」：`isBeforeRateAnchor` 参数非法→false；`cycleParams` 失败→null（R-08 / DQ-11 兑现）。
- 四态是四种不同**形状**（实心点/对勾圆/虚线圆/斜纹方块）而非同圆换色，色盲通道保住；`StateLine` 严守「休息 ≠ 停用」两个词。

### 待优化项清单（已写入 docs/项目体检报告-2026-09-22.html）

- **P0**：`IMPLEMENTATION.md` / `REQUIREMENTS.md` 与代码脱节 → 建议加弃用横幅（推荐）或移入 `docs/archive/`。
- **P1-1**：今日页满宽 vs 全站 720 居中——代码已按设计稿走满宽且 DECISIONS **D-40 有记录**，但 `UI设计提示词-v1.0.md` §2 仍写「720 居中」为硬性要求 → 文档内部自相矛盾，补例外条款即可。
- **P1-2**：走查清单编号在 v12.2 §9 与实施指导书 §13.2 之间错位，DECISIONS 引用「走查第 N 步」未注明出处。
- **P2-1**：L-2（Geist 字体）/ L-3（导入后上次导出时间变旧）/ L-4（历史日期写「今日休息」）三处遗留只记在 DECISIONS §3，代码里无锚点注释。
- **P2-2**：硬编码色值散落 4 文件（`#F3FDF8` `#343434` `#161616` `#FAFAFA`），是刻意对齐设计稿的选择，可收敛进 `@theme` 或常量但非必须。

### 与 Ardot 设计稿的关系

文件 727847477232991 已打开。代码↔设计稿关系健康且有留痕：侧栏 220 逐项对齐（D-38）、四态卡片底色照搬、四态图形尺寸照设计稿（休息改虚线圆，§5.2 优先）、今日页满宽看板（D-40 用户裁决）。**唯一真正「设计稿落后于代码」的是 L-1**：成分卡口径与位置——DECISIONS 说「M3 实现成分卡时同步更新设计稿」，M3 已落地，现在该改设计稿了。

### 产出

`docs/项目体检报告-2026-09-22.html`（HTML 卡片格式，符合用户偏好）

---

## 同日执行：P0 文档归档 + L-3 / L-4 修复（用户裁决）

用户回复：「移入 archive/，以及进行L3 L4」。

### 1. P0 文档归档

- `docs/IMPLEMENTATION.md` → `docs/archive/IMPLEMENTATION.md`
- `docs/REQUIREMENTS.md` → `docs/archive/REQUIREMENTS.md`
- 两份都在文首加了弃用横幅（含「你要找什么 → 去看哪个文件」对照表），未删内容
- 同步修正活跃文档里的陈旧路径：`v12.0-需求重梳理.md` 3 处、`实施指导书` §0.2 与 R-07 共 2 处
- `DECISIONS.md` 文首新增「文档地图」（有效 4 份 + 归档 2 份）与「走查编号约定」（统一指实施指导书 §13.2）

**docs/ 现在有效文档只有四份**：v12.0-需求重梳理.md / 实施指导书-AI执行版.md / UI设计提示词-v1.0.md / DECISIONS.md。

### 2. L-3 修复：导入后 lastExportAt 被备份文件覆盖

- 文件：`src/services/importExportService.ts`
- 改法：导入事务**末尾**（`metaService.initDefaults()` 之后）把 `lastExportAt` 重写为本次导入时间 `importedAt = nowIso()`
- **关键判断**：仅当 `!options.skipBackup` 才重写。skipBackup 时用户磁盘上没有新备份文件，保留备份里的原值才不会说假话
- 测试：`importExportService.test.ts` 新增 `describe('L-3 · 导入后的「上次导出」时间')` 2 条
- **踩坑**：测试环境是 node（无 DOM），而 `skipBackup=false` 真的会走 `exportToFile()` 去点 `<a>` 触发下载 → 报 `document is not defined` / `window is not defined`。解决办法是在测试里 `vi.stubGlobal` 三样东西：`document`（createElement 返回假 anchor + body.appendChild）、`URL`（createObjectURL/revokeObjectURL）、`window`（setTimeout 立即执行回调）。用 `vi.unstubAllGlobals()` 在 finally 清理

### 3. L-4 修复：日历在历史日期上说「今天」

- 文件：`src/pages/calendar/index.tsx`（3 处）+ `src/components/common/ItemStateView.tsx`（1 处）
- ① `BackfillEntry` rest 分支：「今日休息」→「该日休息」
- ② 详情卡统计行：「今天休息 N」→「休息 N」
- ③ 图例：「今天休息」→「休息」
- ④ **额外发现**：共用组件 `ItemStateView::StateLine` 的休息文案写死「今天不用吃」，而该组件**今日页与日历详情共用** → 改为按 `item.date === today()` 分支：今天「今天不用吃」，历史「该日不用吃」（需新 import `today`）
- 其余四态文案（待服用/已服用/停用中）不含时间指代，无需改

### 验证

`pnpm test` **347 通过**（24 文件，原 345 + 新增 2）· `typecheck` 0 错误 · `lint` 0 错误 · `build` 成功（10.37s）。

### 仍未处理（等用户决策）

- **L-1**：设计稿落后于代码（成分卡口径与位置）—— M3 已落地，该改 Ardot 稿了
- **L-2**：Geist 字体（要一致需自托管）
- **P1**：`UI设计提示词-v1.0.md` §2 仍写「720 居中」为硬性要求，与今日页满宽（D-40）冲突
- **P2**：硬编码色值散落 4 文件（刻意为对齐设计稿，非必须收敛）

### 额外产出

`docs/项目体检报告-2026-09-22.html` 增补「6. 处理结果」章节，KPI 从「345 测试 / 5 待优化」更新为「347 测试 / 2 已修复」。

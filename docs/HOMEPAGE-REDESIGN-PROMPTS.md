# SupTrack 首页 UI 重设计 · 提示词（两版对比）

> **本次目标：只看 UI 图，不落项目代码。**
> 把「PART 1 共用底座」+「PART 2A」或「PART 2B」拼成一条消息发给 AI，AI 输出**一个独立 HTML 预览文件**（含示意数据），浏览器直接打开即可看到首页 + 左侧导航长什么样。
> 本文件只出提示词，不含任何代码改动。

---

## 0. 使用说明

| 步骤 | 操作                                                                         |
| ---- | ---------------------------------------------------------------------------- |
| 1    | 复制 **PART 1** 整个代码块                                                   |
| 2    | 拼接 **PART 2A**（仪表盘式，信息密度高）或 **PART 2B**（清单式，主任务优先） |
| 3    | 同一条消息发给 AI，得到 `design-preview/` 下的一个 `.html`                   |
| 4    | 双击 HTML 用浏览器打开截图；两版分别截图后横向对比                           |

两版**共用同一套约束**（配色 token / 信息结构 / 示意数据 / 禁止事项），只在「布局与视觉取向」段落分叉，因此差异纯粹来自设计方向，可直接比较。

| 维度       | 2A 仪表盘式                | 2B 清单式                |
| ---------- | -------------------------- | ------------------------ |
| 第一眼看到 | 进度数字 + 全局状态        | 今天还剩几项没打卡       |
| 信息密度   | 高（KPI 行 + 双栏）        | 低（单列 + 大行高）      |
| 主要风险   | 容易做花、抢主任务         | 信息藏得深、找功能要滚动 |
| 适合场景   | 想同时看库存 / 成分 / 进度 | 每天只想点打卡就关掉     |

### 0.1 为什么用 HTML 预览而不是直接改代码

- 零风险：不碰 `src/`，不动构建，不需要跑 `pnpm dev`，改到满意再谈落地
- 快：一版 HTML 就能看清信息层级和配色，比在真实项目里试错快得多
- 可比较：两版用**同一份示意数据**渲染，差异只来自布局，不会被数据干扰

> 副作用（需知悉）：HTML 预览里的 shadcn 组件是**手写复刻**的，和真实 `components/ui/*`（new-york 风格）在细节上会有 1~2px 级差异。看方向足够，落地时仍需以真实组件为准。

---

## 1. PART 1 · 共用底座（必粘贴）

```markdown
# 任务

为 SupTrack 的**首页（今日页）**与**左侧导航栏**绘制一份 **UI 预览稿**，用于评审视觉方向。

**交付物：一个独立的单文件 HTML**，双击用浏览器打开就能看到完整界面（左侧导航 + 首页正文）。
页面里填**示意数据**，不需要真实功能，不需要接数据库，不需要跑构建。

**本任务绝对禁止修改项目里的任何现有文件**（`src/` 下所有内容、`package.json`、`vite.config.ts` 等一律不动），
只在 `design-preview/` 目录下新增 HTML 文件。这是纯 UI 稿，不是实现。

# 界面要表达什么

SupTrack 是「补剂摄入记录」应用，纯前端、无后端、**PC 优先**、中文界面。
用户每天打开首页，按「早 / 中 / 晚 / 睡前」四个时段打卡；偶尔补录昨日、修正记录、看今日成分合计。

这份 UI 稿必须让人一眼看出三件事：

1. **今天还剩什么没吃**（这是页面的主角）
2. **今天已经吃了什么**（弱化，但从属清晰）
3. **有什么要注意的**（库存预警、昨天漏服、成分是否超上限）

设计目标：**直观**（不用思考就知道点哪）+ **方便**（打卡 / 撤销尽量少的点击）。

# 视觉语言：严格复刻项目现有设计系统

这份稿子是给项目用的，所以视觉必须**看起来就是这个项目**，不能是通用的 AI 风格。

## 配色（唯一来源：`src/styles/index.css`）

在 HTML 的 `<style>` 里**原样复制**下列变量（oklch 值一字不改）：
```

:root {
--radius: 0.625rem;
--background: oklch(1 0 0);
--foreground: oklch(0.145 0 0);
--card: oklch(1 0 0);
--card-foreground: oklch(0.145 0 0);
--popover: oklch(1 0 0);
--popover-foreground: oklch(0.145 0 0);
--primary: oklch(0.205 0 0);
--primary-foreground: oklch(0.985 0 0);
--secondary: oklch(0.97 0 0);
--secondary-foreground: oklch(0.205 0 0);
--muted: oklch(0.97 0 0);
--muted-foreground: oklch(0.556 0 0);
--accent: oklch(0.97 0 0);
--accent-foreground: oklch(0.205 0 0);
--destructive: oklch(0.577 0.245 27.325);
--border: oklch(0.922 0 0);
--input: oklch(0.922 0 0);
--ring: oklch(0.708 0 0);
}

```

这就是一套**中性灰阶**设计系统（无色相）。因此：

- 全站只有 `background / card / muted / accent / secondary / primary(近黑) / border / destructive`
- **禁止新增任何色相**：不许蓝紫渐变、不许品牌色、不许彩色标签
- 允许的两处语义色（仅这两处）：
  1. 预警区：`border-amber-400` / `bg-amber-50` / `text-amber-900`
  2. 超上限数值：`var(--destructive)`
- 圆角只用 `--radius` 派生：`rounded-sm`(6px) / `rounded-md`(8px) / `rounded-lg`(10px) / `rounded-xl`(14px)
- 阴影克制：只允许 shadcn 默认的 `shadow-xs`（按钮/输入框）与 `shadow-sm`（卡片），禁止大阴影、发光、模糊色块
- 字体：**用系统中文栈**（`-apple-system, "PingFang SC", "Microsoft YaHei", "Segoe UI", sans-serif`）。**不要引入任何 web font**

## 组件外观基准（shadcn/ui，new-york 风格 + neutral 底色）
项目用的是 shadcn/ui，HTML 稿里请按下列规格**手写复刻**，保证观感一致：

| 组件 | 规格 |
| --- | --- |
| Button `default` | `h-9 px-4 rounded-md text-sm font-medium`，`bg: var(--primary)`，`color: var(--primary-foreground)`，hover 降 10% 不透明度 |
| Button `outline` | 白底 + `1px var(--border)` + `shadow-xs`，hover `bg: var(--accent)` |
| Button `ghost` | 无边框，hover `bg: var(--accent)` |
| Button `secondary` | `bg: var(--secondary)`，`color: var(--secondary-foreground)` |
| Button size `sm` | `h-8 px-3`；size `xs` | `h-6 px-2 text-xs` |
| Card | `rounded-xl border bg-card py-6 shadow-sm`，内含 `px-6` 的内容区，标题 `font-semibold` |
| Badge `secondary` | `h-5 rounded-md px-2 text-xs bg-secondary text-secondary-foreground` |
| Input / Select | `h-9 rounded-md border bg-transparent px-3 text-sm`，`shadow-xs` |
| Dialog | 遮罩 `rgba(0,0,0,.5)` + 居中白卡 `rounded-lg` + `shadow-lg`（**只有需要展示弹窗时才画**） |

## 图标
用 `lucide`，尺寸 `16px`（`size-4`），描边 `stroke-width: 2`，颜色继承文字色。
通过 CDN 引入：`https://unpkg.com/lucide@latest/dist/umd/lucide.js`，在 `</body>` 前调用 `lucide.createIcons()`，图标写 `<i data-lucide="pill"></i>`。
**不要用 emoji 代替图标。**

## 技术规格（写死，照做）
- 单文件 HTML，UTF-8，`lang="zh-CN"`
- 用 Tailwind v4 浏览器版：`<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>`，
  设计 token 写在 `<style type="text/tailwindcss">` 里，用 `@theme inline` 把上面的 CSS 变量映射成 Tailwind 类（`--color-background: var(--background)` 等），这样就能像项目里一样写 `bg-card` / `text-muted-foreground`
- 不要引入其他任何库（不要 React、不要图标库之外的 CDN、不要图表库）
- 画布按 **PC 1440×900** 设计；窄屏允许纵向堆叠即可，不必精修
- **不要给页面加"设计稿说明""标题页""配色说明"之类的包装**，打开就是产品界面本身

# 示意数据（两版必须完全一致，方便对比）
**必须原样使用这份数据**，不要自己另编（否则两版没法比）。

**日期**：2026-09-20 · 星期日
**副标题**：按时段完成打卡，漏服请在次日通过补录标记。

**次日漏服提醒**：昨日有 1 项未记录 → 文案「昨日有 1 项计划未记录，可补录」

**库存预警（3 条，都要显示）**：
- 库存为负：鱼油
- 临期补剂：维生素 C
- 库存不足：镁（不足 7 天用量）

**时段清单（共 7 项，5 已打卡 / 2 未打卡）**：

| 时段 | 补剂 | 计划量 | 状态 | 备注 |
| --- | --- | --- | --- | --- |
| 早 | 维生素 D3 | 1 粒 | 已打卡（实际 1 粒） | — |
| 早 | 鱼油 | 2 粒 | **未打卡** | 库存为负，行内可给一个极轻的危险提示 |
| 早 | 铁剂 | 1 片 | 未打卡 | **停药中**（Badge「停药中」） |
| 中 | 维生素 C | 1 片 | 已打卡（实际 1 片） | — |
| 晚 | 镁 | 200 mg | **未打卡** | — |
| 睡前 | 锌 | 15 mg | 已打卡（实际 15 mg） | — |
| 睡前 | 钙 | 600 mg | 已打卡（实际 600 mg） | — |

> 单位说明：粒/片/mg 都要按上表原样显示，别统一成一个单位。

**今日成分摄入（5 条，含 1 条超上限）**：

| 成分 | 摄入 | 上限 | 展示 |
| --- | --- | --- | --- |
| 维生素 D3 | 2000 IU | 4000 IU | 正常 |
| 维生素 C | 500 mg | — | 正常 |
| 镁 | 200 mg | 350 mg | 正常 |
| 锌 | 15 mg | 40 mg | 正常 |
| 钙 | 600 mg | 500 mg | **超上限 → 数值用 `var(--destructive)`** |

小字说明（原样保留）：「数据基于标签含量累加，不代表医学精确摄入量。」

**今日记录（5 条）**：
1. 维生素 D3 · 早 · 1 粒 · 已服用 · 计划打卡
2. 维生素 C · 中 · 1 片 · 已服用 · 计划打卡
3. 锌 · 睡前 · 15 mg · 已服用 · 计划打卡
4. 钙 · 睡前 · 600 mg · 已服用 · **补录**
5. 复合维生素 · 中 · 1 片 · 已服用 · **手动录入**（该补剂已删除，名称按真实逻辑显示为 `复合维生素`，不要显示占位符）

每条右侧有 `修改` / `撤销` 两个次要按钮。

**左侧导航（7 项，当前停在「今日」）**：
今日 / 补剂库 / 成分库 / 服用计划 / 停药期 / 日历 / 设置

**主操作**：`补录昨日`、`手动录入`

**空状态文案（若要画第二种状态）**：「今天没有启用的计划，先去补剂库添加补剂并创建服用计划。」

# 必须画出来的信息块（一个都不能少）
1. 左侧导航栏（含品牌标识区、7 个导航项、当前项激活态）
2. 页头：日期 + 副标题 + 主操作按钮
3. 次日漏服提醒
4. 库存预警区（3 条）
5. 时段分组打卡区（早/中/晚/睡前，7 项，含「停药中」Badge）
6. 今日记录列表（5 条，含「补录」「手动录入」来源标记，右侧 修改/撤销）
7. 今日成分摄入（5 条，含 1 条超上限标红 + 免责小字）
8. 进度的视觉表达（已完成 5 / 7）

# 交互（示意即可，不做真功能）
- 静态为主。允许极少量 JS 做 hover / 激活态 / 「打卡 ↔ 撤销」的按钮外观切换，但**不得改变 DOM 结构与信息排布**
- 所有可点元素要有 hover 与 `focus-visible` 反馈（`focus-visible` 用 `ring: var(--ring)`）
- 打卡按钮必须**整行可点**或尺寸明显足够，不能只有一个小按钮能点
- 已打卡项要明显弱化（次要色 + 状态 Badge），未打卡项要突出 —— 这是这份稿子最重要的视觉任务
- 数字统一 `font-variant-numeric: tabular-nums`

# 禁止事项（红线）
- **不改项目任何现有文件**，只在 `design-preview/` 下新增 HTML
- 不新增任何颜色 / 字体 / 渐变
- 不使用 emoji
- 不编造示意数据以外的内容（不要加"使用人数""健康评分""趋势图"这类项目里没有的东西）
- 不加"卡片 + 彩色左侧竖条"这类模板化装饰
- 不加"设计稿标题页""配色说明面板"之类的包装层
- 不要为了填满版面而堆砌内容（没有数据源的内容一律不画）

# 交付格式
1. 输出文件：`design-preview/today-A.html`（B 版为 `design-preview/today-B.html`）
2. 附一段简短设计说明：信息层级为什么这样排、如何体现「直观」和「方便」
3. 附「区块 → 数据来源」对照表，说明每块对应项目里的哪个真实字段（证明没有凭空造数据）

# 验收标准
- 双击 HTML 能在浏览器正常打开，控制台无报错
- 只出现上文定义的 CSS 变量色 + 两处允许的语义色，无任何硬编码色值
- 示意数据与本文档完全一致
- 所有 8 个信息块齐全
- 一眼能看出"还剩哪 2 项没打卡"，不需要找
```

---

## 2. PART 2A · 仪表盘式（信息密度高）

> 拼接方式：PART 1 + 本段

```markdown
# 设计方向：仪表盘式（Dashboard）

## 调性

理性、数据优先、克制。像一块「每日补剂仪表盘」：先给结论（进度 / 预警），再给行动（打卡清单），
最后给辅助明细（成分、流水）。**信息密度高，但每块都有明确职责**，不靠装饰撑场面。

## 布局结构（PC 1440 画布，主内容区 `max-w-6xl`）

从上到下：

1. **页头行**
   - 左：日期大字（`text-2xl`，tabular-nums）+ 一行小字副标题
   - 右：`补录昨日`（outline）+ `手动录入`（outline）+ 一个主操作按钮
2. **次日漏服提醒**（`MissedPlansBanner`）：一条横向提示条，带图标
3. **KPI 概览行**：一行 4 个等宽统计块（`grid grid-cols-4 gap-4`，`items-stretch`）
   - 块 1「今日进度」：进度**环**（CSS `conic-gradient` 手写）+ 中心百分比 + 下方小字 `已完成 5 / 7`
   - 块 2「待打卡」：`2`（`text-3xl` + tabular-nums）+ 小字「项」
   - 块 3「库存预警」：`3` + 小字「条」，**为 0 时显示「无」且保持中性色，不要变绿**
   - 块 4「今日成分」：`5` + 小字「种」
   - 四个块统一样式（`Card` 规格），高度对齐；数值与标签字号对比 ≥ 3 倍
4. **主体双栏**（`grid grid-cols-3 gap-6`）
   - **左列 `col-span-2`** —— 时段打卡区
     - 每个时段一张 Card；`CardTitle` = 时段名 + 右侧 `x / y` 计数（`text-xs text-muted-foreground tabular-nums`）
     - 卡内每项一行：左侧两行信息（补剂名 + 「停药中」Badge / 计划量 · 实际量 · 状态），右侧操作按钮
     - 未打卡行：整行 `cursor-pointer`，hover `bg-accent/50`，右侧 `打卡` 为 `default` 按钮
     - 已打卡行：整行降低对比（名称 `text-muted-foreground`），右侧 `撤销` 为 `ghost` 按钮
     - 已打卡行**不要加删除线**（中文划线可读性差），用颜色弱化 + 状态 Badge 表达
   - **右列 `col-span-1`** —— 侧栏信息（可 `sticky top-6`）
     - 库存预警卡（3 条，用 amber 语义色）
     - 今日成分摄入卡（5 条，钙的超限值标红，保留免责小字）
     - 今日记录卡（5 条紧凑行，右侧 `修改` / `撤销` 用 `ghost` + `xs`）
5. 侧栏三块间距一致，侧栏不出现横向滚动

## 视觉要点

- 数值是主角：`text-3xl` / `text-2xl` + `font-semibold` + `tabular-nums`；标签退到 `text-xs text-muted-foreground`
- 阴影克制：只用 Card 自带 `shadow-sm`，不叠加
- 间距统一 8px 基数（`gap-2 / gap-4 / gap-6`，内容区 `p-6`）
- 进度环实现：外层圆 `background: conic-gradient(var(--primary) 71%, var(--muted) 0)`，中心叠一个 `bg-card` 圆 + 百分比文字。**只允许用这两个 CSS 变量**，不要写死颜色。百分比与 `5 / 7` 都要有，避免单数字说不清含义

## 这个方向要避免的坑

- ❌ 不要加假折线图 / 柱状图（项目没有历史趋势数据源）
- ❌ 不要给每个 KPI 块配不同颜色
- ❌ 不要用彩色左侧竖条标记卡片
- ✅ 靠**字号层级 + 留白**建立秩序，而不是靠颜色

## 侧栏（左侧导航）在这个方向下的做法

- 宽度 `w-56`，`border-r`，`p-4`
- 顶部品牌区：一个占位方块标识（20×20 圆角方块，`bg-primary`，内有 `i data-lucide="pill"` 白图标）+ 文字 `SupTrack`（`font-semibold`）
- 导航项：`h-9 rounded-md px-3 text-sm`，图标 + 文字 `gap-2`
- 激活态：`bg-accent` + `font-medium` + `text-foreground`，左侧**不要**加彩色竖条
- 图标：今日 `calendar-check`、补剂库 `pill`、成分库 `flask-conical`、服用计划 `list-checks`、停药期 `pause-circle`、日历 `calendar`、设置 `settings`
```

---

## 3. PART 2B · 清单式（主任务优先）

> 拼接方式：PART 1 + 本段

```markdown
# 设计方向：清单式（Today List）

## 调性

极简、安静、任务感。像一本「今天要吃什么」的纸质清单：**大字号、大行高、单列聚焦**。
用户打开首页的第一动作就是从上往下打卡，不需要扫表格、不需要读 KPI。

## 布局结构（单列，主内容区 `max-w-3xl` 居中）

从上到下：

1. **页头**
   - 左：日期大字（`text-3xl`）+ 小字星期
   - 右侧**不放主按钮**；把 `补录昨日` / `手动录入` 收敛成页头下方一行次要操作（`ghost` 或 `outline` + `size="sm"` + 带图标）
   - 页头下一条横向进度条：纯 div 手写，外层 `h-1.5 rounded-full bg-muted`，内层 `bg-primary` 宽度 71%；右侧一行小字 `已完成 5 / 7`（`text-xs text-muted-foreground tabular-nums`）
2. **次日漏服提醒**：紧跟进度条下方，一条细提示（图标 + 文案），**不要做成大色块**
3. **库存预警**：压缩成**一行**提示（警告图标 + 文案 + 条数），仍用 amber 语义色，不要卡片
4. **时段清单（本页主体）**
   - 每个时段一个小标题行：`text-xs font-medium text-muted-foreground`，右侧计数；可 `sticky top-0 bg-background` 保留分组感
   - 每项是一个**大行**（`min-h-16`，`py-3`），整行就是可点区域：
     - 左：补剂名（`text-base font-medium`）+ 第二行小字「计划量 · 实际量 · 状态」
     - 右：未打卡 → `打卡` `default` `size="sm"`；已打卡 → 状态 Badge + `撤销` `ghost` `size="sm"`（默认半透明，hover 才明显）
   - 行之间用 `border-b` 分隔（**不要**每项做成独立卡片，那是仪表盘方向的做法）
   - 已打卡行整体 `text-muted-foreground`，**不加删除线**
   - 停药项（铁剂）：名称旁 Badge「停药中」，行视觉**不弱化**（它仍可让人看到）
   - hover / focus-visible 必须有明确反馈
5. **辅助信息区（视觉弱化，滚到下方才看到）**
   - 「今日成分摄入」与「今日记录」各一张 Card，标题 `text-sm`
   - 成分卡：5 条，钙的超限值标红，保留免责小字
   - 记录卡：5 条紧凑行 + `修改` / `撤销`（`ghost` + `xs`）
6. **空状态（若要画第二种状态）**：页面中央一句说明 + 一个跳转补剂库的主按钮，占 `py-24` 高度，不要缩在角落

## 视觉要点

- 字号对比要大：日期 `text-3xl` vs 辅助文字 `text-xs`。**不要一屏全是同字号**
- 行高宽松：每行 `py-3`~`py-4`，宁可少显示几项也要好点
- 阴影几乎为零：主清单区**不加卡片外壳**，直接铺在 `bg-background` 上；只有辅助信息区用 Card
- 进度条是页面唯一的「强调色」元素，其余保持中性

## 这个方向要避免的坑

- ❌ 不要把什么都塞进卡片（卡片套卡片会让页面变碎）
- ❌ 不要给时段加 emoji 或彩色圆点
- ❌ 不要因为单列而在两侧留出大片空白 —— 用 `max-w-3xl` 居中 + 统一垂直节奏来消化宽度
- ✅ 靠**行高、字号对比、极少颜色**建立"安静的工具感"

## 侧栏（左侧导航）在这个方向下的做法

- 与仪表盘版**保持同一套导航规格**（宽度、激活态、图标一致），只把宽度收到 `w-48`，让主区更宽、更聚焦
- 品牌区：占位方块标识 + `SupTrack`，`text-sm font-semibold`
- 导航项 `h-8 rounded-md px-3 text-sm`，激活态 `bg-accent font-medium`
- 视觉上比正文更"轻"：图标 `text-muted-foreground`，激活时转 `text-foreground`
```

---

## 4. 出图后的人工复核清单

| 检查项                    | 说明                                                                  |
| ------------------------- | --------------------------------------------------------------------- |
| 是否真的没碰项目代码      | `git status` 里应只多出 `design-preview/` 下的 HTML，`src/` 零改动    |
| 配色是否跑偏              | 全页只应有中性灰阶 + 两处 amber/destructive；出现任何蓝/紫/绿即为跑偏 |
| 是否加了 emoji            | 图标必须是 lucide 线性图标                                            |
| 示意数据是否一致          | 两版必须能对得上同一批数据（7 项 / 5 已打卡 / 3 条预警 / 5 条成分）   |
| 第一眼能否看出"还剩 2 项" | 这是「直观」的核心验收点，看不出就退回改                              |
| 打卡按钮好不好点          | 整行可点或按钮足够大，不能是小按钮                                    |
| 已打卡项是否被恰当弱化    | 弱化过头会让人找不到"撤销"，弱化不足会抢主任务                        |
| 是否有编造内容            | 出现趋势图、健康评分、使用人数等，一律视为 AI 自行加戏                |
| 有无"设计稿包装层"        | 打开就该是产品界面，不是带标题和配色说明的展示页                      |

# SupTrack · UI 设计提示词 v1.0

> **用途**：喂给 AI 设计/编码工具（v0、Claude、Cursor、Figma Make、Lovable 等），
> 生成可直接落进本项目的界面代码或高保真视觉稿。
>
> **法源**：`实施指导书-AI执行版.md`（施工）+ `v12.0-需求重梳理.md`（需求）+ `原型线框-v12.2.html`（结构）。
> 本文件**不新增、不修改**任何需求或状态定义，只把已有规定翻译成设计语言。
> 若本文件与实施指导书冲突，**以实施指导书为准**。

---

## 0. 怎么用这份提示词

### 0.1 三种用法

| 用法                     | 怎么喂                                                                                           | 适合                       |
| ------------------------ | ------------------------------------------------------------------------------------------------ | -------------------------- |
| **A · 一次性生成全套**   | 把 §2 主提示词 + §4 负面提示词 拼成一条消息，让 AI 先出「视觉基调 + 今日页」样本，确认后再逐页要 | 想先看风格对不对           |
| **B · 逐页生成**（推荐） | 先喂 §2 建立设计系统，再喂 §3.1 → §3.6，每次只出一页                                             | 质量最稳，改起来最省       |
| **C · 只要视觉稿**       | 用 §2 主提示词，把「输出格式」那段改成「输出 HTML + Tailwind 单文件静态稿，不接数据」            | 喂 Figma Make / 纯设计工具 |

### 0.2 喂的顺序（关键）

```
§2 主提示词  ──→  AI 生成「设计系统 + 今日页」
                     ↓ 你看一眼：中性基调对吗？四态分得开吗？
                  确认或修正（这一步别省，后面 5 页都吃它）
                     ↓
§3.1 今日页  ──→  §3.2 补剂页  ──→  §3.3 停药页  ──→  §3.4\~3.6
```

> **为什么先出今日页**：全产品的价值都在这一屏。这一页的密度与状态表达定了，其余 5 页只是同一套模式的复制。

### 0.3 一句话版（只想快速试一下时用）

```
为「SupTrack 补剂记录工具」设计今日页：PC 优先，左侧栏 220px + 内容区 720px 居中。
黑白灰中性基调，唯一强调色是近黑主按钮。核心是一张按「早上 / 中午 / 晚上 / 睡前」
分组的补剂清单，每行用「颜色 + 图形 + 文案」三维区分四种状态：待服用（琥珀实心圆点）、
已服用（绿色对勾圆）、今天休息（灰色虚线圆 + 下次日期）、停用中（紫色斜纹方块 + 恢复日）。
每行右侧固定列放按钮：待服用是实心「打卡」，已服用是描边「撤销」。
打开 5 秒内要知道今天吃什么，点一下原地完成，不跳转、不滚动、不弹窗。
不要进度条、不要百分比、不要庆祝动效、不要健康建议文案。
```

---

## 1. 设计基线速览（一页纸）

| 项                 | 内容                                                                            |
| ------------------ | ------------------------------------------------------------------------------- |
| **产品**           | SupTrack —— 个人补剂记录工具                                                    |
| **一句话**         | 帮一个人记住「今天该吃哪些补剂」                                                |
| **用户**           | 单人自用。每天打开 1–3 次，场景是**电脑前**（早餐时、睡前）                     |
| **唯一目标**       | 打开 **5 秒内**知道今天吃什么 → 点一下完成记录                                  |
| **产品定性**       | **记录工具**。不是健康管理、不是习惯养成、不是医疗建议                          |
| **平台**           | **PC 优先**（≥1024px）；窄屏只保「不破坏」，不做移动端专门适配                  |
| **技术底座**       | React + TS + Vite + Tailwind 4 + shadcn/ui(new-york / neutral) + lucide + Dexie |
| **数据**           | 纯本地，离线可用，不联网、不账号、不云同步                                      |
| **视觉基调**       | 中性、克制、高密度、无装饰、文字优先、状态驱动                                  |
| **信息架构**       | 6 个导航项（M1 只出现 4 个）：今日 / 补剂 / 停药 / 日历(M2) / 成分库(M3) / 设置 |
| **设计难度集中在** | ① 四态的可区分性 ② 打卡 5 秒闭环的视觉引导 ③ 密集场景下的信息压缩               |

---

## 2. 主提示词 · 设计系统与全局规范

> **整段复制**（含标题）。这一段的目的是让 AI 建立设计系统，之后再逐页要页面。

```
你是资深 UI 设计师兼前端工程师。请为一个「本地优先的单人工具」设计界面。

════════════════════════════════════════
一、产品背景（先理解，再动手）
════════════════════════════════════════

产品名：SupTrack —— 个人补剂记录工具。
一句话：帮一个人记住「今天该吃哪些补剂」。

用户：单人自用，每天打开 1～3 次，坐在电脑前，早餐时或睡前。
他最初的需求只有两件事：
  1) 有个补剂要隔一天吃一次，怕记混；
  2) 有些补剂要停一段时间，怕停错了日子。

唯一核心目标：**打开后 5 秒内知道今天吃什么，点一下完成记录。**

产品定性（这条决定所有设计取舍）：
这是一个**记录工具**，不是健康管理 App、不是习惯养成产品、不是医疗建议工具。
它只回答三件事：今天该吃什么（Q1）、吃了吗（Q2）、昨天吃了吗（Q3）。
它不做任何判断、不给任何建议、不出任何结论。

════════════════════════════════════════
二、技术底座（必须遵守，不得替换）
════════════════════════════════════════

- React + TypeScript + Vite + React Router
- Tailwind CSS 4（token 用 CSS 变量，项目无 tailwind.config 文件）
- shadcn/ui，style = new-york，baseColor = neutral
- 图标用 lucide（但四态图形不用图标，见第四节）
- 可用的组件原语仅这 12 个，不要发明新的：
  button / card / dialog / table / tabs / badge / checkbox /
  input / label / select / separator / toaster
- **禁止新增任何第三方依赖**（含动画库、图表库、图标库、UI 库）
- 输出 React + TSX + Tailwind class。
  不要输出独立 CSS 文件，不要用内联 style（四态图形的 CSS 绘制除外，见第四节）

════════════════════════════════════════
三、布局（PC 优先，硬性）
════════════════════════════════════════

- 左侧栏**固定 220px**，不随内容滚动
- 内容区**最大宽度 720px，居中**，内部独立滚动
  （宽屏下不要拉成一行超长列表——这是本产品最容易犯的视觉错误）
- 每页结构统一为：
    标题区（左：标题 + 一行统计；右：页面级主操作）
    → 内容区（卡片 / 清单）
- 窄屏（< 860px）：侧栏收为顶部下拉，内容区占满宽度
- **不做底部 Tab、不做触摸手势、不做 44px 热区、不做安全区适配**
- 导航项（顺序固定）：
    今日 / 补剂 / 停药 / 日历(M2) / 成分库(M3) / 设置
  M1 阶段只渲染「今日 / 补剂 / 停药 / 设置」四项，其余不显示入口
- 导航项不做图标 + 文字的堆叠，用纯文字，字重区分选中态

════════════════════════════════════════
四、视觉基调（六个词）
════════════════════════════════════════

**中性 · 克制 · 高密度 · 无装饰 · 文字优先 · 状态驱动**

具体含义：

1. **黑白灰打底。** 整个应用的强调色只有一个：近黑（primary #343434）。
   除四态语义色外，页面里不应出现任何彩色。

2. **语义色只用于状态指示，不用于装饰、不用于大面积填充。**
   四态色出现在：状态圆点/图形、状态文字、行底色（极淡）。
   不允许出现在：按钮、卡片边框、标题、图标装饰、渐变。

3. **圆角沿用现有 token：**
   控件 `rounded-md`，卡片 `rounded-xl`，圆点用 `rounded-full`。
   同一屏内圆角不超过 2 档。

4. **阴影克制。** 卡片 `shadow-sm`，弹窗 `shadow-lg`。不叠加、不做悬浮光效。

5. **数字用 `tabular-nums`。**
   余量、时间、日期、统计数字必须等宽对齐——用户会纵向比对它们。

6. **中文用系统字体栈，不引入 web font。**
   正文 14px（`text-sm`），次要信息 12px（`text-xs`），
   页面标题 20px（`text-xl`），补剂名 14px 且 `font-medium`。

7. **信息密度优先于呼吸感。**
   这是个人工具，用户每天看，不需要留白来"降低压迫"。行高紧凑，
   一屏尽量多放（目标：8 个补剂不用滚动）。

════════════════════════════════════════
五、色彩系统
════════════════════════════════════════

基础色（**已存在**，直接引用 token，不要重定义）：
  background       #FFFFFF      页面底
  foreground       #252525      正文
  muted            #F7F7F7      次级面
  muted-foreground #8E8E8E      次要文字
  border           #EBEBEB      分隔线 / 卡片边框
  primary          #343434      唯一强调色（主按钮）
  destructive      #E7000B      仅用于「删除」类危险操作

**四态语义色**（唯一允许出现在界面上的彩色）。
它们正好对应 Tailwind 调色板的整档位，**直接使用下面的工具类，不必新建 CSS 变量**：

| 状态 | 色值 | Tailwind 类 |
| 待服用 | #F59E0B | `amber-500` |
| 已服用 | #10B981 | `emerald-500` |
| 今天休息 | #94A3B8 | `slate-400` |
| 停用中 | #8B5CF6 | `violet-500` |

行底色（极淡，用于让用户"一眼跳过去"）：
  待服用行 → **无底色**，保持白（它是待办，最需要被看见）
  已服用行 → `bg-emerald-50/60`，正文转 `text-muted-foreground`
  今天休息行 → `bg-slate-50`，整体降饱和
  停用中行   → `bg-violet-50`，整体降饱和

> **重要语义澄清**：
> 琥珀在通用 UI 语义里通常表示"警告"，但**在这里它表示"这件事还没做"**。
> 因此所有相关文案里禁止出现"逾期""未按时""漏了""还差"。
> 漏服是日历页上的一个客观标记，不是对用户的指责。
> 同理，绿色表示"已记录"，**不表示"做得好"**；也没有红色表示"做得差"。

════════════════════════════════════════
六、四种状态的设计规范（本产品最重要的设计资产）
════════════════════════════════════════

每个补剂在每一天只处于四种状态之一。四态必须**同时用三个维度区分**：
**颜色 + 图形 + 文案**。任一维度失效（色盲、灰度打印、缩放）仍要能辨认。

| 状态 | 颜色 | 图形（用 DOM/CSS 绘制） | 文案模板 | 主操作 | 次要操作 |
| --- | --- | --- | --- | --- | --- |
| 待服用 | 琥珀 #F59E0B | **实心圆点** 10px | `待服用 1 粒` | **打卡**（primary 实心按钮） | — |
| 已服用 | 绿 #10B981 | **实心圆 16px + 白色对勾** | `已服用 2 粒 · 08:12` | **撤销**（outline 按钮） | 追加一次（文字按钮） |
| 今天休息 | 灰 #94A3B8 | **虚线圆** 14px（1.5px dashed 边框，无填充） | `今天不用吃 · `**`下次 9/22`** | 无 | 仍要服用（文字按钮） |
| 停用中 | 紫 #8B5CF6 | **斜纹方块** 14px（`repeating-linear-gradient(45deg,…)`） | `停用中 · 抗生素期间 · 9/30 恢复` | 无 | 仍要服用（文字按钮） |

图形实现要求：
- 全部用 `<span>` + Tailwind class 或 `background-image: repeating-linear-gradient` 绘制
- **不要用 lucide 图标替代**——圆点、对勾、虚线圆、斜纹方块是四种不同的形状，
  这是给色盲用户的区分通道
- 四个图形尺寸接近（10–16px），视觉重量递减：已服用 > 待服用 > 休息 > 停用

三条不可省略的文案细节：
1. **「今天休息」必须给出下次该吃的日期**，且该日期**加粗**。
   这是用户原始痛点（"隔一天吃怕记混"）的唯一解药，不是可选装饰。
2. **「已服用」必须显示服用时间**（`08:12`）。这是用户判断"我是不是记错了"的第一条线索。
3. **「停用中」必须写出原因与恢复日**。原因可能为空，此时省略该段，但恢复日必须有。

════════════════════════════════════════
七、通用状态（每页都要有，最容易漏）
════════════════════════════════════════

| 状态 | 要求 |
| --- | --- |
| **加载中** | **骨架屏，不用 spinner**（保留结构感，避免闪白）。**超过 300ms 才显示** |
| **出错** | 必须给「原因 + 重试」，不能只说"出错了"。文案区分数据是否丢失 |
| **空状态** | **三种，文案各不相同**：首次使用 / 全部关闭 / 筛选无结果 |
| **断网** | 可关闭的提示条，**不阻塞、不弹窗、不遮罩** |

具体文案见分页提示词。原则是：**错误信息要说清"数据还在不在"**——
这个产品的数据只存在用户本机，用户最怕的就是"丢了"。

════════════════════════════════════════
八、交互原则
════════════════════════════════════════

1. **原地反馈。** 点击后不跳转、不滚动、不弹窗。本行原地变成新状态。
   这是"5 秒闭环"的全部实现方式。
2. **不 toast 成功。** 行本身的变化已经是反馈，再弹一次是噪音。
   **只在失败时 toast。**
3. **按钮位置固定。** 清单每行右侧是固定宽度的操作列，鼠标移下来按钮都在
   同一水平位置，不需要瞄准。
4. **每个操作都有结果预期。** 按钮文案必须说明会发生什么
   （用「打卡」不用「确认」，用「撤销」不用「取消」）。
5. **hover / focus 状态必须有。** PC 端鼠标经过要有可见反馈；
   焦点环用 `focus-visible:ring-[3px]`（沿用现有 button 组件）。
6. **键盘可达。** 全流程可用 Tab 聚焦 + Enter 触发完成，
   包括建补剂和打卡。这在本项目里不是可选项。
7. **危险操作二次确认，且确认内容具体。**
   "将删除 X 及 N 条历史记录"比"确定要删除吗"有效一个数量级。
   N 必须是实时算出的真实数字。
8. **撤销永远不需要确认。** 它只是删一条记录，随时可以重做。

════════════════════════════════════════
九、文案规范
════════════════════════════════════════

- 语言：简体中文。语气：**直白、陈述、不鼓励、不评判**
- 用第二人称"你"，不用"您"（这是自用工具，不是客服）
- 不用感叹号，不用 emoji，不用"哦""啦"这类语气词
- 数量词写清楚：「1 粒」不写「1」，「2 片」不写「2」
- 日期：年内写 `9/22`，跨年写 `2027-03-01`
- 时间：24 小时制 `08:12`
- 空值显示 `—`，不显示空白，也不显示"暂无"

**禁止词表**（出现在界面上一律算缺陷）：
  医疗/健康类：过量、超标、有害、建议减少、注意补充、营养均衡、健康评分
  评判类：逾期、未按时、漏了、还差、失败、你应该
  诱导类：坚持、连续 N 天、达成、加油、太棒了
  干扰类：温馨提示、请注意、小提示（除非是真正必要的操作说明）

════════════════════════════════════════
十、禁止清单（负面约束）
════════════════════════════════════════

**这些元素一出现，方案即不合格：**

❌ 进度条、进度环、完成度百分比、任何形式的"完成了几分之几"可视化
❌ 庆祝动效、彩带、连续打卡天数、成就徽章、等级
❌ 健康建议、营养结论、参考值的超限暗示（**禁止用颜色/长度/图标暗示"超了"**）
❌ 渐变背景、玻璃拟态、大面积彩色块、装饰性插画
❌ 深色主题（本轮不做，只出 light）
❌ 移动端底部 Tab 栏、触摸手势、抽屉式导航
❌ 超过 2 层的导航层级
❌ 无意义的占位图、骷髅图、loading 旋转图标（用骨架屏）
❌ 折叠隐藏"待服用"或"已服用"（只有"今天不用吃"的项可以折叠）
❌ 在今日页放日期切换（回看是日历页的职责，两处重复会打架）

════════════════════════════════════════
十一、输出格式
════════════════════════════════════════

1. 先输出「设计系统说明」（不超过 20 行）：色板、字号、间距、四态图形的实现方式
2. 再输出页面代码：单个 TSX 文件，包含该页所有子组件
3. 数据用 mock 常量（`const MOCK_ITEMS = [...]`），**不要接真实数据层**
   mock 数据用有代表性的虚构内容：维生素 D3 / 鱼油 / 钙片 / 镁片 / 复合维生素
4. 代码里对每个关键决策加一行中文注释，说明它对应哪条规则
   （例如：`// P2：休息项必须给出下次日期`）
5. 页面末尾附一段「设计自检」，逐条回答：
   - 四态是否三个维度都可区分？
   - 打卡路径是否零跳转、零滚动、零弹窗？
   - 窄屏（375px）是否横向滚动？
   - 是否出现了禁止清单里的任何元素？

════════════════════════════════════════
十二、如果只能记住一件事
════════════════════════════════════════

用户早上坐下，打开这一页，扫一眼，点两下，合上。
整个过程不说话、不思考、不犹豫。

**所有设计取舍都以"减少这三秒里的认知负担"为准。**
好看不重要，不需要被欣赏。它应该像一张写清楚了的字条。
```

---

## 3. 分页提示词

> 用法：先喂 §2，再喂下面任意一段。每段可独立使用。
> 页面顺序即推荐生成顺序（今日页必须先出并确认）。

### 3.1 今日页 ★ 最重要的一页

````
【任务】生成「今日页」——SupTrack 产品价值全部集中在这一屏。

页面目标：打开 5 秒内知道今天吃什么 → 点一下完成记录 → 不跳转、不滚动、不弹窗。

────────────────────────────
结构（自上而下，四块）
────────────────────────────

```text
┌─ ① 标题区 ────────────────────────────────────────┐
│ 今天 · 9月20日 周日          [手动录入]（outline） │
│ 待吃 2 · 已吃 1 · 今天不用吃 2                     │
└───────────────────────────────────────────────────┘

┌─ ② 停药提醒条（条件渲染：仅当存在执行中的方案组）──┐
│ 当前停药方案：抗生素期间（3 项）           [停止]  │
└───────────────────────────────────────────────────┘

┌─ ③ 预警区（条件渲染：有内容才出现）───────────────┐
│ 库存为负：X、Y   临期：Z   余量偏低：W             │
└───────────────────────────────────────────────────┘

┌─ ④ 时段分组（主体）───────────────────────────────┐
│ 早上          1 待吃 · 1 已吃 · 3 今天不用吃       │
│  ├ 维生素 D3   1 粒 · 余量 34            [打卡]    │
│  ├ 鱼油        2 粒 · 已服用 08:12       [撤销][追加]│
│  └ ▾ 另有 3 项今天不用吃（含 1 项停用中）          │
│ 睡前          1 待吃                               │
│  └ 镁片        1 粒 · 余量 12（偏低）    [打卡]    │
└───────────────────────────────────────────────────┘

┌─ ⑤ 计划外记录（条件渲染）─────────────────────────┐
│ 今天额外记了 1 条：鱼油 1 粒 · 追加一次      [撤销]│
└───────────────────────────────────────────────────┘
```

────────────────────────────
① 标题区
────────────────────────────
- 左：「今天 · 9月20日 周日」，`text-xl font-semibold`
- 左下方一行统计：`待吃 2 · 已吃 1 · 今天不用吃 2`，`text-sm text-muted-foreground`
  - **第三个数字独立呈现，不计入任何分母**，不要把它和"待吃"合并成"2/3"
- 右：[手动录入] 次要按钮（outline）——记录没有计划的一次服用

────────────────────────────
② 停药提醒条（M2）
────────────────────────────
- 只在"存在执行中的方案组"时渲染，没有则**整条不占位**
- 内容：`当前停药方案：抗生素期间（3 项）` + 右侧 [停止]
- 视觉：浅底（`bg-slate-50`）+ 左侧紫色竖条 3px 标记停用语义
- **整条不可点、不跳转**，只有 [停止] 可交互
- 存在理由：解决"忘了自己设过停药"

────────────────────────────
③ 预警区
────────────────────────────
- 三类预警同行并列：库存为负 / 临期 / 余量偏低
- 每条是一个可点的文字，点击跳对应补剂
- 用暖色点缀（琥珀文字），**不用红色**——这不是错误，是提醒

────────────────────────────
④ 时段分组（页面主体）
────────────────────────────
- 四个固定时段：早上 / 中午 / 晚上 / 睡前
- **某时段没有内容，该分组整块不渲染**（不是显示空状态）
- 分组头：时段名 `font-medium` + 右侧统计 `1 待吃 · 1 已吃 · 3 今天不用吃`（`text-xs`）
- 行结构（`grid` 三列）：
    图形列（24px 固定） | 内容列（自适应） | 操作列（固定宽度，右对齐）
- **操作列必须固定宽度** —— 鼠标纵向移动时按钮位置不漂移，这是可用性关键
- 内容列两行：
    第一行：补剂名 `text-sm font-medium`
    第二行：`待服用 1 粒 · 余量 34`（`text-xs text-muted-foreground`）
- 打卡按钮：`size="sm"`，primary 实心
- 撤销按钮：`size="sm"` variant="outline"
- 追加一次 / 仍要服用：`size="xs"` variant="ghost"，弱化

**密集折叠规则（必须实现）**：
- 同一时段内「今天休息 + 停用中」合计 **≥ 3 项** → 折叠成一行：
  `▾ 另有 3 项今天不用吃（含 1 项停用中）`，点击展开
- **「待服用」与「已服用」永不折叠**
- 折叠状态是组件局部 state，默认折叠，不持久化
- 理由：吃 8 种补剂时四态同屏会很长，但折叠待办会让用户找不到要打勾的那一项

────────────────────────────
⑤ 计划外记录
────────────────────────────
- 只在与计划无关的手动记录存在时出现
- 一行一条：`鱼油 1 粒 · 追加一次` + [撤销]
- 视觉上比主清单**弱一档**（放在一个浅底分组里）

────────────────────────────
交互（5 秒闭环的全部）
────────────────────────────
点击 [打卡]
  ├─ 按钮立即 disabled + 加载态（**局部，不阻塞整页**）
  ├─ 成功 → 本行原地变化：
  │        图形变绿色对勾圆
  │        文案变「已服用 1 粒 · 08:12」
  │        按钮变 [撤销]
  │        余量数字立即减 1
  │        顶部统计同步（待吃 -1，已吃 +1）
  │     **不跳转、不滚动、不弹窗、不 toast**
  └─ 失败（重复打卡）→ 行内展开提示「今天已打卡」+ [追加一次]
      不进 Dialog，不弹窗

────────────────────────────
三种空状态（文案不同，不可共用）
────────────────────────────
| 场景 | 文案 | 动作 |
| 首次使用（无任何补剂） | 主：`还没有要吃的补剂` / 副：`先添加一个补剂，再设置它的服用节奏` | [添加补剂] 主按钮 |
| 全部关闭（有补剂无启用计划） | 主：`所有补剂都已停用` / 副：`启用的计划才会出现在这里` | [去补剂页看看] 次要按钮 |

加载态：骨架屏 3 行灰条，超过 300ms 才显示
错误态：`数据读取失败` / `本地数据库可能被其他标签页占用。你的数据没有丢失。` + [重试] [导出备份]

────────────────────────────
明确不做（请在代码注释里写出来，防止后人顺手加上）
────────────────────────────
❌ 日期切换   ❌ 完成度百分比 / 进度环
❌ 庆祝动效 / 连续打卡天数 / 健康评分   ❌ 「今天先不吃」快捷入口
````

### 3.2 补剂页

```
【任务】生成「补剂页」——补剂与它的服用节奏合并在同一个入口。

设计理由（会影响布局决策）：
节奏在数据上属于"计划"，但在用户心里它就是"这个补剂怎么吃"。
所以不单独开一个"服用计划"页面，两者合并展示、合并编辑。

────────────────────────────
结构
────────────────────────────
标题区：`补剂` + 右侧 [＋ 新增补剂]（primary）
主体：一张表格

列定义：
| 列 | 内容 | 说明 |
| 名称 | 维生素 D3 | `font-medium` |
| 每次量 | 1 粒 | 无启用计划显示 `—` |
| 节奏 | 隔天 / 吃 5 停 2 | **用户语言，不暴露参数**（不要写 on=1,off=1） |
| 时段 | 早上 / 早 + 晚 | 多时段用 ` + ` 连接 |
| 余量 | 34 | 空显示 `—`；负数用 destructive 色；偏低显示 `12（偏低）` |
| 状态 | 启用 / 已关闭 / 配置异常 | badge |
| 操作 | 编辑 / 删除 | 配置异常时「编辑」换成「修正」 |

────────────────────────────
「配置异常」行（独立视觉状态）
────────────────────────────
- 触发：节奏或停药参数缺失 / 非法
- 视觉：暖底 `bg-amber-50` + 加粗提示「配置异常，请修正」
- **该补剂仍然出现在今日页，并判为"该吃"** —— 宁可多显示，绝不静默隐藏
  （用户没有任何渠道发现"某个补剂被藏起来了"）
- 主操作按钮文案由「编辑」改成「修正」，点击直达表单的节奏区

────────────────────────────
「已关闭」与「今天休息」是两件事，文案不得混用
────────────────────────────
- 已关闭（`isActive = false`）：在列表里降饱和显示，**今日页完全不出现**
- 节奏休息：只在今日页表现为「今天不用吃」，列表里一切正常

────────────────────────────
新增 / 编辑 Dialog（宽 560px）
────────────────────────────
字段顺序（9 个平铺，不做折叠区，顺序不可调）：

名称 *                    [input]
单位 *                    [select：胶囊 / 片 / 粒 / 颗粒 / 袋 / 支 / ml / g / 勺 / 滴]
每次服用量 *              [number，≥1]
余量（可留空）            [number]   辅助文字：留空 = 不记录余量
服用时段 *                [4 个可多选 chip：早上 中午 晚上 睡前]
    辅助文字：一天吃两次就选两个时段，不要建两条计划
服用节奏 *                [4 段控件：每天 / 隔天 / 每 N 天一次 / 吃 N 停 M]
    ├ 选「每 N 天一次」→ 展开 [每 __ 天]（N ≥ 2）
    ├ 选「吃 N 停 M」 → 展开 [吃 __ 天] [停 __ 天]
    └ 选中任一循环节奏 → 展开 [起点 *]（默认今天）
         辅助文字：起点之前该补剂不出现，不报错
过期日（可留空）          [date]
备注（可留空）            [input]
启用这个计划              [checkbox，默认勾选]

关键设计点：
- **时段是多选 chip，不是单选**。表单里不提供"建第二条计划"的入口——
  从源头消除"同一时段重叠"的可能
- **节奏用四个用户语言的分段控件**，不让用户填 on/off 两个数字。
  「隔天」与「每 2 天一次」参数相同但用户语言不同，所以两个入口都要给
- 选「隔天」时参数自动为 吃 1 停 1，用户不需要换算

表单校验：
- 时段一个都没选 → 行内报错「请至少选择一个服用时段」，不关 Dialog
- 选循环节奏但参数缺失 → 「节奏参数不完整，请补齐」

────────────────────────────
删除确认 Dialog（唯一防误删闸门，必须认真做）
────────────────────────────
标题：`删除补剂`
正文：`将删除 维生素 D3、它的计划，以及 34 条历史记录。`
      ← **N 必须是实时算出的真实数字**，抽象提示不构成有效确认
勾选：`[✓] 同时删除该补剂的 34 条历史记录`（**默认勾选**）
  副文案：`取消勾选则保留历史记录，但补剂会被隐藏，成分汇总里这些记录将无法解析来源。`
提示：`建议先导出备份再删除。` + [前往导出] 链接
按钮：`[取消]        [删除]（destructive 实心）`
      右对齐两段式，危险按钮与取消在视觉上明确分离
```

### 3.3 停药页

````
【任务】生成「停药页」——两个分区并列，对应两种真实用法。

分区 1「停药方案组」= 成套情景，一键切换（M2）
分区 2「临时停药」  = 随手加一条，轻量高频（M1）

M1 就要把「方案组」分区的骨架渲染出来（标题 + 空状态 + 入口），
避免 M2 再改一次信息架构。

────────────────────────────
布局：两个分区并列，不藏进 Tab
────────────────────────────
```text
┌─ 停药方案组 ─────────────────────────────────────┐
│ ● 抗生素期间  [执行中]                            │
│   覆盖 3 项 · 9/18 起 · 未设结束日   [停止][编辑] │
│ ○ 服用碘剂前后                                    │
│   覆盖 2 项 · 已停止                 [执行][编辑] │
└──────────────────────────────────────────────────┘

┌─ 临时停药 ────────────────────── [＋ 加一条停药] ┐
│ 鱼油      9/19 → 9/21 · 原因：胃不舒服 [编辑][删除]│
│ 全部补剂  9/25 起 · 持续中（未设结束日）[编辑][删除]│
└──────────────────────────────────────────────────┘
```
- 执行中的方案**置顶并高亮**，用实心圆点标记；已停止的用空心圆点
- 「同一时刻至多一组执行中」——这是设计约束，不是技术限制。
  多组叠加虽然能实现，但用户无法解释"我现在到底在停哪些"

────────────────────────────
「加一条临时停药」Dialog（字段尽可能少）
────────────────────────────
停哪个 *        [select：补剂列表，末尾用分隔线隔开后是「全部补剂」]
从哪天开始 *    [date，默认今天]
到哪天结束      [date]
    标签写「到哪天为止（含当天）」← 这个括号很重要，避免用户算错一天
    辅助文字：留空 = 持续中，今日页显示「停用中 · 持续中」
原因（可留空）  [input]
    占位符：帮未来的你想起为什么停
    ← 这个占位符比"备注"有效，它说明了为什么值得填

────────────────────────────
「全部补剂」不做成独立开关
────────────────────────────
用一条 `supplementId = 'ALL'` 的条目承担，渲染成一行「全部补剂」。
**不单独做一个"全局停药"开关** —— 两个入口表达同一件事，用户会分不清。

────────────────────────────
执行方案组的轻确认
────────────────────────────
点击 [执行] 时提示：
`执行「服用碘剂前后」将结束当前执行中的「抗生素期间」，确认？`
理由：用户设过两组方案后，很容易忘了当前哪一组在跑

────────────────────────────
编辑停药时的一句提示（必须出现）
────────────────────────────
`修改停药参数只影响未来判定，已产生的记录不变。`
理由：用户改完历史日期会怀疑"那我前几天记的还算不算数"
````

### 3.4 日历页（M2）

```
【任务】生成「日历页」——回答"昨天吃了吗"。

布局：左右并排，左侧月历，右侧详情**常驻**（PC 优先，不需要抽屉）

────────────────────────────
左：月历
────────────────────────────
- 顶部：`‹ 2026 年 9 月 ›` + [回到今天]
- 表头：一 二 三 四 五 六 日
- 每格：日期数字 + 最多 **3 个状态点**，超出显示 `+N`
- 状态点颜色沿用四态色
- **今日用边框加粗标记，不用填充色** —— 填充色会和"已服用"的绿色填充混淆
- 未来日期灰显、不可点

日级状态 → 视觉映射：
| 状态 | 视觉 | 图例文案 |
| done | 绿色实心 | 已服用 |
| partial | 绿色半填充 | 部分完成 |
| missed | 橙红描边 | 漏服 |
| rest | 灰色 | 今天休息 |
| paused | 紫色 | 停用 |
| empty | 无标记 | — |

图例放在月历下方一行，`text-xs`

────────────────────────────
右：日期详情（常驻）
────────────────────────────
- 标题：`9 月 19 日 周六` + 统计 `已吃 1 · 停用 2`
- 逐条列出该日补剂，**复用今日页同一套状态行组件**（同一批数据只出现一次的表现形式）
- 底部按条件渲染补录入口：

| 条件 | 显示 |
| 在过去 7 天内 且 有可补录的应服项 | `[+ 补录一条]` |
| 超过 7 天 | `超出补录窗口`（无按钮） |
| 该日为休息日 | `今日休息`（无补录入口） |
| 该日为停用日 | `停用中 · X 日恢复`（无补录入口） |

────────────────────────────
一条必须写进代码注释的规则
────────────────────────────
**「漏服」不需要用户手动标记** —— 过了当天仍未打卡，自动就是漏服。
用户要做的只有"补录"这一件事。
```

### 3.5 成分库（M3）

````
【任务】生成「成分库」——两件事：管理成分主数据，看每日汇总。

⚠ 这一页有合规边界，是本产品最需要克制的地方。请严格按下面写。

────────────────────────────
分区 1：成分列表
────────────────────────────
| 列 | 说明 |
| 成分名 | `name` 是跨补剂归并的唯一锚点 |
| 单位 | mg / μg / g / IU / ml |
| 参考摄入量 | 用户自填；空显示 `—` |
| 上限 | 用户自填；空显示 `—` |
| 来源补剂 | `2 个`，**可点开展开** |
| 操作 | 编辑 |

「来源补剂」展开内容（发现重复成分的唯一入口）：
```text
维生素 D3 被以下补剂包含：
  · 维生素 D3 胶囊 · 每份 1000 IU · 生效 9/1 起（当前有效）
  · 复合维生素    · 每份 400 IU  · 生效 9/15 起（当前有效）
```
已失效的配方保留可见但降饱和 —— 用户需要看到"改过配方"，不是痕迹被抹掉

编辑 Dialog：
成分名 *        [input]   辅助文字：跨补剂归并按名称匹配，请与瓶子上的写法保持一致
单位 *          [select]  辅助文字：IU 与 ml 独立累加，不与 μg 混算
参考摄入量      [number]  留空 = 不做比较
上限            [number]  留空 = 不显示
备注（可留空）  [input]

────────────────────────────
分区 2：每日成分汇总卡（今日页也复用）
────────────────────────────

⚠⚠ 这是全篇最需要克制的组件，请逐条执行：

正确形式：
  维生素 D3      参考 800 IU      今日 1000 IU
  钙             参考 600000 μg   今日 800000 μg

禁止：
  ❌ 进度条、双色条、任何长度可视化
  ❌ 用颜色区分"是否接近参考值"
  ❌ 「过量」「超标」「建议减少」「注意」等任何结论性措辞
  ❌ 「达标」「不错」等正向评价（同样是一种判断）
  ❌ 用图标暗示超限（⚠、↑、红色数字）

数字对齐用 `tabular-nums`。参考值为空则该列显示 `—`，不做比较。
展示顺序按成分名，不按"离参考值远近"排序。

────────────────────────────
系统不预置任何默认阈值
────────────────────────────
参考摄入量与上限**全部由用户自填**，留空即为"不比较"。
不要内置任何营养学常识数值（即使你知道）。

────────────────────────────
配方变更（写在补剂编辑 Dialog 内，不单独开页面）
────────────────────────────
- 改配方不修改原记录：原记录失效日 = 昨天，新建记录生效日 = 今天
- 回看历史时按记录所属日期匹配当时配方 → 历史数字不因今天改配方而变动
- 这条决定了汇总卡必须标注"按 9/20 的配方计算"之类的口径说明

────────────────────────────
合规底线（写在文件顶部注释里）
────────────────────────────
本页只做「累加」与「并列」，不出任何结论。产品是记录工具，不是营养建议工具。
````

### 3.6 设置页

```
【任务】生成「设置页」——三个分区，无开关。

────────────────────────────
分区 1 · 数据
────────────────────────────
[导出 JSON]  [导入 JSON]
上次导出：2026-09-18 21:03          ← **必须常驻显示**
辅助文字：导入将清空当前数据并替换为备份内容（导入前会自动备份）

设计理由：这行时间让"多久没备份了"变得可感知。
这个产品的数据只存在本机，没有第二份，导出是唯一的保险。

────────────────────────────
分区 2 · 危险区
────────────────────────────
清除全部数据
辅助文字：需输入「清除」二次确认，且不可恢复
→ 点击后弹 Dialog，要求**输入文字「清除」**才能启用确认按钮。
  不用普通 confirm —— 破坏面太大。

────────────────────────────
分区 3 · 关于
────────────────────────────
版本 0.2.0
全部数据只存在本机浏览器，不上传、不联网、不收集信息
本应用是记录工具，不是医疗建议
删除后无法恢复，请定期导出备份

────────────────────────────
明确不做
────────────────────────────
M1 不放任何开关（默认时段 / 日期格式 / 是否显示余量 / 主题）。
**每个设置项都是一个需要长期维护的决策**，不到真实需要不加。
```

---

## 4. 负面提示词 · 禁止清单

> 单独一段，可追加到任何提示词末尾。**这一段的效力不低于前面的要求。**

```
【必须避免的】以下元素一出现即为设计缺陷：

视觉
❌ 渐变背景、玻璃拟态、毛玻璃、霓虹、发光描边
❌ 大面积彩色块、彩色卡片、彩色标题
❌ 装饰性插画、吉祥物、空状态配大图
❌ 超过 2 档圆角混用、超过 2 种阴影
❌ emoji 当作图标
❌ 深色主题（本轮只做 light）
❌ 移动端底部 Tab 栏、触摸手势、抽屉导航、44px 热区

功能可视化
❌ 进度条 / 进度环 / 完成度百分比 / "完成 2/3"
❌ 连续打卡天数 / 成就徽章 / 等级 / 健康评分
❌ 折线图、柱状图、趋势图（统计图表属最低优先级，且本轮不做）

文案
❌ 医疗健康结论：过量、超标、有害、建议减少、营养均衡
❌ 评判用户：逾期、未按时、漏了、还差、你应该
❌ 行为诱导：坚持、加油、太棒了、达成目标
❌ 感叹号、语气词、卖萌文案

交互
❌ 打卡后跳转页面 / 滚动 / 弹窗 / 弹 toast
❌ 折叠"待服用"或"已服用"项
❌ 无可视反馈的按钮
❌ 按钮位置随内容宽度漂移
❌ 只靠颜色区分状态（必须配合图形与文案）

配置
❌ 新增任何第三方依赖
❌ 内置任何营养学默认阈值
❌ 在今日页放日期切换
❌ 在设置页加任何开关
```

---

## 5. 设计 Token 附录

### 5.1 四态色：直接用 Tailwind 调色板，不必新建变量

**一个有用的巧合**：四个状态色正好落在 Tailwind 调色板的整档位上——
这意味着不需要往 `:root` 里加任何东西，所有颜色都能用工具类写完。

| 语义     | 色值      | **Tailwind 类** | 浅底（行背景用）   |
| -------- | --------- | --------------- | ------------------ |
| 待服用   | `#F59E0B` | `amber-500`     | 无底色（保持白）   |
| 已服用   | `#10B981` | `emerald-500`   | `bg-emerald-50/60` |
| 今天休息 | `#94A3B8` | `slate-400`     | `bg-slate-50`      |
| 停用中   | `#8B5CF6` | `violet-500`    | `bg-violet-50`     |

> 现有 `:root` 是 shadcn 的 neutral 单色系（oklch 表达），**不要改动它**。
>
> 如果你希望语义可读性更强，也可以额外加 4 行变量（可选，非必需）：
>
> ```css
> :root {
>   --state-pending: #f59e0b;
>   --state-taken: #10b981;
>   --state-rest: #94a3b8;
>   --state-paused: #8b5cf6;
> }
> ```

### 5.2 四态图形实现速查

核心原则：**四个图形是四种不同的形状**，不是同一个圆换四个颜色。
色盲用户、灰度打印、缩放场景下，形状是唯一还活着的区分通道。

| 状态     | 形状                   | 实现                                                                                                                                                |
| -------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| 待服用   | 实心圆点 10px          | `<span class="size-2.5 shrink-0 rounded-full bg-amber-500" />`                                                                                      |
| 已服用   | 实心圆 16px + 白色对勾 | `<span class="size-4 shrink-0 rounded-full bg-emerald-500 grid place-items-center">` + lucide `<Check class="size-3 text-white" strokeWidth={3} />` |
| 今天休息 | 虚线圆 14px（无填充）  | `<span class="size-3.5 shrink-0 rounded-full border-[1.5px] border-dashed border-slate-400" />`                                                     |
| 停用中   | 斜纹方块 14px          | 见下方注                                                                                                                                            |

> **斜纹方块的实现**：`repeating-linear-gradient` 写在 Tailwind 任意值里时，嵌套逗号
> 容易在构建时被误解析。**推荐在 `index.css` 里加一个语义类**，比堆任意值稳妥：
>
> ```css
> @layer components {
>   .swatch-paused {
>     width: 0.875rem;
>     height: 0.875rem;
>     border-radius: 3px;
>     background-image: repeating-linear-gradient(
>       45deg,
>       var(--state-paused, #8b5cf6) 0 2px,
>       transparent 2px 4px
>     );
>   }
> }
> ```
>
> 注意：**这是四态图形，不是装饰**，所以不受"不用独立 CSS 文件"的限制
> —— 但请只加这一个类，不要借机引入更多样式。

### 5.3 组件与变体映射

| 场景                     | 组件与变体                                                     |
| ------------------------ | -------------------------------------------------------------- |
| 打卡（主操作）           | `Button size="sm"`（default = primary 实心）                   |
| 撤销                     | `Button size="sm" variant="outline"`                           |
| 追加一次 / 仍要服用      | `Button size="xs" variant="ghost"`                             |
| 页面级主操作（新增补剂） | `Button size="default"`                                        |
| 危险操作（删除）         | `Button variant="destructive"`                                 |
| 状态标记                 | `Badge variant="secondary"`，四态各自着色                      |
| 分组容器                 | `Card`（`shadow-sm rounded-xl`）+ `CardHeader` / `CardContent` |
| 列表                     | 用 `div` + `grid`，不用 `Table`（表格语义不适合卡片式清单）    |
| 补剂页 / 成分库列表      | 用 `Table`（真正的二维数据）                                   |
| 弹窗                     | `Dialog`，补剂表单宽 560px                                     |

### 5.4 排版速查

| 用途                      | class                                     |
| ------------------------- | ----------------------------------------- |
| 页面标题                  | `text-xl font-semibold`                   |
| 分组标题（时段名）        | `text-sm font-medium`                     |
| 列表主文本（补剂名）      | `text-sm font-medium`                     |
| 列表副文本（用量 · 余量） | `text-xs text-muted-foreground`           |
| 统计行                    | `text-sm text-muted-foreground`           |
| 数字                      | 上述基础上加 `tabular-nums`               |
| 间距（页面内）            | `p-6` `gap-4` `gap-6`（沿用现有页面模式） |

---

## 6. 生成后自检清单

> 每页生成完对照一遍。这份清单与实施指导书 §13.2 的 21 步走查互补：
> 那边查功能，这边查设计。

### 通用

- [ ] 侧栏 220px、内容区 720px 居中、内容区独立滚动
- [ ] 无新增依赖，只用了 12 个已有原语
- [ ] 页面无彩色（除四态语义色）
- [ ] 所有数字 `tabular-nums`
- [ ] 每个可点元素有 hover 与 focus-visible 状态
- [ ] 错误态给了「原因 + 重试」，空状态文案区分场景

### 今日页专项

- [ ] 四态在**颜色、图形、文案**三个维度都可区分
- [ ] 「今天休息」有下次日期且加粗
- [ ] 「已服用」有服用时间
- [ ] 「停用中」有原因与恢复日
- [ ] 操作列固定宽度，按钮纵向对齐
- [ ] 打卡零跳转、零滚动、零弹窗、无成功 toast
- [ ] 待服用与已服用项**不参与折叠**
- [ ] 顶部统计的第三个数字独立，不进分母
- [ ] 窄屏 375px 下无横向滚动，打卡入口不被遮挡

### 合规专项（M3 尤其）

- [ ] 成分汇总**只有数字并列**，无进度条、无颜色暗示
- [ ] 全文无「过量 / 超标 / 建议减少 / 否则」类措辞
- [ ] 无任何内置默认阈值
- [ ] 无庆祝动效、无连续天数、无健康评分

### 一句话终检

> 用户早上坐下，扫一眼，点两下，合上。
> 整个过程不说话、不思考、不犹豫。
>
> **如果某处设计让这个流程多停留了一秒，就回去删掉它。**

---

## 7. 这份提示词与上游文档的对应关系

| 本文件位置      | 依据                                                       |
| --------------- | ---------------------------------------------------------- |
| §2 技术底座     | 实施指导书 §4.1 技术栈白名单                               |
| §2 布局         | 实施指导书 §8 前言、原型线框 ⓪ 全局骨架                    |
| §2 色彩系统     | `src/styles/index.css` 现有 token + 实施指导书 §8.1 四态表 |
| §2 六、四态规范 | 实施指导书 §8.1 四态渲染规范表 + P1/P2 硬规则              |
| §2 七、通用状态 | 实施指导书 §8.7                                            |
| §2 十、禁止清单 | 实施指导书 §3 红线 R-02 / R-03 / R-11，§8.1 不做清单       |
| §3.1 今日页     | 实施指导书 §8.1                                            |
| §3.2 补剂页     | 实施指导书 §8.2                                            |
| §3.3 停药页     | 实施指导书 §8.3                                            |
| §3.4 日历页     | 实施指导书 §8.4                                            |
| §3.5 成分库     | 实施指导书 §8.5                                            |
| §3.6 设置页     | 实施指导书 §8.6                                            |
| §5.3 组件映射   | `src/components/ui/` 实际变体                              |

**注**：本文件为设计视图，**不具备需求效力**。任何设计上的疑问，
以 `实施指导书-AI执行版.md` 为准；若指导书未覆盖，按 v12.2 需求文档处理。

---

_本方案为产品设计参考，具体实施请结合实际情况确认。示例数据均为虚构。_
